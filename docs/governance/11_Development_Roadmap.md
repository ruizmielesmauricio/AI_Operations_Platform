# 11_Development_Roadmap.md

**Version:** 1.36 (Draft)
**Status:** Draft
**Phase:** Phase 1 – Company Foundation
**Author:** Founder & CTO
**Last Updated:** 08/08/2026

---

# Document Contract

## Purpose

This document defines the delivery roadmap at a governance level: the phases, what each phase must produce, and the gates that must be passed before moving on.

It is written to **map directly onto Jira epics** so that this document remains the strategic source of truth while Jira handles day-to-day execution tracking. Each phase below corresponds to an epic; each deliverable corresponds to a story.

**Jira project:** `COP` — https://mauricio-copilot-app.atlassian.net/jira/software/projects/COP/boards/1/timeline

*Note: the Jira instance is private and cannot be read from outside the authenticated session. This document does not attempt to mirror Jira's current state — it defines what the roadmap should contain, so Jira can be populated from it.*

---

## Audience

* Founder
* Engineering Team
* Future Employees
* Investors

---

## In Scope

* Delivery phases and their objectives
* Deliverables per phase, structured as Jira epics and stories
* Milestone gates and exit criteria
* Sequencing rationale

---

## Out of Scope

This document intentionally does **not** define:

* Requirements detail (see `10_Product_Requirements.md`)
* Technical implementation (see `03_System_Architecture.md`)
* Sprint-level task breakdown (lives in Jira)
* Commercial forecasting (see `09_Business_Model.md`)

---

## Related Documents

* 10_Product_Requirements.md
* 15_Customer_Discovery.md
* 12_Decision_Register.md

---

# Executive Summary (TL;DR)

The roadmap deliberately separates **product validation** from **commercial launch**. The founder builds a working prototype and validates it with real bike shops *before* taking on company registration, legal costs, and production billing.

Seven phases, four gates. Nothing proceeds past a gate without evidence, not optimism.

The most important sequencing decision: **customer discovery comes before serious build.** Phase 1 interviews should inform what gets built in Phase 2, not the other way round.

---

# Jira Mapping Convention

| This Document | Jira |
|---|---|
| Phase | Epic |
| Deliverable | Story |
| Acceptance criterion | Story acceptance criteria |
| Gate | Epic-level "Definition of Done" / milestone on timeline |

**Suggested Jira labels:** `phase-0` … `phase-7`, `gate-a` … `gate-d`, `governance`, `backend`, `frontend`, `data`, `ai`, `billing`, `discovery`

---

# Phase 0 — Documentation and Decisions

**Epic:** `COP — Phase 0: Foundation Documentation`
**Status:** Substantially complete

| Deliverable | Status |
|---|---|
| Company Constitution | Complete |
| Project Vision | Complete |
| Operational Domains | Complete |
| System Architecture | Complete |
| Technology Stack | Complete |
| AI Architecture | Complete |
| Database Design | Complete |
| Deployment Guide | Complete |
| Cost Analysis | Complete |
| Business Model | Complete |
| Product Requirements | Complete |
| Development Roadmap | Complete (this document) |
| Decision Register consolidated | Complete |
| Branding Strategy | In progress |
| Repository structure created | Complete |

**Outstanding housekeeping:**

- [x] Consolidate the former decision registers into `12_Decision_Register.md`
- [x] Update `01_Project_Vision.md` to remove the import-template guidance (superseded by PD-006; replaced by the "Low-Friction Use" product principle)
- [x] Update `06_Database_Design.md` and `10_Product_Requirements.md` if ADR-016 is accepted — done 04/08/2026; `10_Product_Requirements.md` reviewed and needed no change (no repairs-specific content exists there)
- [ ] Generate remaining individual decision files in `docs/decisions/` — partially started 04/08/2026: ADR-016, ADR-022, ADR-023 now have files; full historical backfill remains

---

# Phase 1 — Customer Discovery

**Epic:** `COP — Phase 1: Customer Discovery`
**Priority:** Highest. Nothing else matters until this is done.

| Deliverable | Acceptance Criteria |
|---|---|
| Interview script finalised | `15_Customer_Discovery.md` reviewed and ready to use |
| Interview target list built | 25–30 Irish independent bike shops identified with contact details |
| 15–20 interviews completed | Each recorded in `docs/business/interviews/` |
| POS/export formats catalogued | Which systems shops actually use, and what their exports look like |
| Sample data files collected | At least 5 real (anonymised) export files for schema-detection testing |
| Pain point register populated | Top 10 pain points ranked by frequency and severity |
| Assumptions scored | Every assumption in `15_Customer_Discovery.md` marked Validated / Not / Unsure |
| Willingness-to-pay evidence gathered | Test commercial response to the accepted €80/month price |
| Module priority ranked | Which of the five domains customers actually value most |

**Why sample data files matter more than they look:** PD-006 commits the platform to schema-agnostic ingestion. That is impossible to build or test without real, messy export files. Collecting these during interviews is the single highest-value technical output of Phase 1.

---

## Gate A — Problem Validation

Proceed only when:

- Interviews confirm repeated, specific pain points (not vague dissatisfaction)
- Businesses can and will provide usable data
- At least one module shows clear, articulated value
- At least some evidence exists that owners would pay for it

**If Gate A fails:** stop and reconsider the segment or the product, before writing production code.

---

# Phase 2 — Technical Prototype

**Epic:** `COP — Phase 2: Technical Prototype`
**Environment:** Local or private only. No customer data, no billing.

| Deliverable | Notes |
|---|---|
| Repository scaffolding | Backend, frontend, Docker, CI skeleton |
| Authentication | Supabase Auth integration; session verified by API |
| Business creation and template selection | Bicycle shop template first |
| Tenant isolation | Enforced and tested from day one, not retrofitted |
| File upload to object storage | Signed URL flow via R2 |
| **Schema detection engine** | Alias dictionary + structural heuristics; tested against real Phase 1 files |
| Column mapping confirmation UI | Plain language, sample values shown (PR-2.4) |
| Mapping profile persistence | Second upload requires no input (PR-2.5) |
| Validation and normalisation | Deterministic; plain-language errors (PR-2.8) |
| Transactional import | Idempotent, reversible |
| Core KPI calculation | Revenue, gross margin, stock cover, repair turnaround |
| Basic dashboard | ECharts, browser-rendered |
| AI explanation layer | Behind gateway; structured input only |
| Import deletion / undo | PR-2.11 |
| Unit tests for all formulas | ED-007 |
| Tenant isolation integration tests | ED-008 |

**Explicitly deferred from Phase 2:** billing, email, monitoring, multi-user roles, forecasting.

---

## Phase 2/3 Detail — Prototype Build Sequence

The founder-agreed, ordered build checklist for the free-tier prototype (spans Phase 2 and pulls forward a few Phase 3 items — conversational agent, alerts, Stripe test mode — since testing the full service offering end-to-end was judged more valuable at this stage than strictly following the phase boundary). This is the sequence actually followed; update it in place as items complete rather than tracking progress elsewhere.

**Stage A — Foundation**
- [x] A1. Create the database (local Postgres via docker-compose)
- [x] A2. Create the full schema — shared core, canonical entities, bicycle-shop template extension (`06_Database_Design.md`)
- [x] A3. Authentication (Supabase Auth — backend JWT verification + frontend client)
- [x] A4. Business signup + template selection (PR-1)
- [x] A5. Tenant isolation enforcement + automated tests (PR-6.1/6.2, ED-008) — built now, not retrofitted

**Stage B — Getting real data in**
- [x] B6. Upload data interface (signed URL to R2)
- [x] B7. Schema/column detection engine — alias dictionary + structural heuristics, tolerant of any POS export format (PR-2, the "critical section") — supports both `sales` and `inventory` entity types (entity type chosen explicitly at upload time, not auto-detected)
- [x] B8. Clean data — deterministic normalisation, validation, plain-language rejected-row reporting, transactional import, undo (PR-2.6-2.11) — includes SKU/name product matching (auto-create on first sight) and inventory_movements recording per sale, needed for stock-level/popularity tracking; frontend "Run import"/"Undo" trigger UI added, and PR-2.2's manual header-row-picker escape hatch built for files auto-detection can't confidently place — both verified live end-to-end
- [x] B8b. Inventory upload entity type — stock-count snapshot uploads reconcile against derived stock (stock is never stored directly, only summed from `inventory_movements`) by writing a single `adjustment` movement per product equal to `uploaded_count - current_derived_stock`; guards against corruption if a sales import is undone *after* a later inventory reconciliation has baked its effect into a subsequent adjustment (blocked with a clear error instead of silently under/over-counting stock) — verified via automated tests and a live end-to-end browser walkthrough exercising the undo-guard scenario for real
- [x] B8c. Purchases and repairs upload entity types, plus a restock-freshness indicator. **Purchases** — restocking as its own transactional entity type (the `"purchase"` reason on `inventory_movements`, reserved since the model was first designed, now has its first writer): each row an independent `+quantity_received` movement, `unit_cost` (optional) seeds a new product's `cost_price` or overwrites an existing one via a new `ProductRepository.update_cost_price` — the first update path `cost_price` has ever had. **Repairs** — a shop's own workshop log/export ingested the same way as sales, one row per finished repair into `production_events` (`event_type="repair"`, `status="completed"`, no parts-consumed detail in v1); `customer_id`/`performed_by_id` deliberately left NULL, same data-minimisation precedent as sales never getting customer-matching. `run_import`/`undo_import`'s entity-type dispatch refactored from a hardcoded if/else into dispatch tables (pure refactor, no behavior change for `sales`/`inventory`) now that there are four entity types. Found and fixed a real bug during testing: the undo-ordering guard was blocking `repairs` undos unconditionally whenever *any* later inventory reconciliation existed, even though repairs write zero `inventory_movements` and can never be affected by one — narrowed the guard to exempt `repairs`. A new read-only `GET /businesses/{id}/uploads/freshness` endpoint and uploads-page indicator show "last uploaded N days ago" per entity type (no push/email notification — no scheduler exists yet, Stage D17). Verified via the full test suite (239 passing) and a live end-to-end browser walkthrough: uploaded and imported a purchases file (cost seeded/overwritten correctly, `reason="purchase"` movements written) and a repairs file (`production_events` rows, no stock impact), then ran an inventory reconciliation and confirmed the exact undo-guard fix live — undoing the purchases import was correctly blocked (409), undoing the repairs import was correctly allowed, exactly as the bug fix intended.
- [x] B8d. Remap a confirmed-but-not-yet-run upload. Found live: a user testing B8c's purchases/repairs mapping picked the wrong column for a field, confirmed it, and had no way back — `detect-mapping`/`confirm-mapping` refused to run again once `Upload.status` left `"uploaded"`, and the frontend had no button for it anyway. Fix scoped deliberately to `status == "mapped"` (confirmed, not yet run) — an upload whose import already ran can't be remapped even after undo, since the source file is deleted from R2 right after a successful import (ADR-008); that case's recovery stays "re-upload the file," unchanged. `detect_mapping_for_upload` skips the saved-profile auto-reuse fast path on a remap (`upload.status == "mapped"` is itself the signal) — without this, `confirm_mapping`'s own `upsert()` would have already saved the wrong mapping as this source's profile, and reuse would silently reapply the same mistake before the user ever saw the form again. New `ImportRecordRepository.delete_for_upload` replaces the previous attempt's record on a remap, preserving the "exactly one `ImportRecord` per Upload" invariant `get_for_upload`/`map_by_upload_id` both assume. New "Remap" button on the uploads page next to "Run import." Verified via the full test suite (250 passing) and a live browser walkthrough: confirmed a deliberately wrong mapping (SKU/product-name swapped), remapped, confirmed it re-detected fresh rather than reapplying the wrong saved profile, ran the corrected import, and verified the resulting `sale_items` had the right SKU/product name, not swapped.

**Stage C — The actual product value**
- [x] C8b. Schema foundation for multi-vertical calculations (ADR-016, ADR-022, ADR-023) — `repairs`/`repair_parts_used` (bicycle-specific, unused) replaced by the canonical `production_events`/`production_event_inputs`/`production_event_outputs` pattern, covering bike-shop repairs and cafe kitchen production/recipes under one shape; added canonical `inventory_lots` (lot/batch + expiry-date tracking, requested by a real pharmacy prospect) and a pharmacy `prescription_details` template extension. Models + migration only — no repository/service/API/calculation logic yet, that's C9. Verified via the full test suite, the dedicated migration-chain structural test, and applying to local dev Postgres.
- [x] C9. Core calculations — Retail + Financial Performance, unit-tested per formula (PR-3, ED-007). Workshop (repair) profitability deferred until `production_events` gets a real-time entry workflow and starts generating data. Revenue, gross margin (with a PR-3.5/3.6 cost-data-completeness flag and PR-3.6 revenue/profit distinction), and top/bottom-margin products cover Financial Performance; stock cover, sell-through rate, dead-stock detection, and inventory value at cost cover Retail Operations. Pure formulas live in `app/analytics/`, orchestration in `app/application/`, exposed via two new tenant-scoped GET endpoints. Verified via unit tests on every formula plus an integration test against a real (SQLite) database.
- [x] C10. Findings & recommendations engine (PR-4). Six deterministic rules over C9's output (revenue decline, low gross margin, incomplete cost data, products selling at a loss, low stock, dead stock), each producing a `Finding` (evidence, severity, rule version — PR-4.1/PR-4.4) mapped to a `Recommendation` from a fixed approved library (PR-4.2), ranked by severity then a dollar-denominated impact score (PR-4.3). Not persisted — computed on demand, same as C9, via a new `GET /businesses/{id}/analytics/findings`. The `low_stock` rule (`app/analytics/findings.py::evaluate_low_stock`) is deliberately standalone (just a stock-cover list + threshold, no DB/summary object) so C12 reuses it directly instead of reimplementing the threshold logic. Verified via unit tests per rule plus an integration test seeding a scenario that trips every rule at once — which caught a real bug during design (a loss-making product with too few peers to appear in `bottom_margin_products` was being missed; fixed by scanning `top_margin_products` too).
- [x] C11. Charts/dashboard section, browser-rendered from structured API payloads (PR-3.3), with drill-down scoped to the per-product/per-repair breakdown rows the existing analytics endpoints already return (PR-3.4) — full drill-down to individual `Sale`/`SaleItem` rows deferred, no endpoint lists those yet. Also added, since B8c's repairs upload now writes real `price_charged`/`labour_cost` per completed job: a **Workshop Performance** calc mirroring C9's pattern — `ProductionEventRepository.aggregate_completed_repairs_in_range`, `app/analytics/workshop.py` (labour-only profitability, honestly caveated the same way C9 caveats missing cost data — no `ProductionEventInput`/parts-cost writer yet), `app/application/workshop_performance.py`, exposed via `GET /businesses/{id}/analytics/workshop-performance`. Frontend: `frontend/app/dashboard/page.tsx` — Financial Performance, Retail Operations, Workshop Performance, Findings & Recommendations, and Active Alerts sections, a shared period control, ECharts bar charts (`echarts`/`echarts-for-react`, per ADR-010) for margin and stock-cover breakdowns, plain-language metric definitions on hover (PR-3.7) and data-completeness notes (PR-3.5). Extracted the business-selection logic shared with the uploads page into `useBusinessSelector`, and added the first shared nav (`AppNav`) now that there are two real app pages to move between. Verified via the backend test suite (246 passing, including new Workshop unit/integration tests), a clean frontend production build, and a live browser walkthrough against the running Docker Compose stack confirming all five sections render correctly (including a genuinely empty-data business, proving the empty-state paths) with real API responses.
- [x] C12. Low-stock alerts — real-time, deterministic (PD-009, PR-9). Reuses C10's `evaluate_low_stock` rule unmodified — `app/application/alerts.py::refresh_low_stock_alerts` groups touched products by resolved threshold (product override -> category override -> default, `resolve_low_stock_threshold`) and calls it per group. Fires from `app/imports/importer.py`'s existing post-commit points in both `run_import` and `undo_import` (try/except-wrapped, never fails an otherwise-successful import), for exactly the products that import/undo touched. Persisted to the existing `alerts` table with create/update-in-place/resolve state-transition logic (no duplicate alerts for an ongoing condition), exposed via `GET /businesses/{id}/alerts`. Added `low_stock_threshold_days` (nullable) to `Product`/`ProductCategory` and a composite `alerts` index for PR-9.3. **Follow-up flagged, not built here:** no product-management API/UI exists yet to let an owner actually set those threshold columns — the data model and rule resolution are ready, the settings screen isn't. Verified via unit tests (threshold resolution, JSON-evidence conversion) and integration tests (repository CRUD, full create/update/resolve cycle, and a real `run_import`/`undo_import` round-trip creating then resolving a persisted alert).
- [x] C13. Simple forecasting baseline (seasonal/moving average) — deterministic (PR-10), no AI. `app/analytics/forecasting.py::compute_baseline_forecast` picks day-of-week seasonal naive averaging once at least 3 full weeks of history exist, falling back to a plain moving average between 14-20 days, and refuses to forecast at all under 14 days (`insufficient_data`, no fabricated number). Confidence range is the sample standard deviation of the historical values behind each forecast day (`Decimal.sqrt()`, never float). Covers both business-wide revenue (`GET /businesses/{id}/analytics/forecast`) and per-product unit demand, the latter feeding a `suggested_reorder_quantity` (confidence band's high end minus current stock) — explicitly labelled as a starting point, not a lead-time/safety-stock-aware reorder system. A product/business only starts its averaging window from its own first-ever observed sale, not a synthetic zero-filled `MAX_LOOKBACK_DAYS` window — the first version of this logic zero-filled blindly back 90 days regardless of actual history, which would have made the insufficient-data gate never trigger for a genuinely new business (caught before shipping, during design). **Known, stated limitations:** only day-of-week seasonality is attempted — monthly/yearly seasonality needs 1-2+ years of history no new pilot business will have yet, which is exactly what C14/C15's weather-signal augmentation is for; the reorder suggestion has no supplier lead-time or safety-stock model. Verified via the backend suite (296 passing, including hand-computed variance fixtures), a clean frontend build, and a live walkthrough against the running Docker stack.
- [x] C14. Weather data API integration (free tier — relevant to Irish cycling demand). Built as the redirected, non-ML design from C15's own note below, not a point-forecast model — see v1.81 below for the full writeup, live validation results, and recommendation.
- [ ] C15. ML forecasting models (weather-enhanced, built on the Stage C13 baseline). **A first attempt was tried and assessed (17/08/2026) — see v1.80 below.** It didn't hold up: no ablation isolated weather's own contribution from the ~40 non-weather features already doing most of the work, the comparison baseline wasn't ORLA's real one, and — the real problem — it only ever looked at *today's* weather, never the actual forecast for the days it was predicting, so it couldn't have delivered the intended capability even if every metric had checked out. Redirected to a different, better-fitting design instead of a point-forecast ML model: see v1.80 for the "grounded weather-pattern insight" direction the team wants to pursue when this gets picked up.
- [ ] C16. Test forecasting model results against held-out data

**Stage D — Reports**
- [x] D17/D18. Scheduled weekly/monthly reports (PR-8, PD-007, ADR-019) — built together since one engine generates both (only the comparison period differs). **Scheduler**: a new, separate lightweight `scheduler` service (`docker-compose.yml`, same backend image, `python -m app.scheduler` entrypoint) polling every 15 minutes — no Redis/job-queue dependency added. Its single reconciliation tick (`app/scheduler/tick.py::run_tick`) deliberately does three PR-8 jobs at once: scheduling (PR-8.1/8.2 — generates a business's previous completed week/month once its local 08:00 generation moment has passed), retry (PR-8.9 — a `failed` row under a 5-attempt cap is still "not completed," so the next tick just tries it again), and recovery (PR-8.10 — a tick resuming after any downtime notices and catches up, no separate recovery process needed). **Generation** (`app/application/report.py::generate_report`) assembles one JSON payload per report entirely by calling the existing C9-C13 application-layer functions for the resolved period (`get_financial_performance`, `get_retail_operations`, `get_findings`, `get_forecast`, and `get_workshop_performance` gated on `Business.template == "bicycle_shop"` — omitted, not faked, for any other template, satisfying PR-8.3's own requirement) — no calculation logic of its own, no AI anywhere (PR-8.7), the payload sections serialized via the exact same Pydantic schemas the dashboard API uses so a report and the live dashboard can never disagree about a number's shape. Two small new deterministic additions: `app/analytics/retail.py::classify_movers`/`compute_inventory_turnover` (fast/slow-mover buckets off existing stock-cover data, and a turnover ratio explicitly caveated as using current, not period-average, inventory value) and `app/analytics/report_narrative.py::build_executive_narrative` (a handful of template sentences with numbers plugged in — not free text generation). Idempotency (PR-8.8) is a real DB unique constraint on `(business_id, report_type, period_start)`, not just an application convention. `Report.status` moves `pending` -> `completed`/`failed`; a row is never hard-deleted even after its 7-day customer-facing availability window (PR-8.5) expires — it's its own audit record (PR-8.12), just excluded from list queries. New `GET /businesses/{id}/reports` (list, active only) and `GET /businesses/{id}/reports/{report_id}` (full payload) routes, and new `frontend/app/reports/page.tsx`/`frontend/app/reports/[id]/page.tsx` pages added to `AppNav` — the detail page reuses the dashboard's own `Chart`/`Section`/`Stat` components and chart-option builders (newly extracted into `frontend/components/Section.tsx`/`frontend/lib/chartOptions.ts`/`frontend/lib/format.ts` so both pages share one implementation, not two). **Explicitly deferred, not silently solved:** revenue by category/brand/supplier and the "Operational Opportunities" section (no such data is ingested by any upload path yet); a Pharmacy near-expiry section (`inventory_lots` from ADR-022 has a schema but no writer — a real ingestion feature, not a report-engine task); any business-specific section beyond Bike Shop; PDF/Word export (PR-8.6 is explicitly on-demand/decoupled — in-app viewing only for this pass); richer chart types from the original mock (treemap, Pareto, heatmap) beyond the existing bar/line patterns. Verified via the backend suite (339 passing, including new unit tests for the mover/turnover/narrative/period functions, integration tests for `generate_report` and `run_tick`, and a tenant-isolation test), a clean frontend production build, and a live walkthrough against the running Docker stack: applied the new migration to real dev Postgres, started the `scheduler` container fresh, and watched it generate real weekly/monthly reports end-to-end for both existing dev businesses from actual imported history — then confirmed a second tick left them untouched (`already_done: 4, generated: 0`), proving idempotency live, not just in tests.

**Stage E — The AI layer (deliberately last — it only explains what Stages C/D already calculated)**
- [x] E19/E23/E24/E25. AI Provider Gateway + business-data Q&A chat (PD-008, ADR-020, PR-5.1/5.3/5.6) — built as one conversational agent rather than separate pieces, since a chat window was the user's explicit ask over a one-off "explain this report" button. **Gateway** (`backend/app/ai/client.py`, the only file importing an AI-provider HTTP SDK, per CLAUDE.md): connects to OpenRouter's OpenAI-compatible chat-completions endpoint via `httpx` (promoted from a dev-only to a main dependency), sets `provider: {"data_collection": "deny"}` on every request — OpenRouter's own concrete mechanism for restricting routing to providers that don't retain/train on request data, operationalizing 05_AI_Architecture.md's "EU regulatory fit" requirement rather than a per-model judgment call — and passes an optional `models` fallback array for free failover. **Routing, per ADR-020** ("route every question through an intent classifier that selects one of a fixed, approved set of deterministic query functions... before any AI generation occurs"): `app/ai/service.py::answer_question` runs a two-call pipeline — a small classify call constrains the model's output (via JSON mode plus server-side re-validation against a fixed Python enum, never trusted blindly) to one of eight approved intents, each mapping straight to an existing C9-C13/D17-18 application function (`get_financial_performance`, `get_retail_operations`, `get_workshop_performance`, `get_forecast`, `get_findings`, or the latest stored `Report`) or a static glossary; then a second call phrases the answer from only that fetched, real data. A cheap deterministic keyword pre-check (`app/ai/glossary.py::match_definition_question`) catches obvious "what does X mean?" questions before any AI call at all — genuinely zero cost, not just a cheap one. **Guardrail** (`app/ai/guardrail.py`, PR-5.3, pure functions with no I/O): every numeric claim in an AI answer is extracted and must equal some number found anywhere in the structured context it was given (Decimal equality, so "39.9%" matches "39.90" but a rounded "€11,908" does not match a true 11907.76 — deliberately fails closed on any ambiguity); a claim that doesn't trace back gets the whole answer rejected in favour of a safe fallback message, never shown to the user. **Usage & degradation** (PR-5.4/5.5): a per-business rolling-24-hour request cap (`AIRequestRepository.count_today_for_business`, default 30/day) blocks further AI calls with a clear message, checked before any call is dispatched; any OpenRouter network failure/timeout is caught and degrades to a friendly "temporarily unavailable" message, never a 500 — nothing else on the page depends on this succeeding. Every real AI call (successful or not) is logged to the previously-unused `ai_requests` table (`AIRequestRepository`, its first writer). **Frontend**: new `frontend/app/chat/page.tsx` ("Ask ORLA" in `AppNav` — ORLA is this assistant's product name, used throughout its user-facing copy and system prompt) — a plain single-turn message thread (no conversation history resent as model context, a stated limitation to keep token cost down), reusing the existing `apiPost` pattern, no new chart/streaming infra. **Explicitly deferred, stated not solved:** E20/E21/E22's full vision — dynamic cost/quality-threshold model auto-selection — wasn't built; the model is a single plain config value (`openrouter_model`, trivially swappable) rather than a scored selector, since no real evaluation test set exists yet to justify one algorithmically (05_AI_Architecture.md's own open question) — ADR-015 stays "Proposed." E26/E27 (the product/support lane) is untouched — it needs real help-content to retrieve from, which doesn't exist yet. Multi-turn conversation memory isn't built. **Live-verified end-to-end once a real key was added** (not just mocked): queried OpenRouter's real model catalog directly and found almost every free-tier model requires accepting "free model training" as the price of being free — `data_collection: "deny"` correctly filters those out, narrowing the live default to `openai/gpt-oss-20b:free` (a reasoning model with real hidden-token overhead, fine for testing, worth swapping for a plain instruct model at launch per the cost goal) plus two verified-compliant fallbacks. Live testing caught and fixed two real bugs the mocked suite couldn't: (1) `AIProviderError` wasn't surfacing the actual response body, making a live failure undebuggable — fixed to include it; (2) the guardrail rejected a genuinely correct, fully-grounded answer because the model phrased a decline as "down 35.7%" while the real data stored `-35.7` — the literal minus sign was never in the prose, exactly how this codebase's own `report_narrative.py` already phrases a decline (`abs(change_pct)` + the word "decreased"). Fixed by also accepting a negative context value's absolute form as grounded, with a new regression test. A follow-up real question — "what should I order this week?" — surfaced two more: (3) the classifier picked `findings_recommendations` (which has no per-product order quantities) over `forecast` (which does) for a reorder question — fixed by adding a short per-intent guide to the classify prompt describing what data each one actually contains; (4) the resulting answer named specific products ("CityRoll MTB 200") and got rejected anyway — a product name containing a model number is legitimate context, but the guardrail only recognised a value as numeric if the *entire* field parsed as one, so a number embedded inside a name was invisible to it. Fixed by also extracting embedded numbers from non-UUID strings (UUID-shaped strings are excluded — their digit runs are structurally meaningless and would only dilute protection against a real invented number). Debugging that also surfaced the actual cost problem: this test business's un-trimmed `forecast` context (152 products x a 7-day daily curve each) was 141,660 characters, and its stored report payload (which nests the same forecast) was 300,573 characters — a real explain call logged **69,006 prompt tokens and still failed**. Added `app/ai/service.py`'s `_trim_forecast`/`_trim_retail_operations`/`_trim_findings`/`_trim_report_payload` — capping large per-product lists to the most urgent/highest-impact rows (mirroring the dashboard's own `.slice(0, 15)` display cap, applied before the data reaches the model rather than only at render time) and dropping chart-only day-by-day curves entirely. The same question re-run afterward: correctly classified, grounded, real reorder quantities for correctly-named products, using 2,268 prompt tokens — a 97% reduction. That trimming raised an immediate follow-up question: capping a list means ORLA is no longer looking at the whole picture, so what happens if someone asks for a full/complete list, or a count, of something that got capped? `_cap_rows` now adds a `{field}_shown_of_total` note ("15 of 152") to the trimmed context whenever truncation actually happens (and never when it doesn't) — a real, grounded fact the model can cite. But live testing showed a model asked point-blank for "the full list" answered correctly from the 15 shown rows without mentioning the other 137 existed — a prompt is a hint, not a guarantee, the same reasoning already behind the numeric guardrail. So the disclosure is enforced deterministically instead: `_append_truncation_disclosure` always appends a plain-Python sentence pointing to the Dashboard/Reports page whenever any `_shown_of_total` note exists anywhere in the context (including nested inside a stored report), regardless of what the model itself said — verified live afterward, both when the model already disclosed it (a little redundant, by design — the backstop doesn't know or trust that it did) and when it didn't. Two direct follow-up requests refined this further: (1) reorder suggestions should be ordered by urgency, since that's the actually valuable ordering — rather than trust the model's prose to preserve the order it was given (already shown unreliable above), `app/ai/service.py::_build_priority_order_list` deterministically formats every product needing reorder in guaranteed urgency order (get_forecast's own sort), built from the *untrimmed* product list (free — it's local formatting after the AI calls are done, not another prompt) so it's genuinely complete unless it itself exceeds a much higher 30-row display cap, gated behind a cheap keyword check (`_looks_like_a_reorder_question`) so a plain revenue-forecast question doesn't get an unrelated product list appended. (2) A clickable link to the Dashboard/Reports for the full picture — implemented as a small fixed-enum `links` field (`AnswerResult`/`ChatResponse`, values from `{"dashboard", "reports"}` only, computed deterministically from whether anything was actually truncated) rendered by `frontend/app/chat/page.tsx` as real `<a>` elements, deliberately never by treating any part of the model-generated `answer` text as HTML (an XSS risk this design avoids entirely by construction, not by sanitising). Checking that link would actually deliver "the full list" surfaced one more real gap: the Dashboard's and Report's own Forecast/Purchasing-Recommendations *tables* also capped display at 15 (`frontend/app/dashboard/page.tsx`, `frontend/app/reports/[id]/page.tsx`) — pointing someone there wouldn't have solved anything. Removed that cap on both (the underlying API already returns the complete list; only the table rendering was artificially short) — left the stock-cover *chart's* cap alone, since a 150-bar bar chart is genuinely unreadable regardless of correctness, unlike a scrollable table. 369 backend tests passing (all of the above with regression tests), a clean frontend build, and `backend/scripts/verify_ai_chat_live.py` for future live spot-checks.
- [ ] E20. Test candidate AI models under GDPR/EU compliance — partially addressed: every request already enforces OpenRouter's `data_collection: "deny"` regardless of which model is selected (see above), but no real comparison of candidate models has been run — there is no live OpenRouter key yet to test against.
- [ ] E21. Select the quality/price threshold (ADR-015) — deferred; no evaluation test set exists yet to define one.
- [ ] E22. Code automatic cheapest-model-above-threshold selection — deferred; the model is a static config value for now (see above).
- [ ] E26. Build support knowledge base content
- [ ] E27. Conversational agent — support lane, with human handoff when unresolved (PR-5.7)

**Stage F — Payments and wrap-up**
- [x] F28. Simulate Stripe payments (test mode — PR-7)
- [x] F29. Test prototype end-to-end (Gate B checklist, below) — a genuine pass, not a synthetic smoke test: created a fresh business and imported four real files (stock, sales, purchases, repairs — 8,255 total rows) from a standardized synthetic-data source (`TEST_SYNTHETIC_DATA`), replacing the ad-hoc test fixtures accumulated across earlier stages, which had themselves started polluting shared dev businesses with debris (five zero-activity products named after their own test scenarios — deleted). Against each Gate B criterion: **real files import end-to-end** — all 4 did, with sensible, explainable rejections, not failures (undelivered purchase orders, a handful of malformed sales rows). **Calculated metrics verifiably correct** — hand-calculated total sales revenue from the raw CSV and traced a small discrepancy against the API to its exact root cause (the importer's own documented per-row cent-rounding when reconstructing a unit price from an authoritative total) — confirmed correct, not a bug. **Dashboard findings understandable** — read the full rendered dashboard end to end. **Tenant isolation** — covered by the existing automated suite. **Schema detection** — this is where the pass earned its keep: stock and sales auto-mapped 100%, but purchases and repairs each had a real, distinct problem, both found and fixed (see below), re-verified against the same real files afterward at 100% auto-mapped, zero manual intervention.

  **Fix 1 — schema detection could confidently pick the wrong money column** (`app/imports/detection.py`). Repairs' `price_charged` auto-mapped to `labour_amount` instead of `total_amount`; `labour_cost` auto-mapped to `labour_hours` — an hours-worked column, not a cost, which would have silently corrupted every workshop margin figure computed from it. Root cause: when multiple structural candidates tied at the exact same score, the winner was whichever column happened to appear first in the file — an accident of Python's stable sort over a column-ordered list, never a deliberate ranking. Fixed with three changes: (1) a genuine tie among a money field's candidates is now never auto-picked — the field comes back unmapped, with every tied candidate still shown at its real confidence for a human to choose between, mirroring this file's own pre-existing identifier-header veto reasoning ("a wrong assignment silently corrupts every margin number downstream... hence a veto, not a reduced score"); (2) a soft "looks like currency" signal (fraction of values with genuine 2-decimal-place formatting) blended into the score, separating an hours/rate column from a real money column when the header's token overlap alone can't; (3) token overlap for money fields is now proportional (fraction of the field's token set matched) rather than binary any-match-at-all, so a strong multi-token match ("Total Amount" matching both "total" and "amount") correctly outranks a weak single-token one ("Labour Amount" matching only "amount") — this alone resolved `price_charged` cleanly, no tie left to break. Plus the missing aliases the real files also exposed (`purchases.purchase_reference` had zero candidates for `purchase_order_id`/`supplier_order_reference`/`supplier_invoice_number` — none matched anything in the alias list, which is exact-string-match only; `repairs.description`/`price_charged`/`labour_cost` gained `"issue description"`/`"total amount"`/`"labour amount"` respectively). 16 new regression tests.

  **Fix 2 — a sale row with a negative quantity was rejected outright, silently excluding real return/refund data from every calculation** (`app/imports/importer.py`). 211 of 5,798 rows in the synthetic sales file were exactly this shape — negative-quantity "Sale" rows (the file's own unused `transaction_type` column confirms them as `"Return"`), a common real-world POS export pattern, previously just discarded as `non_positive_quantity`. Fixed to accept `quantity < 0` as a return (only an exact zero is still rejected, as `zero_quantity`) — and it turned out almost nothing else needed to change: revenue/margin/stock math were *already* sign-agnostic (every downstream `SUM()` has no sign filter), and `InventoryMovement.REASONS` already reserved an unused `"return"` value, with a complete-but-never-written `Return` model/table from the very first migration — deliberately scaffolded and left for this. New `ReturnRepository` (`sum_refund_amount_in_range`/`count_in_range`, mirroring `SaleRepository`'s exact shape) feeds a new `ReturnsSummary` (`gross_revenue`/`returns_amount`/`return_count`/`net_revenue`/`return_rate_pct`) — a new sibling dataclass, not a change to `RevenueTrend`, added to `FinancialPerformanceSummary`/`FinancialPerformanceOut` and flowing automatically into the dashboard, the stored report payload, and ORLA's `financial_performance` chat context (all three already reuse this one schema — no changes needed in any of them beyond adding "returns/refunds" to ORLA's own intent-routing guide, found live: a "how much have I refunded?" question initially came back `out_of_scope` since the guide didn't mention the topic existed). New `evaluate_high_return_rate` finding (>10% of gross revenue refunded), matching C10's existing six-rule pattern exactly. Live-verified end-to-end against the real Docker stack: imported a fresh return alongside a normal sale, confirmed the `Sale`/`Return`/`InventoryMovement` rows were all individually correct, confirmed the dashboard's new disclosure line, the finding, and ORLA's grounded answer all matched. **Explicitly not addressed**: `rank_products_by_margin`'s pre-existing exclusion of zero/negative-revenue products from top/bottom ranking (a product whose only activity was a return could be absent from that list — unchanged, noted not fixed); purchase-side returns (to a supplier) are a different, unaddressed concept — this is sales-side customer returns only, matching what the real data actually contained.

  A follow-up live check surfaced one more real bug: OpenRouter's free-tier *daily* quota (separate from this app's own 30/business/day cap) was exhausted from the session's own testing, and every question — including perfectly in-scope ones like "how much was my revenue on the 24th?" — came back with the generic "I can't help with that topic" message. Root cause: `_classify_intent` silently mapped a failed provider call (network error, rate limit, timeout) to the exact same `"out_of_scope"` value a genuine "the model looked at this and it's not about your business" verdict produces — conflating "your question is out of scope" with "the AI service is down right now," two very different situations the user needs told apart. Fixed with a distinct `"provider_unavailable"` sentinel, routed to the existing `_UNAVAILABLE_MESSAGE`/`grounded=False` path (PR-5.4) instead — live-reverified against the real (still-exhausted) 429 afterward: an in-scope question now honestly says ORLA is temporarily unavailable, not that the topic is unsupported.

  A further direct ask — "clients will have questions about dates, days, specific amounts, products, orders, deliveries, repairs" — surfaced a real coverage ceiling: ORLA could only answer *aggregate* questions over 3 fixed periods (recent/last-week/last-month), with no way to ask about a specific date or one specific product/purchase/repair by name or reference. Research first confirmed the underlying data already supported this per-record, just never made queryable: `InventoryMovement` (a purchase is one row) already stored `purchase_reference`/`event_date`/`quantity_delta` per row, and `ProductionEvent` (a repair) already stored `repair_reference`/`opened_at`/`price_charged`/`labour_cost` — a schema change was never needed, only new repository search methods and classify-prompt coverage. **Explicit dates**: `ALLOWED_PERIODS` gained `"explicit_date"`; the classify call's JSON gained optional `start_date`/`end_date`, extracted by telling the model today's actual date (business timezone) in the system prompt — otherwise it has no way to resolve "the 24th of June" to a real year. Never trusted blindly: a new `_parse_explicit_dates` parses via `date.fromisoformat`, enforces `start <= end`, and bounds both to a sane 10-years-back/1-year-forward window — any failure falls back to `default_recent`, the same fail-closed posture every other classify field already had. **Three new per-record lookup intents** — `product_lookup` (new `ProductRepository.search_by_name_or_sku`, case-insensitive substring on name, exact-normalized match on SKU — deliberately not fuzzy on SKU, a partial match there is usually noise not a real hit), `purchase_lookup` (new `InventoryMovementRepository.list_purchases`, tries a purchase/PO reference first, falls back to resolving the term as a product name), `repair_lookup` (new `ProductionEventRepository.find_repairs`, tries a repair/job reference first, falls back to a description substring match — repairs have no stored customer name in this data at all, so a "which repairs did customer X have" question is correctly routed `out_of_scope` rather than guessed at). All three share one new dispatch pattern (`_dispatch_lookup`): zero matches is an honest "couldn't find X" (zero further AI cost); several matches is a deterministic "found several matching X — which one?" capped at 5 (also zero further AI cost — never silently guesses, the same reasoning behind this session's numeric guardrail); exactly one match proceeds through the normal fetch-explain-guardrail pipeline like every existing intent, just with a far smaller single-record context. `_classify_intent`'s return grew from a bare 3-tuple to a small `ClassifyResult` dataclass (`intent`/`period`/`start_date`/`end_date`/`metric`/`search_term`) now that the field count no longer fit readable positional unpacking. Live testing against the real Gate B Synthetic Shop business caught one more real regression before it shipped: the classify prompt's growth (10 intents, date extraction, search-term extraction) pushed some real questions into consistently burning the entire `max_tokens=300` reasoning budget with zero tokens left for the visible JSON — reproduced deterministically (temperature=0) across repeated calls, not a one-off — which `_parse_intent_json` had no way to tell apart from a genuine out-of-scope verdict. Raised to `max_tokens=700`. Live-verified afterward against real Gate B data end-to-end, with a real (no-longer-exhausted) OpenRouter key: a product-stock lookup by name, a purchase lookup by PO reference (correctly triggering the many-match disambiguation — one real PO in this data covers several different products, exactly the shape the design anticipated), a repair-cost lookup by job reference, and a specific-date revenue question — all four returned grounded, correct answers matching the underlying database rows exactly. 40 new tests (explicit-date parsing/bounds, the three new repository search methods' 0/1/many-match cases, and end-to-end chat-pipeline coverage per lookup intent).

  424 backend tests passing (up from 384; all of the above with regression/unit/integration coverage), a clean frontend build (no frontend changes required — `ChatResponse.intent` was already a plain string).

  Direct request: product categories — data capture, per-section dashboard filters, and a reports table (revenue, expenses, stock value per category). `ProductCategory`/`Product.category_id` already existed in the schema since Stage C8b/C12 but had zero writer anywhere — nearly every product showed "Uncategorized" because nothing ever set it. Confirmed with the user before building: "expenses by category" means purchase cost (unit cost × qty received), not COGS — a genuinely different figure this deliberately doesn't reuse — which surfaced a real gap: no per-purchase cost history existed anywhere (only `Product.cost_price`, a single always-overwritten "current" value), so a new `InventoryMovement.unit_cost` column was added (mirrors `SaleItem.cost_price_at_sale`'s exact role, captured going forward only — existing purchases stay `NULL`, no retroactive backfill possible). "Stock value" is deliberately at **sell price**, not cost — a different number from Retail Operations' existing `inventory_value` (which stays at cost, unchanged). A new optional "category" column on sales/purchases/inventory uploads (not repairs — no product link exists there) resolves against `ProductCategory` by normalized name, match-or-create, mirroring `ProductMatcher`'s exact shape (new `CategoryMatcher`); a product's category is set on creation and unconditionally overwritten on every later sighting that maps one, the same "latest wins" semantics `ProductRepository.update_cost_price` already established. New `app/analytics/category.py::compute_category_breakdown` groups already-fetched per-product data by category in Python (no new SQL joins — matches `sum_by_product_ids`'s own "fetch raw, finish in Python" convention), with an `expenses_data_coverage_pct` completeness flag (PR-3.5 pattern) disclosing what fraction of purchased quantity actually has a known cost. Financial Performance and Retail Operations dashboard sections each get their own independent category filter dropdown (confirmed: one per section, not a shared control) — Workshop Performance and Forecast stay unfiltered, confirmed out of scope (repairs have no product link at all; forecast explicitly excluded). Every product-row table (top sellers, stock cover, dead stock, margin products) now shows its category name beside the product name, and per-product findings (low stock, dead stock, selling-at-a-loss) carry it in their evidence — whole-business findings like revenue decline are untouched, per direct instruction. New `GET /businesses/{id}/product-categories` (list only — no create/edit endpoint yet, same "no product-management UI" gap already flagged as of C12) and `GET .../analytics/category-breakdown`; `category_id` query param added to the existing financial-performance/retail-operations routes. **Explicitly not solved**: returns stay whole-business even under a category filter (`Return` has no product/category-aware query) — `gross_revenue`/`return_rate_pct` are therefore not strictly accurate for a filtered category, stated in code rather than silently wrong-but-unflagged.

  Two real bugs found live, both fixed before shipping. **(1)** Checking the new "stock value at sell price" figure against the real Gate B business showed **every one of its 180 real products had `sell_price = NULL`** — `sell_price` was only ever set once, at product-creation time in `_write_sales`; a product first created via a purchases/inventory file (no price concept in those rows) never got one at all, even once later actually sold, and even a sales-created product's price never reflected a later real change. Fixed with `ProductRepository.update_sell_price`, called from `_write_sales` on every existing-product sighting — the exact missing counterpart to `update_cost_price`'s own "latest wins" pattern (which had this identical gap until v1.11). **(2)** Loading an already-stored report (generated before this session) crashed the whole page with `Cannot read properties of undefined (reading 'returns_amount')` — a report payload is a JSON snapshot taken at generation time, never re-serialized against a later schema, so `financial.returns` (added earlier this session) was simply absent on that older row. The identical risk applied pre-emptively to the brand-new `category_breakdown` key. Fixed with defensive guards at both access sites (`frontend/app/reports/[id]/page.tsx`) rather than assuming every stored report matches today's shape — live-reverified against the exact report that crashed: renders fully now, with the Category Breakdown section correctly showing "No category data for this period" instead of erroring.

  Live-verified end-to-end against the real Gate B Synthetic Shop business (real Postgres, not just SQLite tests): a purchases file with a category column imported through the real pipeline auto-created the category and assigned it to a new product; `retail-operations`'s `category_id` filter correctly scoped to just that product; the dashboard and a previously-crashing stored report both reload cleanly with the new backend live. 455 backend tests passing (up from 424), a clean frontend build.

  A third real gap found live, this time by direct feedback rather than self-testing: after all of the above, the filter dropdown and category labels were reported as simply not visible anywhere on the dashboard or reports. Root cause — a real design flaw, not a broken feature: every business genuinely had zero categories at that point (the one created during live verification was deliberately deleted afterward as cleanup), and `CategoryFilterSelect` hid itself entirely whenever `categories.length === 0`, with `CategoryLabel` doing the same per-product — making "the feature has no data yet" indistinguishable from "the feature isn't built." Fixed by always rendering the dropdown (disabled, with an explanatory hint, when there are zero categories) rather than hiding it — the control needs to be discoverable regardless of data state. Separately, assigned real categories (Bikes/Accessories/Components/Maintenance & Consumables/Workshop Supplies/Tools, derived from each product's own name) to all 180 real Gate B products via a genuine "inventory" reconciliation import through the real pipeline — chosen specifically because reconciling to each product's own already-current stock is a true no-op on quantity (verified: inventory value unchanged at €671,355.00 before and after), so only the category assignment landed. This surfaced one more consequence of the `sell_price` bug fixed earlier in this same entry: the fix only takes effect on sales imported *after* it shipped, so Gate B's pre-existing 180 products still had `sell_price = NULL`, leaving the new "stock value" figure at €0 for all of them. One-time backfill (not a permanent code path — a bounded migration script, matching the precedent of v1.19's `event_date` backfill): each product's `sell_price` set from its own most recent real sale line, 174 of 180 products backfilled (the other 6 have no sale history at all to derive one from — correctly left `NULL`, not guessed). Re-verified live afterward on both the dashboard (both category dropdowns populated with all 6 categories, filtering correctly scopes revenue, every product-row table shows its category, per-product findings show category while "Investigate the revenue drop" correctly doesn't) and on a freshly force-regenerated report (Category Breakdown table now shows real, non-zero revenue/stock-value figures across all 6 categories).

  A fourth round of direct feedback, covering both remaining pages. **Reports**: (1) a "Download PDF" button, deliberately using the browser's own print-to-PDF (`window.print()` plus a new `@media print` stylesheet hiding nav/controls and avoiding mid-row/mid-chart page breaks) rather than a new client-side PDF-generation library — no new dependency, and the browser already renders the report's ECharts canvases and tables correctly in print; (2) a "Sections" table of contents at the top linking to each section (`Section` gained an optional `id` prop for the anchor targets); (3) a real bug: "Top recommendations" and "Action Plan" showed only the generic rule description ("At the recent sales rate, this product is projected to run out shortly.") with **no product name at all** — both sections were built without the finding-message pairing the dashboard's own Findings section already had (a `Finding.message` is what actually names the specific product; `Recommendation.description` alone never does). Fixed by reusing that exact pairing. **Dashboard**: the category filter was missing from three places it was expected — Forecast's "Products to watch" table, and Findings & Recommendations entirely (both filter and per-product category labels). Root cause for Findings specifically: `get_findings` had no `category_id` parameter at all. Added one, but deliberately not by re-scoping everything blindly — per direct instruction ("Investigate the revenue drop... is more like a different thing than anything related to stock itself"), whole-business rules (revenue decline, low gross margin, incomplete cost data, high return rate) always read from an *unfiltered* `get_financial_performance`/`get_retail_operations` call, while the three per-product rules (low stock, dead stock, selling at a loss) read from a second, category-filtered call made only when a filter is active — one extra pair of already-existing queries, not new query logic. The same "investigate revenue drop is different" instruction is now also a real visual split, not just an implicit one: Findings & Recommendations (and the report's Action Plan) render as two labeled groups, "Business performance" and "Stock & products," with the category dropdown attached only to the latter. `get_forecast` gained the identical `category_id` treatment for its own per-product table — the revenue-forecast figure itself stays whole-business always (a forecast model run over a filtered history is a real, separate computation this pass doesn't attempt, not just a filter on an already-computed result — same reasoning as Retail Operations' `inventory_value` staying whole-business under a filter). The dashboard's `RecommendationList`/`findingKey`/business-vs-stock split logic was extracted into shared `frontend/lib/findings.ts`/`components/Section.tsx` so the reports page reuses the exact same implementation rather than a second copy. 10 new backend tests (forecast/findings category scoping, including a test proving a whole-business finding's evidence is byte-identical whether or not a category filter is active). Live-verified end-to-end: Forecast and Findings & Recommendations both now show independently-working category dropdowns and per-product category labels on the real Gate B business; the Action Plan/Top recommendations business-vs-stock split and product-name fix confirmed on a freshly regenerated report; the TOC and Download PDF button both render correctly. 459 backend tests passing (up from 455), a clean frontend build.

  A real user pasted three ORLA chat transcripts showing wrong/confusing answers. Diagnosed each live against the actual pipeline rather than guessed at, per this project's own established practice — one turned out to be a different root cause than the user's own guess. **(1) A SKU-annotated lookup found nothing**: "how much of this WorkshopPro Lubricant Plus (SKU-00175) i have left?" came back "I couldn't find anything matching...". Confirmed live: `ProductRepository.search_by_name_or_sku("WorkshopPro Lubricant Plus (SKU-00175)")` returned zero rows, while either half alone ("WorkshopPro Lubricant Plus" or "SKU-00175") matched correctly — and that exact combined "name (sku)" format is what ORLA's *own* many-match disambiguation labels use (`app/application/lookups.py`), so a user copying one back verbatim was guaranteed to fail. Fixed by also splitting a trailing "(...)" annotation off the query and trying the two halves separately. Re-verified live: now returns a correct, grounded answer. **(2) "Biggest cost"/"largest expense" questions were unsupported** — confirmed live: classified either `out_of_scope` outright, or (inconsistently) `financial_performance`, which has no per-line-item cost breakdown to actually ground an answer in. Added a new `category_breakdown` chat intent, reusing `app/application/category_breakdown.py` (already built for the dashboard/reports) as-is — no new calculation logic. Re-verified live: correctly classifies and answers honestly from real data ("all categories show zero expenses" — accurate for Gate B, since expenses require the `unit_cost` capture added in v1.26 and Gate B's real purchase history predates it, a known, already-documented gap, not a new bug). **(3) A many-match reply the user assumed lost context** — the user's own diagnosis was "model lost memory of what I was looking for," but ORLA never had conversation memory to lose (a stated design limitation from Stage E, kept deliberately to control cost) — live-repeated the exact same bare follow-up 5 times and it classified correctly as `product_lookup` every time; what actually failed was the *explain* step producing an ungrounded answer from a context with no real question framing, correctly caught and safely rejected by the existing guardrail (not a wrong answer shown as confident). Since full conversation memory is a genuinely separate, larger, cost-affecting feature (still explicitly deferred, not silently ignored), the practical fix here is cheaper and immediate: the many-match message now models a real, complete follow-up question using one of the actual matched items ("Ask me again naming just the one you mean — for example, \"how much stock of X do I have?\"") instead of a bare "Could you be more specific?" — live-verified this exact suggested phrasing round-trips to a correct, grounded answer. 463 backend tests passing (up from 459).
  Ran a live fire test against Gate B — a representative 20-question subset of a formal 56-question stress-test document (10-Question Fire Test, the Brutal Executive Test, 4 Direct AI Attack Tests, 2 Hallucination Traps, a cross-customer isolation test, 2 Bad-Reasoning traps), chosen to cover every critical category while skipping the multi-turn conversational tests (no conversation memory exists — a stated, deliberate limitation, not a gap to chase) and the tests requiring deliberately corrupted business records (not requested, too invasive to real data). Cross-tenant isolation, hallucination resistance, and prompt-injection resistance all held cleanly with zero findings. Found and fixed four real, distinct bugs, each diagnosed live before fixing: **(1)** a reorder/stock-out-shaped question ("what's likely to run out of stock") occasionally classified `out_of_scope` even though the classify prompt's own "forecast" description covers it — added a narrowly-scoped `out_of_scope` → `forecast` recovery using the same keyword check already built for the priority-list feature (can only ever recover a real question, never grant a more sensitive intent). **(2)** an explain-step provider failure (as opposed to a classify-step one) kept whatever intent had already been classified instead of being tagged `provider_unavailable` — misleading for anything inspecting `result.intent`, even though the user-facing message was already correct. **(3)** the guardrail's `validate_grounded` only checked the model's numeric claims against the fetched business-data context, never against the user's own question — so a question like "if I had €2,000 to invest, what would you recommend?" got its own echoed-back "€2,000" flagged as an invented number. Fixed with an additive-only `question` parameter: a number the user themselves supplied is a safe premise to reference back, not a business fact needing grounding — confirmed this doesn't weaken protection against a genuinely invented number (a second test asserts a real invented figure is still rejected even with a question supplied). **(4)** live-verified separately, a second guardrail gap: a numbered-list answer ("1. Reorder Widget... 2. Review pricing...") — which the explain prompt's own instructions require for a "give me N actions/products" question, since markdown/bullets are otherwise forbidden — had every list marker parsed as an unsupported Decimal claim, rejecting an otherwise fully-grounded answer 100% of the time for any multi-item question. Fixed by stripping a numbered-list-marker token (a 1-2 digit number immediately followed by "." or ")" and whitespace, whether at a true line start or inline after a lead-in phrase) before the numeric-claim scan ever sees it — verified a genuine number sitting right after a marker, and a genuine decimal like "3.5%", both still get checked correctly. **(5)** a fifth, related bug found via the same live run: "what's likely to run out in the next two weeks" always fetched the forecast's default 7-day horizon, so the model's own correct "14 days" phrasing had nothing to trace back to in the fetched context — not a guardrail problem, a real data gap. `ClassifyResult` gained an optional `horizon_days`, extracted at classify time when the question names a specific timeframe ("next two weeks" → 14, "next month" → 30) and server-side bounded to the same [1, 90] range the `/forecast` API route already enforces; an out-of-range or absent value falls back to the existing default of 7. Also raised `_generate_answer`'s `max_tokens` 500 → 800 (fixed mid-sentence truncation on complex multi-part questions) and added explicit currency-formatting guidance to the explain prompt (a money value must render with a € prefix and thousands separators, never rounded or re-digited). 477 backend tests passing (up from 470), including new regression tests reproducing each of the five bugs above verbatim. Live re-verified against the running Docker stack after every fix: the €2,000 question, the "next two weeks" stock-out question, and a "give me the five actions" question all now return grounded, correct, fully-detailed answers where they previously failed.

- [ ] F30. Video tutorials — deferred past prototype validation; UI will likely change once tested

---

## Gate B — Prototype Validation

Proceed only when:

- A real (anonymised) customer file imports successfully end-to-end
- Calculated metrics are verifiably correct against manual calculation
- Dashboard findings are understandable to a non-technical reader
- Tenant isolation tests pass
- Schema detection succeeds on a meaningful proportion of real files without manual mapping

---

# Phase 3 — Pilot-Ready MVP

**Epic:** `COP — Phase 3: Pilot-Ready MVP`

| Deliverable | Notes |
|---|---|
| Production deployment | VPS + Coolify per `07_Deployment_Guide.md` |
| Production database | Neon, EU region, backups verified |
| Backup restoration tested | Not assumed — actually tested |
| Monitoring | Sentry + Uptime Kuma |
| Transactional email | Resend: invitations, import results |
| Scheduled reporting | Weekly (Monday) and monthly (1st-of-month) in-app reports — never emailed — per PR-8/PD-007/ADR-019 |
| User roles and permissions | Owner, manager, staff |
| Import history and error recovery | Visible, reversible |
| Stripe test mode | Checkout and webhook flow proven, not yet live |
| Privacy controls | Retention settings, deletion, data export |
| Audit logging | Privileged actions recorded |
| Forecasting module | Simple methods first (seasonal/moving average) |
| Conversational agent — business Q&A lane | Intent classifier + approved deterministic query functions (PD-008, ADR-020); extends Phase 2's AI explanation layer |
| Conversational agent — product/support lane | Retrieval over an approved help-content knowledge base; hands off to human support when unresolved (PR-5.7) |
| Low-stock alerting | Real-time, deterministic, in-app (PD-009, PR-9) |
| Pilot onboarding material | Setup guide, not a template |
| 3–5 pilot businesses recruited | From Phase 1 interview relationships |

---

# Phase 4 — Pilot Validation

**Epic:** `COP — Phase 4: Pilot Validation`
**This is a measurement phase, not a build phase.**

| Metric to Measure | Why It Matters |
|---|---|
| Import success rate | Validates PD-006 (no-template approach) |
| Manual mapping rate | If high, schema detection needs work |
| Time to first value | Onboarding friction indicator |
| Dashboard usage frequency | Are they actually coming back? |
| AI question volume and cost | Validates the €3/customer cost assumption |
| Support requests per customer | The hidden cost that kills solo-founder SaaS |
| Most-used modules | Informs what to build next |
| Recommendation usefulness | Do they act on them? |
| Data quality problems encountered | Real-world ingestion edge cases |
| Willingness to continue paying | The commercial answer |

**Outputs:** revised data model, onboarding, pricing evidence and any future pricing recommendation, metrics, module priority, and support process — plus real numbers to replace the estimates in `08_Cost_Analysis.md` and `09_Business_Model.md`. The current price remains €80/month unless a later accepted decision supersedes BD-005.

---

## Gate C — Pilot Validation

Proceed only when:

- Pilot businesses use the product repeatedly and unprompted
- Support effort per customer is sustainable for one person
- Data import is reliably standardisable
- Customers express genuine willingness to pay

---

# Phase 5 — Company and Commercial Readiness

**Epic:** `COP — Phase 5: Commercial Readiness`
**Deliberately placed after validation, not before.**

| Deliverable |
|---|
| Legal structure and company registration |
| Revenue/tax registration |
| Business banking |
| Accounting setup |
| Insurance |
| Customer contracts and Terms of Service |
| Privacy notice and data-processing terms (GDPR) |
| Production Stripe account |
| Brand identity finalised (see `13_Branding_Strategy.md`) |
| Domain and trademark clearance completed |

---

# Phase 6 — Public Launch

**Epic:** `COP — Phase 6: Public Launch`

| Deliverable |
|---|
| Production billing activated |
| Public pricing published (€80/month, BD-005) |
| Public marketing website live |
| First paying customers onboarded |
| Application health monitoring in place |
| Churn and support tracking |
| Change log maintained |

---

## Gate D — Launch Validation

Proceed only when:

- Billing works end-to-end, verified with a real transaction
- Legal and privacy documents are in place
- Monitoring and backups are proven
- Unit economics are acceptable against real data

---

# Phase 7 — Product Expansion

**Epic:** `COP — Phase 7: Expansion`
**Only after evidence.**

| Deliverable | Trigger |
|---|---|
| Direct POS integrations | Customer demand for the specific POS |
| Automated recurring imports | Manual upload friction proven |
| Second business template | Validated demand in a new vertical |
| ~~Production Events generalisation (ADR-016)~~ | ~~Second template being built~~ — completed early, in Phase 2/3 (Stage C8b, 04/08/2026), once cafe and pharmacy were both being actively scoped simultaneously rather than waiting for Phase 7 |
| Advanced forecasting (e.g. ML models incorporating external signals such as weather — relevant to Irish cycling demand) | Simple methods proven insufficient |
| Multi-location support | Customer with multiple locations |
| Custom/additional report scheduling (beyond the default weekly/monthly cadence in PR-8) | Requested repeatedly |
| Mobile workflows | Usage data shows mobile need |
| Real-time repair-entry screen (staff-facing "log a repair" workflow writing directly into `production_events`, `status="open"` → `"completed"`) | Shops want live workshop tracking, not just the periodic repairs-file upload (Stage B8c) — would also be the natural place to reliably capture `customer_id`/`performed_by_id`, which the upload path deliberately leaves NULL |
| Supplier tracking on purchases (matching/creating `Supplier` records, wiring the currently-unused `suppliers` table to purchase rows) | Shops need per-supplier reporting — deliberately out of scope for Stage B8c's purchases upload v1, mirroring sales' own precedent of not doing customer-matching |

---

# Sequencing Rationale

**Why discovery before build:** the schema-detection engine (the hardest part of Phase 2) cannot be built well without real customer files. Building first means guessing.

**Why validation before company registration:** legal and accounting costs are real and recurring. Incurring them before knowing anyone will pay converts a cheap experiment into an expensive commitment.

**Why billing is deferred to Phase 3:** it adds meaningful complexity and no learning value during prototyping.

**Why forecasting is deferred:** it is the most technically interesting module and the least necessary for proving core value. That combination makes it a classic trap.

---

# Business Perspective

The gates exist to protect founder time and money. Their purpose is to make stopping — or changing direction — a legitimate, planned outcome rather than an admission of failure.

---

# Customer Perspective

Pilot customers should feel like collaborators, not test subjects. Phase 4 should return value to them, not just extract data from them.

---

# Technical Perspective

Tenant isolation and formula testing are built in Phase 2, not added later. Retrofitting either is disproportionately expensive and error-prone.

---

# Commercial Perspective

Revenue is not expected before Phase 6. The scenarios in `09_Business_Model.md` reflect this — modest MRR by month 12 is the realistic base case, not a disappointment.

---

# Current Decisions

* Separate product validation from commercial launch (BD-004, Accepted)
* Customer discovery precedes serious build (Accepted)
* Payments deferred until Phase 3, production billing until Phase 6 (Accepted)
* Jira `COP` is the execution tracker; this document is the strategic source of truth (Accepted)

---

# Risks

* **Skipping or shortening Phase 1** is the most likely and most damaging failure mode. It is tempting because building feels productive and interviewing feels slow.
* Solo-founder capacity means phases will likely take longer than planned; the roadmap deliberately has no fixed dates for this reason.
* Gate criteria can be rationalised away under pressure. They should be assessed honestly, ideally with a second opinion.

---

# Future Improvements

* Add estimated durations per phase once Phase 1 gives a realistic sense of pace.
* Populate Jira epics and stories directly from this document.
* Add a lightweight weekly review checkpoint to catch drift early.
* **Transactional employee-email delivery before rollout.** Employee invitation flow, case-insensitive per-shop duplicate-email protection, pending-seat reconciliation, and the owner-side resend action are built. Real delivery is blocked until a Resend account is configured with a verified sender: set `RESEND_API_KEY` and `RESEND_FROM_EMAIL` in the deployment environment, then run one end-to-end invite delivery check. Do not put provider keys in source control.
* **Page styling sweep, low priority.** Reports, report detail, suppliers, transactions, Ask ORLA, and the authentication pages inherit the shared visual system but have not yet received tailored per-page layout treatment. Keep this cosmetic pass separate from functional/security work.
* **Real per-client AI cost control before launch (PR-5.5 follow-up).** `ai_daily_request_limit_per_business` (currently 200, `app/settings/config.py`) is a *request-count* cap sized for the free-tier testing model, not a real cost-control mechanism — once launched on a paid, non-`:free` model this needs to become a genuine *spend* cap instead: only a set percentage of the €80/month plan price can be allocated to a given client's token usage without eroding margin. Direct instruction for the intended design: track actual spend per business (the already-logged `AIRequest.cost_eur` per call gives this for free, just unused for limiting today), and once a client crosses roughly 50% of their allotted per-plan token/cost budget for the period, automatically move them to a free/cheaper model for the remainder rather than hard-blocking further questions outright — degrade gracefully, don't cut off. Not designed or built yet: needs the actual allocated-€-per-plan number decided, a spend-based (not count-based) check replacing/supplementing `count_today_for_business`, and a per-business "current model tier" that `_classify_intent`/`_generate_answer` read instead of the single static `openrouter_model` setting.

---

# Phase 3 Detail — Scheduled Reporting Implementation

Implementation checklist for the "Scheduled reporting" deliverable in Phase 3's table above.

Reconciled 09/08/2026 against Stage D17/D18's own changelog entry above, which had shipped
most of this without the checkboxes here ever being updated — this list previously read as
entirely outstanding when it wasn't. Removed: customer timezone storage/validation, weekly/
monthly timezone-aware schedules, the reusable cross-industry template, the top/bottom-
products/revenue/margin/forecast/recommendation/low-stock content itself, idempotency
(`(business_id, report_type, period_start)` unique constraint), bounded retries + tick-based
recovery, and the seven-day expiry mechanism (rows excluded from list queries past their
window, never hard-deleted — PR-8.5/PR-8.12) — all confirmed built per D17/D18. On-demand PDF
export also shipped since (v1.28, "Download PDF button"), separately from this checklist.

Genuinely still outstanding:

- [ ] Add an in-app notification/banner showing a report's seven-day expiry date — the expiry
      mechanism itself exists (above); nothing surfaces the countdown to the viewer yet.
- [ ] Add persistent-failure alerting — a `Report` row already serves as its own audit record
      of generation attempts/status (PR-8.12), but nothing notifies anyone when one keeps
      failing across retries.
- [ ] Add on-demand Word export (PDF export shipped in v1.28; Word was never built).
- [ ] Add dedicated tests for daylight-saving-time and timezone-boundary edge cases in the
      scheduler specifically (duplicate-prevention, forced-recovery, and inapplicable-
      business-template-section behavior are already covered per D17/D18's own test suite).
- [ ] Meter report compute, recovery attempts, temporary storage, notifications, and requested
      exports — inherently a pilot-phase measurement, not reachable until Gate C's pilot
      businesses are actually recruited.

This work is governed by PD-007 and ADR-019.

---

# Questions Still Open

* How many interviews are genuinely enough before Gate A — 15, or fewer if patterns emerge clearly?
* Should pilot customers be charged during Phase 4?
* Should the second business template be chosen during Phase 4, to inform ADR-016 before Phase 7?

---

# Revision History

| Version | Date | Changes |
|---------|------|---------|
| 0.1 | 29/07/2026 | Initial draft; structured for Jira epic mapping. |
| 0.2 | 30/07/2026 | Removed the completed €79-to-€80 housekeeping item and clarified that pilots test the accepted €80/month price. |
| 0.3 | 30/07/2026 | Marked the `01_Project_Vision.md` import-template housekeeping item complete; added Scheduled Reporting to Phase 3 (PD-007/PR-8 baseline weekly/monthly cadence); clarified Phase 7's "Report scheduling" as custom/additional scheduling only; synced version header with revision history. |
| 0.4 | 30/07/2026 | Removed the self-referential "(technical set)" Related Document. |
| 0.5 | 30/07/2026 | Fixed the Phase 3 "Scheduled reporting" line, which incorrectly described email delivery — corrected to match the accepted in-app-only design (PD-007/ADR-019). |
| 0.6 | 30/07/2026 | Renamed "Change 8 Delivery Work — Scheduled Reporting" to "Phase 3 Detail — Scheduled Reporting Implementation" and labelled it as elaboration on the Phase 3 table, so the heading follows this document's Phase/Gate structure instead of an internal work-item name. |
| 0.7 | 30/07/2026 | Added the conversational agent's business Q&A and product/support lanes, and low-stock alerting, to Phase 3 (PD-008, PD-009, ADR-020). Named weather-augmented ML forecasting as the concrete example of Phase 7's "Advanced forecasting" trigger. |
| 0.8 | 30/07/2026 | Added "Phase 2/3 Detail — Prototype Build Sequence": the founder-agreed, ordered 30-item checklist (Stages A-F) actually followed while building the prototype, tracked in place with checkboxes. |
| 0.9 | 03/08/2026 | Marked F28 (Stripe test-mode payments) complete — Checkout, webhooks, Customer Portal, subscription/invoice edge cases, and dispute handling all verified via live Stripe test-mode testing on localhost. |
| 1.0 | 03/08/2026 | Marked B6 (upload interface) and B7 (schema/column detection engine) complete. B7 v1 is scoped to sales transactions only, with entity type chosen explicitly at upload time; alias dictionary + structural heuristics + confirmation UI + mapping-profile reuse (PR-2.1-2.5) all verified via automated tests and a live end-to-end browser walkthrough, including reuse on a second upload from the same source. |
| 1.1 | 03/08/2026 | Marked B8 (clean data / transactional import + undo, PR-2.6-2.11) complete. Includes SKU/name product matching (auto-create on first sight, never falling back from a SKU miss to a name guess) and inventory_movements recording per sale — the previously-missing link needed for stock-level and product-popularity tracking. Verified via automated tests and a live API walkthrough (no frontend trigger UI built yet — flagged as a follow-up). |
| 1.2 | 03/08/2026 | Closed the two follow-ups flagged in 1.1/1.0: (1) added the frontend "Run import"/"Undo" trigger UI, exposing each upload's import summary and rejected-row reasons; (2) built PR-2.2's manual header-row-picker fallback for files where auto-detection can't confidently place the header, including persisting the picked row so B8's import step (which re-detects fresh each time) falls back to it instead of failing on the same file it already needed help with. Both verified via automated tests and a live end-to-end browser walkthrough. |
| 1.3 | 04/08/2026 | Added B8b: the `inventory` entity type (stock-count snapshot uploads), extending B7's detection engine and B8's import/undo pipeline beyond sales-only. Reconciles a snapshot against derived stock via a single adjustment movement per product, and closes an undo-ordering hazard found during design review that could otherwise let undoing a sales import silently corrupt stock after a later inventory reconciliation. Verified via 156 passing automated tests (unit, integration, tenant isolation) and a live end-to-end browser walkthrough that deliberately exercised the undo-guard bug scenario, confirming it is correctly blocked rather than silently succeeding. |
| 1.4 | 04/08/2026 | Added C8b: schema foundation for multi-vertical calculations. Accepted ADR-016 (Production Events, replacing bicycle-specific `repairs`/`repair_parts_used`), added ADR-022 (`inventory_lots`) and ADR-023 (pharmacy `prescription_details`). Checked off the Phase 0 housekeeping item this depended on; retired the now-moot Phase 7 "Production Events generalisation" trigger row (completed early). Schema/migration only — Stage C9 calculation work is next. |
| 1.5 | 04/08/2026 | Marked C9 complete: deterministic core calculations for Retail Operations and Financial Performance (PR-3), reading from `sales`/`sale_items`/`inventory_movements`/`products`. Workshop (repair) profitability intentionally deferred until `production_events` has a real-time entry workflow generating data. Every formula unit-tested (ED-007); verified against a real database via a new integration test. |
| 1.6 | 04/08/2026 | Marked C10 complete: deterministic findings & recommendations engine (PR-4) over C9's output — six rules, an approved recommendation library, impact-based ranking. Deliberately built the `low_stock` rule as a standalone function so C12 can reuse it directly instead of re-deriving the threshold logic; updated C12's line to say so. |
| 1.7 | 04/08/2026 | Marked C12 complete: real-time low-stock alerts (PD-009/PR-9), reusing C10's `evaluate_low_stock` rule unmodified. Fires from the existing import/undo commit points, persists to `alerts` with create/update-in-place/resolve dedup logic, adds per-product/category threshold columns. Flagged PR-9.3's owner-facing threshold configuration UI as a follow-up — no product-management API/UI exists yet to build it against. |
| 1.8 | 05/08/2026 | Added B8c: purchases and repairs upload entity types (the `"purchase"` `inventory_movements` reason, reserved since the model was designed, gets its first writer; repairs are ingested from a shop's own workshop log into `production_events`, same pattern as sales), plus a restock-freshness indicator. Refactored `run_import`/`undo_import`'s entity-type dispatch from a hardcoded if/else into dispatch tables for four entity types. Found and fixed a real bug during testing: the undo-ordering guard was unconditionally blocking `repairs` undos whenever any later inventory reconciliation existed, even though repairs write zero `inventory_movements` and can never be affected by one. Recorded two deferred feature ideas in Phase 7: a real-time repair-entry screen, and supplier tracking on purchases. |
| 1.9 | 05/08/2026 | Marked C11 complete: the first user-facing dashboard, plus a Workshop Performance calc (unplanned but roadmap-consistent, unblocked by B8c's repairs data) so it covers all three operational domains from day one. Charts render browser-side from structured API payloads (ADR-010, ECharts) with drill-down scoped to per-product/per-repair breakdown rows — full raw-record drill-down flagged as a follow-up, no endpoint lists individual sales rows yet. Verified via the backend suite (246 passing), a clean frontend build, and a live walkthrough against the running Docker Compose stack. |
| 1.10 | 05/08/2026 | Added B8d: remapping a confirmed-but-not-yet-run upload, closing a real gap found live (a mapping mistake had no way back short of re-uploading). Deliberately scoped to `status == "mapped"` only — remapping after a completed (even undone) import stays unsupported, since the source file is already deleted from R2 by then (ADR-008); recovery there is still re-upload. Found and fixed a second-order trap while building it: naively allowing remap to re-run the normal saved-profile reuse check would have silently reapplied the very mapping mistake the user was trying to fix, since `confirm_mapping` had already saved it as that source's profile — fixed by skipping the reuse fast path on a remap. Verified via the full test suite (250 passing) and a live walkthrough deliberately swapping SKU/product-name columns, remapping, and confirming the corrected data landed right. |
| 1.11 | 05/08/2026 | Two gaps found using C11's dashboard, fixed: (1) `rank_top_sellers` conflated "makes the most money" with "actually sells the most" — a high-price, low-volume item could outrank a genuinely popular one on revenue alone. Split into `rank_top_sellers_by_units`/`rank_top_sellers_by_revenue`, surfaced as a dashboard filter toggle rather than one ambiguous list. (2) Inventory (stock-count) uploads never touched `Product.cost_price` — only sales-at-creation and purchases did — so any product first seen via an inventory upload showed "Value at Cost: unknown" indefinitely. Added an optional `unit_cost` column to the inventory upload, mirroring purchases field-for-field (same alias list, extracted to a shared constant; same "optional, last value in file wins" semantics; reuses `ProductRepository.update_cost_price`). Verified via the backend suite (258 passing), a clean frontend build, and a live walkthrough confirming the units/revenue toggle produces genuinely different rankings against real data. |
| 1.12 | 05/08/2026 | Flipped sales import precedence: `total_amount` (what was actually charged) now wins over `unit_price × quantity` whenever both are mapped and disagree — previously the reverse, which silently discarded the file's total in favour of a derived figure. Real-world impact this fixes: a POS export's "total" is very often tax-inclusive while "unit price" isn't, so *every* row would mismatch and get silently overridden to the pre-tax figure; the warning read as a data-quality problem when it usually wasn't. Reworded the warning to explain the likely tax cause. Also added a `quantity <= 0` rejection for sales rows (mirrors purchases' `non_positive_quantity`, reworded to entity-agnostic wording since the message template is shared) — closes a `ZeroDivisionError` crash risk this change made far more reachable (division by quantity now happens whenever `total_amount` is present, not only when `unit_price` is absent). **Known limitation, not solved here:** `cost_price_at_sale` is untouched by this change and is typically tax-exclusive (a supplier cost), so gross margin is now computed against revenue that may include tax when `total_amount` was the winning source — margin % can read a few points higher than the true economic margin for shops whose exports include tax in "total." Proper VAT-aware margin (a distinct net-amount/tax-rate field) is future scope. Verified via the backend suite (260 passing). |
| 1.13 | 05/08/2026 | Closed v1.12's flagged limitation: added an optional `tax_amount` field to the sales upload (mirrors `cost_price_at_sale`'s exact shape — new nullable `sale_items.tax_amount` column). `app/analytics/financial.py`'s `GrossMarginResult` gains `net_gross_profit`/`net_gross_margin_pct`, computed only over lines where both cost *and* tax are known (never blending an uncertain line into a "confirmed net" figure), plus `tax_data_coverage_pct` — the same PR-3.5 completeness pattern used everywhere else, one level stricter. The dashboard shows one headline "Gross margin" number: the net figure whenever any tax data exists, falling back to the original tax-inclusive figure with an honest caveat when it doesn't — never two competing percentages. Also sharpened the `total_amount_mismatch` check to account for a known `tax_amount`, so a shop that maps price + tax + total together sees the warning mostly disappear, since tax was the actual explanation for it. Verified with a controlled scenario showing the real gap this closes: 50.0% tax-inclusive margin vs. 38.5% true net margin on the same data. **Explicitly not built:** Workshop Performance's `price_charged` (repairs) has the identical tax exposure but wasn't part of the report that prompted this; same fix pattern applies later. Verified via the backend suite (267 passing) and a clean frontend build. |
| 1.14 | 05/08/2026 | Found and fixed a real revenue-accuracy bug in B7's detection engine (`app/imports/detection.py`), surfaced by a pilot's actual test file (a POS export with both a pre-tax `Subtotal` column and a genuine post-tax `Total Amount` column): layer 1's alias matching was strictly first-column-wins, and `"subtotal"` is a listed synonym for our `total_amount` field — since `Subtotal` sat to the left of `Total Amount` in the file, it silently claimed the `total_amount` mapping, leaving the real total unmapped and revenue understated by the tax amount on every row. Fixed with a new sub-pass: an exact canonical-name match (a column literally called `total_amount`/`Total Amount`) is now claimed *before* any alias/synonym match is considered, regardless of column order — a structural fix that protects all four entity types (sales/inventory/purchases/repairs) against this whole class of alias-shadowing bug, not just this one instance. Also, prompted by the same investigation: reworded the sales/repairs field-mapping labels to explicitly say "charged to the customer" vs. "what YOU paid" (the two were being read backwards), added a framing note above the mapping form, and updated the `total_amount_mismatch` warning to also name per-line discounts as an expected, harmless cause (confirmed: `total_amount` already correctly absorbs any discount since it's trusted as-is, so a discount-driven mismatch needs no fix, just correct labelling — no discount field was added). Verified via a new regression test reproducing the exact bug shape, the full backend suite (268 passing), and a clean frontend build. |
| 1.15 | 06/08/2026 | Closed a data-accuracy gap raised directly (a client accidentally re-uploading an overlapping export would silently double-count revenue/stock/workshop income — nothing checked a new file against what was already imported). Sales' existing `order_reference` is now checked against every previously-imported sale for the business before a group is written; two new optional fields, `purchase_reference` and `repair_reference`, do the same for purchases and repairs (which had no reference field at all before this). A row/group reusing an already-used reference is rejected outright (`duplicate_reference`, in the existing rejection summary — never silently imported), including within a single file (a second occurrence of the same PO/job number later in the same upload). Undoing an import correctly frees its references for reuse with no extra code, since undo deletes the underlying rows entirely. Inventory (stock-count) uploads were deliberately left out of this — reconciliation there is already idempotent by design against *current* stock, not a per-file transaction log, so there's no equivalent "re-imported the same file twice" hazard to close. **Known, stated limitation, not solved here:** a row with no reference at all (most real-world exports today) has no reliable natural key — a genuine repeat sale and an accidental duplicate look identical, and no heuristic (date+product+price matching) was added, since it would misfire on real repeat transactions. Verified via the backend suite (281 passing), a clean frontend build, and a live walkthrough against the running Docker stack: uploaded a sales file, re-uploaded an overlapping one (rejected, revenue unchanged), undid the first, then confirmed the same reference re-imports successfully. |
| 1.16 | 06/08/2026 | Marked C13 complete: deterministic forecasting baseline (seasonal/moving average, PR-10) — the first stage of the platform that predicts anything forward-looking, and still not AI (Business Logic First holds). Covers both business-wide revenue and per-product unit demand, the latter feeding a clearly-caveated `suggested_reorder_quantity`. See C13's own roadmap entry above for the full design. New dashboard "Forecast" section: a revenue line chart with a confidence band, a 7/14/30-day horizon toggle, and a per-product reorder table sorted by urgency (soonest-to-need-restocking first). Found and fixed a real design-time bug before shipping: an early version zero-filled the lookback window all the way back to the 90-day cap regardless of how much real history existed, which silently defeated the "don't forecast on too little data" gate for any new business. Verified via the backend suite (296 passing) and a clean frontend build. |
| 1.17 | 06/08/2026 | Made the forecast section's confidence range and reorder caveat visible by default rather than hover-only, per direct feedback — the "typical range" is a sample standard deviation, not a formal statistical guarantee, and that distinction shouldn't depend on a user hovering to find it. Also fixed a real correctness bug found live: v1.15's cross-upload duplicate-reference dedup for purchases keyed on `purchase_reference` alone, so a genuine multi-line PO (one PO number covering several different products — completely normal) had every line after the first wrongly rejected as a duplicate. Real data from a pilot's 1656-row purchase-order export triggered this exactly: 835 rows incorrectly rejected. Fixed by keying purchases' dedup on `(reference, product_id)` instead of the reference alone — resolving the product now happens before the duplicate check, not after. Applied the same fix defensively to repairs (`(reference, description, price_charged, labour_cost)` — repairs have no product_id equivalent, so the rest of the row's detail disambiguates instead), since one invoice covering multiple repairs is the same plausible real-world shape, even though no live bug was observed there yet. Also clarified the pre-existing, unrelated `duplicate_product_in_file` warning: split into a separate `duplicate_product_cost_overwritten` code for purchases specifically, since "used the last value" read as if stock quantity were being dropped when it's actually only about which row's cost price is kept — every row's quantity was always added regardless of this warning. Verified via the backend suite (300 passing, including new regression tests for both the multi-line-PO-imports-correctly case and the genuine-duplicate-still-rejected case) and a live re-verification against the running Docker stack confirming a multi-line PO now imports both lines. |
| 1.18 | 06/08/2026 | Closed a data-accuracy gap raised directly: a purchase dated before a shop's most recent stock count is very likely already reflected in that count, so applying its quantity again would silently inflate derived stock. `_write_purchases` (`app/imports/importer.py`) now rejects any purchase row whose date is strictly before the business's last completed, unreversed inventory import's effective date — reusing `ImportRecordRepository.latest_completed_by_entity_type` as-is, no new query needed. Rejected outright by default (new code `purchase_predates_last_stock_count`, same rejection-summary treatment as `duplicate_reference` — no movement written, not silently applied), with an explicit client-controlled override: a new `include_purchases_before_last_stock_count` query param on `POST .../uploads/{id}/import`, surfaced as an unchecked-by-default checkbox next to "Run import" for purchases uploads specifically, for the case where the physical count genuinely predates the delivery and the system's date-only guess is wrong. Recovery for "ran without checking it, want those rows counted after all" is undo + re-upload with the box checked — the same pattern every other import decision in this pipeline already uses (no new recovery mechanism invented). **Deliberately scoped to purchases only, stated not solved:** repairs never write `InventoryMovement` at all, so this class of risk is structurally N/A there; sales carry the identical risk in the opposite direction (a sale dated before the last count may already be reflected in that lower count too) but building that mirror-image guard is deferred, not silently ignored — sales' write path is more complex (multi-item grouping) and this wasn't part of what was reported. Verified via the backend suite (304 passing, 4 new integration tests covering the reject/override/no-stock-count/undone-stock-count cases) and a live walkthrough against the running Docker stack confirming a stale purchase is rejected by default and imports correctly with the override checked. Also deleted an empty `TEST_SHOP` test business from the dev database at the user's request (zero uploads/products — confirmed before deleting). |
| 1.72 | 14/08/2026 | **Ask ORLA: multi-intent answers, extended follow-up memory.** Direct request, following review/approval of a client-proposed "multi-intent planner": a compound question ("what's my revenue and what should I reorder") previously only ever got answered as one intent, silently dropping the rest. `_classify_intent`'s JSON contract changed from one object to a 1-3 item `"intents"` array (same single classify call — no new AI round-trip added anywhere in this change, kept the cost-bound guarantee), with a defensive backward-compat fallback for a bare non-array object (a free-tier model has a documented history of not always following the exact requested shape). `answer_question` restructured around a `_Part`-per-sub-intent model: each part resolves either fully deterministically (a metric definition, a lookup's 0/many-match message, a "no data for this" note, or an unresolved out_of_scope refusal — zero further AI cost) or needs the one shared explain call. The single-intent case (the overwhelming majority of real questions) is the byte-for-byte original code path — verified, not just asserted, by keeping every one of the 69 pre-existing `test_ai_chat*.py`/`test_ai_context_trimming.py`/`test_ai_glossary.py` tests passing unmodified bar one intentional fix (a `product_lookup` early-result was losing its own distinct `out_of_scope` intent label — see Errors). For a genuine multi-part question, the merged explain call is told to answer each `part_N` on its own line behind a fixed `⟦PART_n⟧` marker; the response is split deterministically and each segment is guardrail-checked (`validate_grounded`) against *only that part's own context* — stricter than one merged check — so one ungrounded part gets replaced with a safe fallback while the other parts keep their real, grounded text, instead of the whole multi-part answer being discarded over one bad claim. Falls back to a single coarse whole-answer guardrail check if the model doesn't follow the marker instruction cleanly (fail-closed, same posture as every other "don't trust the model's exact shape" spot in this module). Conversation memory stays deliberately single-exchange (unchanged — this module already regressed twice from classify-prompt token growth, per `_classify_intent`'s own `max_tokens` history) — the only addition is a new plural `previous_intents`/`ChatResponse.intents` field, purely structural, so a follow-up after a compound answer can recover via the existing `_CONTINUABLE_INTENTS` net against any part of the previous turn, not just the first. **Tests**: 8 new unit (`test_ai_multi_intent.py` — intents-array parsing, capping at `_MAX_SUBINTENTS`=3, the bare-object backward-compat fallback, malformed/empty-list handling) + 9 new integration (`test_ai_chat.py` — a real 2-lane compound question grounding both parts, one part failing grounding while the other survives, one part with genuinely unavailable data disclosed alongside a valid part, a 0-match lookup mixed with an aggregate intent, the marker-split-failure coarse fallback in both its grounded and ungrounded forms, an explicit single-intent regression guard, and `previous_intents` recovering against a non-first part). 911 backend tests passing (up from 895 before this round; some pre-existing `_parse_intent_json`-calling tests in `test_ai_explicit_date_parsing.py` updated for the new `list[ClassifyResult]` return shape, no behavior change). Clean frontend build — `ChatResponse.intents`/`ChatRequest.previous_intents` added, chat page hint copy updated to mention compound questions; no client-side marker-splitting needed, since the backend already strips markers and joins parts with a blank line before the answer ever reaches the frontend, which `whiteSpace: pre-wrap` already renders as separate paragraphs. **Errors found and fixed during build**: (1) the new `.format()`-templated classify prompt literal `{"intents": [...]}` needed escaping to `{{"intents": [...]}}` — caught immediately by the existing test suite (`KeyError: '"intents"'`), not live; (2) `_build_part`'s lookup-intent branch initially reused the outer `intent` variable for an early-result part instead of `early_result.intent` — `product_lookup` with no search term at all is meant to resolve as `"out_of_scope"` specifically (see `_dispatch_lookup`), not `"product_lookup"`, and the one pre-existing test asserting this caught the regression immediately. **Manual verification — live, real OpenRouter, real Test Bike Shop data**, via `docker compose exec backend python`: a plain single-intent question came back identical in shape to before (`intents=('financial_performance',)`, no stray markers); a real 2-part question ("how's my revenue doing, and what should I reorder soon?") came back as one coherent answer covering both, with the existing deterministic priority-order list correctly attached to the forecast half; a real 3-part question (revenue + reorder + biggest expense category) answered all three, `intents=('financial_performance','forecast','category_breakdown')`, still exactly 2 `ai_requests` rows per question regardless of part count (confirmed via direct query) — the cost-bound guarantee holding under real multi-part load, not just in mocked tests. This same live run surfaced a real near-truncation: the multi-part explain call's `max_tokens=1200` left `tokens_out` at 1191 and 1198 on the two real multi-part answers — single digits from the ceiling, not a comfortable margin — raised to 1600 afterward, verified the existing test suite still passes (a numeric-only change, no test asserts the exact value). |
| 1.84 | 19/08/2026 | **PDF supplier-invoice ingestion.** Direct request from a pasted implementation spec: a shop with no CSV/XLSX purchase export can now upload a supplier invoice PDF, review/correct what was extracted, and confirm it into the real purchase ledger — never a blind OCR-to-database write. Confirmed with the user before building: **v1 covers text-native PDFs only** — a scanned/image-only PDF gets an honest rejection, not a guess; OCR needs its own separate cost/GDPR/vendor review the spec itself requires before any paid provider is introduced, and `pyproject.toml` had no PDF/OCR library at all before this. **The core architecture decision**: once a human confirms the reviewed invoice, its lines are handed to `app/imports/importer.py`'s existing `_write_purchases` (renamed `write_purchases_batch`, made public — now has a second caller) via a real, lightweight `Upload`+`ImportRecord` pair (`entity_type="purchases"`, the same literal value CSV purchases already use, not a new type) built for the purpose. This gets, for free, already-tested SKU-then-name product matching (never a silent weak guess), supplier match-or-create, cost-price updates, `(reference, product_id)` duplicate-reference rejection, every `InventoryMovement` write, and — because `entity_type` is literally `"purchases"` — `importer.py`'s existing `undo_import` dispatch table reverses an invoice-originated purchase batch with **zero code changes**, already correctly leaving products/suppliers in place. New `app/invoices/` package (mirrors `app/imports/`'s shape): `pdf_reader.py` (the only file importing `pdfplumber` — real file-signature/page-count/size validation, typed `EncryptedPdf`/`CorruptPdf`/`NoExtractableText`/`InvoiceTooManyPages`/`UnsupportedInvoiceFileType` exceptions, multi-invoice PDFs deliberately not detected or split); `extraction.py` (pure — keyword-anchored header-field parsing reusing the *existing* `app/imports/value_parsers.py::parse_date`/`parse_money` directly; line items via `pdfplumber`'s ruled-table detection **only** — deliberately no word-position column-clustering fallback, since a half-built layout heuristic risks silently misattributing a value to the wrong column, worse than an honest "couldn't detect a line-item table" state; arithmetic validation surfaces issues, never silently corrects); `matching.py` (reuses `ProductMatcher`/`SupplierMatcher`/`CategoryMatcher` from `app/imports/importer.py` unchanged, adding one new supplier-SKU lookup layered in front); `duplicates.py` (source-file hash, normalised supplier+invoice-reference, and date+currency+grand-total signals, all business-scoped by construction — exact matches block, a plausible match requires an explicit `override_duplicate_warning` flag, mirroring `confirm_mapping`'s existing `confirm_multiple_locations` escape-hatch shape exactly); `service.py` (orchestration — synchronous extraction on upload, since this codebase has no background job queue; review/correction; confirm; undo; discard). New `invoice_drafts`/`invoice_draft_lines` tables (additive migration, JSON provenance-plus-typed-final-value shape mirroring `ImportMappingProfile`'s own pattern — raw/value/confidence/issue per field, never a rigid column per attribute). **Retention, a disclosed judgment call**: `docs/governance/19_Data_Retention_Proposal.md` confirms no approved retention policy exists yet for anything in this schema; rather than invent an unreviewed "keep the PDF forever" commitment for what may be commercially sensitive supplier data, this follows the one real precedent that does exist (NFR-5/ADR-008: CSV files are deleted from R2 after a successful import) — the PDF is deleted right after a successful confirm, and the durable trace-back record (spec's own requirement) is the retained *structured* `InvoiceDraft`/`InvoiceDraftLine` rows, never the raw bytes. An abandoned, never-confirmed draft's PDF is only cleaned up by a new explicit discard action (matching the pre-existing, accepted gap that an abandoned CSV upload also has no automatic cleanup). New routes under `/businesses/{id}/invoices` — upload/confirm behind `require_active_subscription` (matching the existing purchase-upload permission tier exactly, not owner-only), everything else behind plain membership; the PDF-preview route is tenant-scoped (unlike the company-logo route's deliberate public exception — a supplier invoice is real commercial data). Frontend: a new "Invoice PDF" section on the existing Upload Data page (multipart upload via the already-existing `apiPostFile` helper, built for the company-logo feature) linking to a new `/uploads/invoices/[id]` review screen — editable header/line fields, a product-search picker reusing the existing global-search endpoint, duplicate-warning banner with the override checkbox, a confirm-impact preview, and undo/discard. **A real self-match bug found and fixed before this shipped**: `duplicates.check_duplicates`'s source-file-hash check queried for *any* draft with a matching hash, with no exclusion for the very draft being checked — since a draft obviously always matches its own hash, every single confirm attempt was seeing itself as an "exact duplicate" of itself and blocking outright. Caught immediately by the new confirm-flow integration tests (not live), fixed with an `exclude_draft_id` parameter threaded through both the repository query and every `check_duplicates` call site. A second, smaller fix during review: `InvoiceLineOut`/`InvoiceDraftOut` initially carried only a bare `matched_product_id`/`supplier_id` UUID with no way for the review screen to show a friendly name for an auto-matched line/supplier — added resolved `matched_product_name`/`matched_product_sku`/`matched_supplier_name` fields, computed server-side via one batched lookup per request. **Tests**: 69 new (28 unit — `pdf_reader.py` against realistic generated fixtures via a new shared `tests/invoice_pdf_helpers.py` covering valid/encrypted/corrupt/image-only/oversized/wrong-signature/too-many-pages; `extraction.py`'s header/line parsing and arithmetic validation, both end-to-end against a generated fixture and via hand-picked values for the pure recompute helpers reused by post-edit corrections; 31 integration — the full service-layer lifecycle including product/supplier auto-matching, exact/plausible duplicate detection with override, blocking-issue gating, confirm writing through the real `write_purchases_batch`, undo leaving created products/suppliers intact, and the full HTTP API walkthrough including a real purchase showing up in Transactions afterward; 10 tenant-isolation — every route 404s under the wrong business, duplicate detection never crosses businesses). 1,058 backend tests passing (up from 989). Clean frontend build. **Live verification against the real dev stack** (real Postgres, real R2 bucket — not the SQLite test suite): uploaded a real generated invoice PDF against Test Bike Shop, confirmed real extraction (supplier/reference/date/lines all correct), confirmed real R2 upload-then-download-matches, resolved lines and confirmed the import — real `Product`/`Supplier`/`InventoryMovement` rows landed in the real database, the real R2 object was deleted immediately after, undo correctly reversed the movements while leaving the created product/supplier in place — then cleaned up every row this created (an explicit throwaway verification, not a permanent seed, unlike v1.82's disclosed weather backfill). Could not rebuild the backend Docker image to bake `pdfplumber` in permanently — this sandbox's apt mirror (`deb.debian.org`) is currently unreachable/unsigned, a pre-existing environment limitation unrelated to this change; `pyproject.toml` is correctly updated so a normal rebuild picks it up, and the already-running dev container has it live-installed for this session's verification. Could not walk through the frontend review screen in a real logged-in browser (this session's standing rule against typing credentials) — verified instead via the HTTP-level API tests exercising the exact same routes the frontend calls, plus a clean TypeScript build. **Explicit limitations, stated in the deliverable, not hidden**: multi-invoice PDFs (not detected or split); scanned/image-only PDFs (no OCR in v1 — a separate, deliberately deferred decision); credit notes, purchase orders, and barcode-only documents (not recognised as distinct document types); accounting-grade tax handling (arithmetic is checked, not authoritative — this is a purchase/restock feature, not an accounting system); a table-less invoice layout (no ruled table detected) reaches review with zero extracted lines and an honest issue code, since no fragile word-position fallback was built. |
| 1.83 | 19/08/2026 | **Ask ORLA: two weather-pattern chat questions, direct follow-up to v1.81/v1.82.** Two new fixed intents, same single-classify-call/single-explain-call architecture as every other intent (ADR-020) — no new AI round-trip added. `weather_pattern_lookup` ("does weather affect sales of [category]?"): resolves a free-text category via new `find_category` (`app/application/lookups.py`, same `LookupResult` 0/1/many shape as `find_products`/`find_purchases`/`find_repairs`) and a new `get_weather_pattern_comparisons_for_category` (`app/application/weather_insights.py`, extracted from a shared `_load_weather_and_sales_data` loader also reused by the existing `get_weather_pattern_findings`) — deliberately **not** gated on the upcoming forecast matching, since "what does my own history show" should answer even when nothing in this week's forecast happens to match a bucket. `weather_outlook` ("what should I expect this week given the forecast?"): calls the existing forecast-gated `get_weather_pattern_findings` directly, zero new data-shaping. Both honest on the zero-data paths at zero further AI cost — an unmatched category names this business's own real category list rather than a generic "not found"; a resolved category with too little history yet says so plainly; an empty `weather_outlook` says "nothing weather-notable stands out" instead of guessing. New `_dispatch_weather_category_lookup` mirrors `_dispatch_lookup`'s exact 0/1/many shape. Compliance boundary holds automatically, unchanged: neither new code path ever puts a raw Met Éireann figure into context, only the bucket label and this business's own real sales numbers, same as the underlying Finding. **Four real bugs found only by live-testing against the real dev stack (real OpenRouter, real backfilled Test Bike Shop data) — none caught by the full passing test suite, since the mocked-response integration tests never exercised the real model's own output shape**: (1) the classify call's `max_tokens` (1000, sized for the v1.72 multi-intent work) proved too tight once these two intents' description paragraphs grew the classify system prompt further — a real `weather_outlook` question burned the full 1000-token reasoning budget with `content=None`, reproduced with `tokens_out=1000` in the logged `AIRequest` row; raised to 1400. (2) `_dispatch_weather_category_lookup`'s context used `dataclasses.asdict(comparison)` directly, leaving `pct_difference`/`avg_on_bucket_days`/`avg_on_other_days` as raw Python `Decimal` objects — `app/ai/guardrail.py::flatten_numeric_values` only recognises int/float/str leaves (every other intent's context arrives already-JSON-serialized via a Pydantic schema's `.model_dump(mode="json")`), so every one of these numbers was silently invisible to the guardrail and a numerically-correct answer was rejected as "unsupported" 100% of the time. Fixed with a new `_json_safe()` helper (`json.loads(json.dumps(value, default=str))`, matching the same string-serialization the rest of this module already relies on) applied to both new context-building sites — same root-cause class as v1.81's direct-import mocking bug, a real gap in how this new code diverged from an established convention. (3) A real division of two integer-ish quantities produces an arbitrary-precision repeating `Decimal` (`21.69291338582677165354330709...`) no human phrasing would ever restate verbatim — `compute_weather_pattern_comparison` (`app/analytics/weather_patterns.py`) now quantizes `pct_difference` to a whole percent (matching the existing Finding message's own `.0f}%` formatting) and the two average-unit fields to one decimal, computed *after* the materiality gate check (never before, so quantization can't push a value across the 15% threshold on rounding alone) — live-verified this was still not automatically sufficient on its own (see next). (4) Even after (3), a real `weather_outlook` answer summarising three categories at once still further-rounded every percentage in its own natural phrasing ("22%" instead of the already-whole "22%" and "-22%" instead of "-23%" — the live case was `-22.3` rounding down to `22`) — not a bug in the given data, just the model's own paraphrase tendency once several figures appear in one answer; resolved by widening the pct precision from one-decimal to whole-number at the source (folding into fix (3) above) so what's given already matches what a natural summary would say. Also found, structurally: `weather_outlook`'s raw `Finding.evidence` dicts carried no explicit signal that they represented conditions matching *this specific week's* forecast (as opposed to generic unrelated history) — a real live answer said "no forecast data is available" even though every entry only exists because the real upcoming forecast already matched it; fixed with an explicit deterministic `"note"` string prepended to the context (plain Python text, not a calculation, so no Core Rule concern), mirroring `_generate_answer`'s own pre-existing `scope_note` pattern for the identical class of "don't leave the model to infer what a payload means" problem. **Tests**: 4 new integration for `get_weather_pattern_comparisons_for_category` (no coordinates, no weather history, a real pattern surfaced with zero forecast mocking at all — the intent's own distinguishing behavior versus the forecast-gated Finding — and an unknown category id); 8 new integration in `test_ai_chat.py` (one-match reaching the explain call, 0-match naming real categories, several-match disambiguation, a resolved category with too little history, no search term falling back safely, `weather_outlook` with a matching forecast, `weather_outlook` with nothing notable, and `weather_outlook` recovering on a period follow-up while `weather_pattern_lookup` correctly does not, matching the existing `_CONTINUABLE_INTENTS` exclusion reasoning already documented for the other lookup intents). 983 backend tests passing (up from 971). **Live verification, both questions, real OpenRouter, real Test Bike Shop data** (a container-local CA trust addition was needed to unblock outbound HTTPS from this dev sandbox — a session-only, non-repo change, not a production concern): "Does weather affect sales of Tyres & Tubes?" → grounded, correctly cites both the cold (-17%) and mild/dry (+22%) patterns from real history; "What should I expect this week given the forecast?" → grounded, correctly names all three real categories with real forecast-week figures (Locks -22%, Scooters -16%, Tyres & Tubes +22%); "Does weather affect sales of Sunglasses?" (a category that doesn't exist) → honest 0-match message naming the business's real 10 categories, zero AI cost. Exactly 2 `AIRequest` rows per question that reached the explain step, 1 for the zero-cost 0-match case — the fixed-cost guarantee held under real load. |
| 1.82 | 19/08/2026 | **Weather-pattern insight: real backfill so it's actually visible on the live dev site, direct request.** v1.81 shipped correctly but silently — production only ever accumulates its own weather history forward, one real day at a time, so there was nothing yet to show on the real Dashboard. Backfilled 383 real days of real Dublin weather (the same disclosed Open-Meteo stand-in as v1.81's own pre-build validation, since Met Éireann's historical archive is still down) directly into the real `weather_observations` table for Test Bike Shop via the actual `WeatherObservationRepository` — a one-time, disclosed seed of real (not fabricated) data, not a schema or behavior change; the production scheduler itself still only ever calls Met Éireann, forward, one day at a time. Confirmed live immediately afterward, through the exact call the Dashboard makes (`get_findings`): **3 real findings now appear** — Tyres & Tubes +22% on mild/dry days (70 real matching days), Locks -22% (61 days), Scooters -16% (10 days, the thinner one already flagged in v1.81) — consistent with the pre-build validation's own numbers. Log in as Test Bike Shop's owner and check the Dashboard's Findings & Recommendations section (or a newly-generated report) to see it live. |
| 1.81 | 19/08/2026 | **Weather-pattern sales insight — built, tested against real data, kept.** Direct instruction, following the redirected design from v1.80's ML assessment: not a point-forecast model, a deterministic correlation between real historical sales and a small, fixed weather-day taxonomy. **New**: `app/weather/client.py` (Met Éireann's live forecast API, the one file allowed to talk to it — confirmed working again live, HTTP only, no key); `Business.latitude`/`longitude` (resolved lazily via the existing Geoapify client, `app/geocoding/service.py::resolve_and_persist_coordinates`, whenever an address field changes — degrades to `NULL` exactly like Geoapify's own existing "not configured" gap); a new `weather_observations` table, ORLA's own accumulating daily record (`app/application/weather_ingestion.py::snapshot_daily_weather`, one snapshot per business per local day past 20:00, wired into the existing scheduler tick) — production never depends on Met Éireann's own historical archive, which was confirmed down in v1.80 and stayed down through this build; `app/analytics/weather_patterns.py` (pure — classifies days into `rainy`/`cold`/`windy`/`mild_dry`, compares real average daily category sales on matching vs. non-matching days, refusing below 10 real matching days or an under-15%-materiality difference, same "don't invent a pattern from too little data" posture as `forecasting.py`'s own `MIN_HISTORY_DAYS`); `app/application/weather_insights.py`, wired into the existing `get_findings` (a new `weather_pattern_insight` Finding type, severity `info`, appended whenever the upcoming forecast genuinely matches a bucket with real supporting history) — **zero frontend changes needed**, confirmed live: `frontend/lib/findings.ts`'s recommendation rendering is already generic by `finding_type`, so the new Finding shows up in the existing dashboard/reports Findings & Recommendations section automatically. **Compliance boundary held by construction**, per the direct instruction: `WeatherPatternComparison`'s own fields never carry a Met Éireann figure at all (no rain-mm/temp-C/wind-kph anywhere in a `Finding.evidence` or message) — only the bucket label (ORLA's own classification) and the business's own real sales numbers, confirmed via a dedicated test asserting the forbidden keys are absent. **A real bug found and fixed before this shipped**: `weather_insights.py`/`weather_ingestion.py` initially did `from app.weather.client import get_forecast` (a direct import) instead of importing the module and calling `weather_client.get_forecast(...)` — every other provider client in this codebase is mocked in tests via `monkeypatch.setattr(module, "function_name", ...)`, and a direct-imported name doesn't pick up that patch, so the first version of the integration tests were silently making real, uncontrolled live calls to Met Éireann during `pytest` runs instead of using the mock. Caught by an unexpected result in a test assertion, not by a lucky guess — fixed by matching this codebase's own established mocking convention. **Tests**: 34 new (14 unit for the bucket/comparison math with hand-computed fixtures, mirroring `test_forecasting.py`'s own rigor; 6 unit for the Met Éireann XML parser against a real captured response shape, including a genuine DST gotcha caught live — August-dated fixtures gave wrong hand-computed answers because Dublin is UTC+1/BST in August, not the UTC+0 this codebase's existing fixtures rely on for February dates; 7 integration for `weather_insights.py`; 6 integration for `weather_ingestion.py`; 2 scheduler-tick extensions). 971 backend tests passing (up from 936). Clean frontend build (no changes needed, confirmed rather than assumed). **Live validation — the actual "do we get good results" answer, run twice**: (1) *Pre-build spike*, before writing any production code: backfilled a real year of Dublin weather from Open-Meteo's historical archive (a disclosed, one-off testing substitute only, since Met Éireann's own archive was down — production never uses it) and ran the real `compute_weather_pattern_comparison` function against Test Bike Shop's real 2025-08-01→2026-07-31 sales history (5,798 real sale-item rows, 10 real categories). Found 7 real comparisons clearing both gates — most notably Tyres & Tubes (the highest-volume category, largest sample sizes: 70 mild-dry days and 86 cold days) showing a consistent, intuitively sensible, opposite-direction pattern (+21.7% on mild/dry days, -17.4% on cold days) — real evidence the underlying approach surfaces genuine, actionable patterns, not noise. Two of the seven results (Scooters, the lowest-volume category at 46 units/year) were flagged as statistically thinner, not filtered out post-hoc to flatter the results — the sample-size/materiality thresholds were fixed before this run, not tuned afterward. (2) *Post-build, end-to-end*: ran the real production code path against the real dev Postgres database and the real (live, not mocked) Met Éireann endpoint — set Test Bike Shop's real coordinates, called the real `snapshot_daily_weather`, confirmed a real row landed in `weather_observations` (rain_mm=0.60, temp_mean_c=14.30, wind_speed_kph=12.96 — genuine live Dublin weather), then called the real `get_weather_pattern_findings`, which correctly and honestly returned zero findings — exactly right, since only one real day of ORLA's own accumulated history exists at this point, correctly below `MIN_BUCKET_DAYS`. **Recommendation: keep it, let it accumulate.** The pre-build correlation test proved the underlying signal is real on a full year of data; the post-build test proved the production wiring is correct and honestly conservative while that year of history is still accumulating from scratch (a real, disclosed trade-off of not depending on Met Éireann's down archive — first genuine insights will start appearing automatically once ~10+ days of a given weather pattern have accumulated for a business, no further code change needed). **Explicitly deferred, not silently dropped**: a dedicated Ask ORLA chat intent for this (the original 5-point direction's own 4th point) — the Finding already surfaces everywhere Findings already show, a chat intent is real, separable follow-up work once more real-world history has accumulated to see it fire organically. |
| 1.80 | 17/08/2026 | **Weather-enhanced ML forecasting: assessed, didn't hold up, deferred with a redirected design for later.** A first experiment (`train_orla_weather_model.py`, not part of this repo, trained separately on 100% synthetic data) trained a `HistGradientBoostingRegressor` to predict 7-day revenue/units from ~103 features including weather, and reported beating a rolling-7-day-average baseline (units MAE 0.378 vs 0.577, revenue MAE €46.85 vs €81.29). Read against this codebase's own production forecasting stack (`app/analytics/forecasting.py`, `app/application/forecast.py`, `app/application/business_group.py`, PR-10, and Stage C13's roadmap entry) before drawing any conclusion — assessment only, nothing in this repo changed. **Verdict: EXPERIMENT ONLY / effectively NO-GO on that artifact.** Confirmed, not assumed, from the code and its own output files: (1) the comparison baseline was a flat rolling-average extrapolation, not ORLA's real shipped baseline (`compute_baseline_forecast`'s day-of-week seasonal-naive with its 14/21-day history gating) — the headline "beats baseline" number isn't measured against the right yardstick; (2) no ablation isolated weather's own contribution from the ~40 non-weather features (lags, rolling averages, stock state, calendar) already doing most of the work — the reported lift can't be attributed to weather at all as reported; (3) every weather feature was either the origin day's own weather or a backward-looking lag/rolling average — none represented the actual forecast-horizon weather (the days being predicted) — meaning even a clean rerun of the same design structurally cannot deliver "a bad-weather week is coming, order accordingly," the actual product idea; (4) the model produced a bare point estimate with no confidence interval at all, a direct conflict with the already-shipped, non-negotiable PR-10.2 ("ship every forecast number with a confidence range"); (5) training data was entirely synthetic, pre-cleaned, and gap-free — none of the messy-real-export problems this platform's own import pipeline has repeatedly had to handle were exercised. **Direction discussed with the user for when this is picked up again, and the one to build toward:** not a black-box point-forecast model at all — a deterministic **weather-pattern correlation insight**, fully consistent with `CLAUDE.md`'s Core Rule ("AI never calculates"). Concretely: classify historical and forecast days into a small fixed taxonomy (rainy/cold/windy/mild, mirroring the flags the assessed experiment's own feature set already used — `is_rainy_day`, `bad_cycling_weather`, etc.) using real ingested weather; for each product/category, compute actual historical average sales during days matching each bucket vs. the all-day average — a plain, unit-testable aggregation in the same style as every other `app/analytics/*.py` module, gated by a minimum sample size per bucket (same "refuse below a threshold" philosophy as `MIN_HISTORY_DAYS`) so it never claims a pattern from a handful of days. When the upcoming week's real weather forecast matches a bucket with enough history, surface it as a new Finding ("historically, rainy days like the ones forecast this week saw X% more/less demand for [category]") — narrated by ORLA, never invented by it, and layered as *context* alongside the existing forecast, not a replacement for it. **Explicitly preserved, direct instruction:** order-quantity and stock-cover numbers stay grounded in the exact sales-movement math this app already has (`compute_baseline_forecast`, unchanged) — weather becomes an annotation a shopkeeper can read and verify against their own real sales history, never a hidden input that silently moves the number itself. This sidesteps every failure mode above: no train/serve mismatch (no model to version), no cross-tenant pooling question (computed per-business from that business's own history, same as everything else here), no missing-confidence-interval gap (a real historical range, not an invented one). Real remaining work when this is picked up: a weather ingestion module (`app/weather/client.py`, the one file allowed to hold a weather-provider SDK, per this repo's existing "no provider SDK outside its dedicated client module" pattern) — provider TBD, needs both a historical-observation archive and a forward forecast for Irish/EU coverage, not picked just because it's free; reuses the existing Geoapify-based business/branch geocoding for lat/lon, no new geocoding work; scheduled via the existing `scheduler` service's tick pattern, with a graceful no-op fallback (never blocks the existing deterministic forecast) when weather data is stale or missing. No code, dependency, migration, or config changed as part of this assessment — read-only, per direct instruction. **Follow-up vendor spike (18/08/2026), direct request, also read-only — provider chosen: Met Éireann, since it's free.** Confirmed live rather than assumed: the forecast endpoint (`http://openaccess.pf.api.met.ie/metno-wdb2ts/locationforecast`) returned real data for a real Test Bike Shop coordinate with no API key, covering ~10 days ahead (comfortably past the 7-day horizon) with hourly-to-6-hourly resolution and temperature/wind/humidity/pressure/cloud/dewpoint/precipitation (the last with a probability and min/max range, not just one figure) — HTTPS is not supported on that host (confirmed: TLS handshake fails), HTTP only. Historical daily data exists for real stations near Dublin (Dublin Airport, Casement, Phoenix Park, Dunsany) back to October 2015 — accessed per-station, not per-coordinate, confirming the earlier note that a nearest-station resolution step (reusing the existing Geoapify-based geocoding) is real, buildable work. Read the actual licence text directly (a `.odt` file, extracted manually since most tools can't open it) rather than trusting a summary: commercial SaaS use is explicitly fine (a standard royalty-free, worldwide, irrevocable grant), but publishing/broadcasting the data by any means carries an ongoing obligation to also publish Met Éireann's current Weather Warnings, kept live-synced and unmodified — a real, separate feature, not a one-time attribution line. **Decision: comply by construction, not by adding the warnings feature** — the derived weather-pattern insight (§ above) will never display Met Éireann's own figures (no raw temperature/rain/wind values in any dashboard, chat answer, or report) — only ORLA's own self-defined bucket label ("similar rainy periods") and the business's own real historical sales numbers, which are not Met Éireann's data at all. This is the deliberate scope boundary for when this gets built: the weather API response is consumed only as an internal signal to pick a bucket, never serialized into any user-facing payload. Still read-only — nothing in this repo changed beyond this note. |
| 1.79 | 17/08/2026 | **Company logo now actually visible somewhere.** Direct follow-up: v1.78 shipped the upload/preview/remove controls, but the uploaded logo was only ever rendered back on that one Company Profile settings page — nowhere else in the app showed it, so uploading one had no visible effect anywhere a user would normally look. `AppNav.tsx` (rendered on every business-scoped page — dashboard, uploads, reports, chat, products, suppliers, transactions, notifications) now fetches the selected business's `has_logo`/`updated_at` (`GET /businesses/{id}`, same call the profile page itself already makes) and renders the logo image next to the "OR ORLA" platform brand mark in the top-left of the nav whenever one exists — same public `<img>` + `?v=updated_at` cache-busting pattern as the profile page's own preview, new `.app-nav__business-logo` CSS class (30px, `object-fit: contain` since a shop's logo can be any aspect ratio, unlike the fixed-square brand mark). Verified live against the real Test Bike Shop business (which already had a real logo uploaded, confirmed via a direct unauthenticated `GET .../logo` returning a real 512x512 PNG) and a clean frontend build. |
| 1.78 | 17/08/2026 | **Company logo upload.** Direct request: an owner-only way to upload a company logo from the Company Profile page (`frontend/app/onboarding/[id]/page.tsx`) — nothing like this existed before (no logo field on `Business`, no image storage route anywhere outside the CSV/XLSX data-import pipeline). New nullable `Business.logo_content_type` column doubles as the "has a logo" flag (`NULL` = none) and the value served back as `Content-Type`, so no separate stored object key/extension field is needed — the R2 key is always deterministic (`logos/{business_id}/logo`, computed on the fly, never stored), so a re-upload just overwrites it with nothing orphaned to clean up. Unlike this app's only prior file-storage path (presigned browser-to-R2 PUT URLs, for large CSV imports), a logo is small enough to validate synchronously and POST straight through the backend as ordinary multipart — new `r2_client.py::put_object_bytes`, this app's first *direct* (non-presigned) R2 write. Three new routes in `app/api/businesses.py`, alongside the existing `PATCH /{business_id}` company-profile edit they mirror: `POST/DELETE .../logo` are owner-only (same `membership.role != "owner"` gate), rejecting a content-type outside `{image/png, image/jpeg, image/webp}` (400) or over 5MB (413). **`GET /businesses/{business_id}/logo` is deliberately public — no auth/membership dependency at all** — confirmed with the user before building: a plain `<img>` tag can't send this app's JWT `Authorization` header, and every other route here requires one; a shop logo isn't sensitive like sales/financial data, so this is a narrow, disclosed exception to the otherwise-universal "every route is tenant-scoped and authenticated" posture, not something slipped in quietly — only the logo bytes are exposed, nothing else about the business. `BusinessOut` gained `has_logo: bool` (so the frontend knows whether to render the `<img>` at all, rather than guaranteeing a 404 request for every logo-less business) and `updated_at` (newly exposed on this schema at all — used as the `?v=` cache-buster on the logo `<img>` src, since a re-upload overwrites the same URL and the browser would otherwise keep showing a stale cached copy). Frontend: `lib/api/client.ts` gained an exported `API_URL` (the public `<img>` tag needs to build the same base URL the rest of this module resolves, since it can't go through the auth-header wrapper) and `apiPostFile` (multipart, no explicit `Content-Type` — the browser sets its own boundary); new "Company logo" section on the Company Profile page, gated the same owner-vs-staff way every other edit control on that page already is. Also required adding `python-multipart` as a real dependency — this is this app's first `UploadFile` route; every prior upload used a presigned URL instead, so it was never needed before. **Tests**: 11 new integration (`test_business_logo_api.py`, new — upload success flips `has_logo`, public GET with no auth serves the right bytes/content-type, GET 404s for no-logo and for a nonexistent business, owner-only enforcement on both POST and DELETE, wrong content-type → 400, oversized → 413, delete clears the flag and GET 404s afterward, delete on a never-uploaded business is a harmless no-op, cross-tenant upload/delete both 403). R2 monkeypatched to an in-memory store in tests, same style already used for `r2_client` elsewhere in this suite. 936 backend tests passing (up from 925). Clean frontend build. **Manual verification — live, scripted, against the real dev Postgres database** (not the SQLite the automated suite uses): ran the full upload → public-fetch → wrong-type-reject → oversized-reject → no-auth-reject → delete → 404-after-delete → cleanup sequence through the real FastAPI app and real DB via a disclosed script, all steps passing. **Note**: this dev environment's `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` are still blank (checked directly) — same pre-existing gap category as the Resend key was before v1.75/v1.77, not something introduced by this change — so the live verification's R2 layer itself was monkeypatched to an in-memory store rather than a real bucket; flagged here rather than silently glossed over. A full logged-in browser walkthrough of the new upload button itself was not completed this round (no active session in the open browser tab, and typing credentials in on the user's behalf is against this session's standing rule) — the scripted live check above exercises the identical route/DB code path the UI calls, but a first real click-through is still worth doing. |
| 1.77 | 17/08/2026 | **Employee invite email: proper branded message, clear about the prefilled signup link.** Direct follow-up request. The invite email's own subject/body (`backend/app/email/service.py::send_employee_invite_email`) was plain, minimal text that stated the raw registration URL and told the employee to "use this exact email address" — read like an instruction/warning rather than a reassurance, and didn't say the signup link's email field is already filled in for them (`frontend/app/signup/page.tsx` has read and prefilled `?email=` from this exact link since task #158 — the message just never said so). Rewritten as inline-styled HTML (no external CSS/JS — email clients strip both) matching this app's own real branding: the same "OR" mark/green (`#24765a`, `frontend/app/globals.css`'s own `--app-green`) every real login/signup page already uses, a clear "Set up your ORLA account" button, and an explicit line — "the link below already has your email address ({email}) filled in, so you'll just need to choose a password." Subject changed from "...on AI Operations Platform" to "...on ORLA," matching what the product actually calls itself everywhere a signed-in user sees it. Every interpolated value (`business_name`, `to_email`, `registration_url`) is now `html.escape()`d before being spliced into the HTML — business_name in particular is whatever an owner typed into the Company Profile form, never assumed safe to splice in as-is; not previously escaped. **Tests**: 5 new unit (`test_email_service.py`, new — subject/body mention ORLA and the business name, the "already filled in / choose a password" copy is actually present, a business name containing `<`/`&` is correctly escaped and never appears unescaped in the output, and the two existing not-configured/provider-error graceful-degradation paths — previously only exercised indirectly through the employee-seat integration test — now have direct coverage). 925 backend tests passing (up from 920). No frontend change (the signup-page prefill this email now correctly describes already existed). **Manual verification — live, real Resend send**: sent the new template to the account's own Resend-registered email (the only recipient Resend's still-unverified sandbox domain will currently accept — see v1.76's own note) and visually confirmed the branded layout, button, and copy render as intended. |
| 1.76 | 17/08/2026 | **Change-login-email + post-login welcome page.** Direct follow-up to a gap-check: two of three confirmed-missing pieces approved to build. (1) **Change-login-email**: new `frontend/app/account/page.tsx` — the only `updateUser(...)` call anywhere in this frontend used to be reset-password's own `{ password }` call; there was no page that could ever call `updateUser({ email })`. Frontend-only — `backend/app/security/auth.py` already re-syncs the local `User.email` mirror from the JWT on every request, so once the frontend can trigger a change, the backend picks it up automatically with no code change needed there. Shows the current email, a new-email + confirm-new-email form, and an honest generic confirmation message after submitting (doesn't assume one specific Supabase project security-notification configuration). **Owner-only, direct follow-up request**: `Business.role` is already the caller's own role on each business (`GET /businesses` is tenant-scoped to the caller's own memberships), so the change-email form only renders when the account owns at least one business — no extra fetch needed. Disclosed rather than overstated: this is a frontend-only gate — `updateUser({ email })` is a direct Supabase client SDK call with no backend route of ours in the path at all, so there is nothing server-side of ours to check a role against; a staff member could still technically call it themselves via the browser console. Closing that fully would mean routing this through a backend endpoint using Supabase's Admin API, which needs a `SUPABASE_SERVICE_ROLE_KEY` this project has deliberately avoided everywhere else (ADR-024) — not built here, flagged instead. **Reauthentication, direct follow-up request**: the form now requires the account's current password before `updateUser({ email })` fires — Supabase has no separate "verify this password without disturbing the session" call, so this reuses the exact same `signInWithPassword` the login page itself already calls, which simply fails (without otherwise changing anything) on a wrong password. Google-only accounts (no password credential at all, per Supabase's own `user.identities` array) skip this step entirely rather than being asked for a password that was never set — a real edge case this app already has, since `login/page.tsx` supports both flows. **Also confirmed live, not a code bug**: a real invite-email send attempt after the Resend API key was configured returned a 403 from Resend's own API — `onboarding@resend.dev` (Resend's shared sandbox sender) can only deliver to the Resend account's own registered email until a custom domain is verified at resend.com/domains; reproduced directly via `app/email/client.py::send_email` (succeeded sending to the account's own address, failed with Resend's own "you can only send testing emails to your own email address" message otherwise). Nothing to fix in this codebase — recorded here so this doesn't get mistaken for a regression later. (2) **Post-login welcome page**: new `frontend/app/welcome/page.tsx` is now the target of both `login/page.tsx` redirects (password login and Google OAuth) — previously both sent every login, brand-new owner and returning owner/staff alike, straight to `/onboarding`'s "Your businesses" management page; grepping the whole frontend for "welcome" returned zero matches anywhere. The welcome page: "Welcome back, {first name}" (resolved via a new shared `frontend/lib/hooks/useCurrentMember.ts` hook, extracted from `AppNav.tsx`'s own pre-existing inline identity-resolution effect — AppNav itself refactored to use it too, no behavior change), a preview of the top 5 current findings (`GET /businesses/{id}/analytics/findings`, already severity-ranked server-side — no new backend logic), an honest empty state when there are none yet, and a "Go to your Dashboard" button. A genuinely new account (zero businesses) is redirected straight to `/onboarding` instead — there's nothing yet worth welcoming them to, and that page's own create-business form is already correctly gated to exactly this case. `frontend/lib/hooks/useBusinessSelector.ts` gained an additive `loaded` flag (every existing caller ignores it) so the zero-businesses redirect can't fire on a false-empty first render before the real fetch resolves. `frontend/components/AppNav.tsx` also gained an "Account" link next to "Company Profile". `signup/page.tsx`'s own redirect deliberately left pointing straight at `/onboarding`, not `/welcome` — a signup is definitionally about to need the create-business flow, so routing through `/welcome` first would just be an extra bounce. No backend changes at all for either part. No frontend test suite exists in this codebase (verification has always been build + live walkthrough) — `npm run build` clean; confirmed live that both new pages correctly redirect an unauthenticated visitor to `/login`, same guard every other authenticated page already uses. |
| 1.75 | 14/08/2026 | **Ask ORLA: real live bug, a legitimate multi-order purchase history misreported as "which one did you mean?".** Direct follow-up report, two messages after v1.74 shipped: "what did I order under E-Motion Trail 500 3?" (one specific, unambiguously-named product) came back "I found several matching results" listing the *same product* five times over — not different candidates, just five real past orders — with no way to actually get an answer. A follow-up, "...in my last order?", got the identical unhelpful list again, ignoring the "last order" phrasing entirely. Root cause: `find_purchases`'s final fallback (`app/application/lookups.py`) resolves a search term to exactly one product, then fetches every past purchase of it — genuinely several *legitimate* rows, not competing candidates — but handed them to `_dispatch_lookup` (`app/ai/service.py`) through the exact same `matches`/`match_labels` shape used for real ambiguity (several *different* products matching a fuzzy name search, from v1.74's fix), so the shared 0/1/many dispatcher couldn't tell "here is the complete answer, several rows long" apart from "I don't know which of these you meant." Fixed with a new `LookupResult.resolved_rows` field, set only when a search term has unambiguously identified one thing (a product, or a PO reference) that legitimately has more than one row to report — `_dispatch_lookup` now short-circuits straight to the explain step with all of them as context the moment this is set, before ever reaching the disambiguation branch, so a multi-order history becomes a real multi-row answer instead of a dead-end question. Also closes the same latent gap for a multi-line PO reference match, not just the reported product case — both paths through `find_purchases` now return `resolved_rows`. Separately handles the ignored "last order" follow-up: a new deterministic keyword net (`_looks_like_last_order_only_question` — "last order", "last time", "most recent purchase", etc., same pattern as every other keyword net in this module) narrows `resolved_rows` down to just the single most-recent row before it reaches the explain call, when the question actually asks for it — `list_purchases`'s own most-recent-first ordering makes this a plain slice, no new query. **Tests**: 2 new integration (`test_ai_chat.py`) — a real 3-order purchase history for one unambiguous product answering completely instead of asking to disambiguate (asserting the explain call is actually reached, `AIRequest` count 2, not the zero-cost disambiguation path); the same history narrowed to just the most recent PO reference when "in my last order" is asked, asserting the two older PO references never reach the explain call's own system prompt at all. Every pre-existing lookup test (including v1.73/v1.74's own new ones) still passes unmodified — the genuine-ambiguity case (different products matching a search term) is completely untouched by this change. 920 backend tests passing (up from 918). No frontend change. **Manual verification — live, real OpenRouter, real Test Bike Shop data**: re-ran both exact reported questions after the fix. |
| 1.74 | 14/08/2026 | **Ask ORLA: real live bug, non-ASCII dash mismatch silently defeating lookup search.** Direct follow-up report from a live transcript, one message after v1.73 shipped: "How many E‑Motion Trail 500 did I order last time?" came back "I couldn't find anything matching" — but the product genuinely exists (`SKU-00031`, confirmed via direct query). Root cause: the reported question's hyphen is U+2011 (NON-BREAKING HYPHEN), not the plain ASCII "-" (U+002D) actually stored in the product's name — `ProductRepository.search_by_name_or_sku`'s `LIKE` match is byte-exact, so two visually identical strings that differ by one Unicode codepoint matched as completely different. Not a "the model isn't finding close matches" fuzzy-matching gap — a real character-encoding mismatch, most likely introduced by the customer's own browser/keyboard/input method (smart-punctuation-style dash substitution), entirely outside this app's control on the input side. Fixed at the comparison layer instead: new `app/text_normalize.py` (`normalize_dashes`/`normalize_dashes_column`) collapses six common dash/hyphen-like Unicode variants (hyphen, non-breaking hyphen, figure dash, en dash, em dash, minus sign) to a plain ASCII `-`, applied on **both** sides of every free-text substring/exact match this app does: `ProductRepository.search_by_name_or_sku` (name substring, sku exact, and the "(SKU)" trailing-annotation split), `InventoryMovementRepository.list_purchases`'s `purchase_reference` match, and `ProductionEventRepository.find_repairs`'s `repair_reference`/`description_contains` matches — normalizing the stored side too (not just the incoming search term), since a client's own imported product/reference data could just as plausibly arrive with one of these from a spreadsheet export. Deliberately narrow and deterministic (CLAUDE.md's Core Rule) — this closes one demonstrated, specific class of visually-identical-character mismatch, not a general AI-driven fuzzy-matching engine. **Tests**: 4 new unit (`test_text_normalize.py` — every variant collapses correctly, plain ASCII text is untouched, the exact reported transcript's string round-trips to the real product name, the SQL-side expression compiles with one `REPLACE()` per variant) + 1 new integration (`test_ai_chat.py`, reproducing the exact reported transcript's literal character end to end through `answer_question`, asserting the lookup now succeeds). 917 backend tests passing (up from 912). No frontend change. **Second real bug, found immediately by this same fix**: with the dash mismatch closed, `search_by_name_or_sku` now correctly found *three* real products ("E-Motion Trail 500", "500 2", "500 3") for the reported search term — genuinely ambiguous. `find_purchases`'s own "matched several products, which one?" branch (`app/application/lookups.py`) populates `match_labels` but deliberately leaves `matches` empty (there's no single resolved purchase record yet, only candidate product identities) — and `_dispatch_lookup` (`app/ai/service.py`) keyed its entire 0/1/many decision off `matches` alone, so this genuinely-ambiguous case was still misreported as "I couldn't find anything matching," the exact opposite of what happened. Every other lookup-result-producing function (`find_products`, `find_repairs`, `find_purchases`'s own reference-match path) always keeps `matches` and `match_labels` the same length — `match_labels` is the one field populated in every real case including this exceptional one, so `_dispatch_lookup` now keys its 0/1/many decision off `match_labels` instead, fixing this for all three lookup intents at once, not just purchase_lookup. **Tests**: 1 more new integration test (`test_purchase_lookup_search_term_matching_several_products_asks_which_one_instead_of_reporting_not_found`), seeding two real ambiguous products and asserting the correct "found several — which one?" disambiguation. 918 backend tests passing (up from 917). **Manual verification — live, real OpenRouter, real Test Bike Shop data**: re-ran the exact reported question (byte-for-byte, including the real U+2011 character) after both fixes — now correctly returns the actual "E-Motion Trail 500" disambiguation/answer instead of a false "not found." |
| 1.73 | 14/08/2026 | **Ask ORLA: real live bug, purchase_lookup leaking unrelated purchases.** Direct report from a live transcript: "How many E-Motion Trail 500 did I order last time?" (a product that doesn't exist in the catalog) came back as "I found several matching results" listing five completely unrelated products/purchases the user never asked about. Root cause in `app/application/lookups.py::find_purchases`: when a search term matched neither a purchase reference nor any real product, the code fell through to `InventoryMovementRepository.list_purchases(product_id=None, start=None, end=None)` — but `product_id=None` there means "no product filter," not "match nothing," so an unresolved search term silently returned the 10 most recent purchases *business-wide*, mislabelled as a disambiguation list for products that were never a match at all. Fixed: a search term that resolves to neither a reference nor a product match now returns a clean "not found" immediately, the same way `product_lookup`/`repair_lookup` already behaved — never broadens into an unscoped browse (that behavior is preserved only for a genuinely bare, no-search-term question, which legitimately means "show me purchases in this date range"). **Why the existing test suite didn't catch this**: the one pre-existing "no match" regression test (`test_purchase_lookup_with_no_match_is_a_deterministic_zero_cost_answer`) ran against an empty business with zero other purchases, so the broken fallback had nothing to leak and passed by coincidence. Added `test_purchase_lookup_with_no_product_or_reference_match_never_leaks_unrelated_purchases`, seeding one real unrelated product/purchase and asserting it never appears in the answer. 912 backend tests passing (up from 911). No frontend change. **Manual verification — live, real OpenRouter, real Test Bike Shop data**: re-ran the exact reported question after the fix; now returns a plain "couldn't find anything matching" answer instead of the five unrelated purchases. |
| 1.72 | 14/08/2026 | **Ask ORLA: multi-intent answers, extended follow-up memory.** Direct request, following review/approval of a client-proposed "multi-intent planner": a compound question ("what's my revenue and what should I reorder") previously only ever got answered as one intent, silently dropping the rest. `_classify_intent`'s JSON contract changed from one object to a 1-3 item `"intents"` array (same single classify call — no new AI round-trip added anywhere in this change, kept the cost-bound guarantee), with a defensive backward-compat fallback for a bare non-array object (a free-tier model has a documented history of not always following the exact requested shape). `answer_question` restructured around a `_Part`-per-sub-intent model: each part resolves either fully deterministically (a metric definition, a lookup's 0/many-match message, a "no data for this" note, or an unresolved out_of_scope refusal — zero further AI cost) or needs the one shared explain call. The single-intent case (the overwhelming majority of real questions) is the byte-for-byte original code path — verified, not just asserted, by keeping every one of the 69 pre-existing `test_ai_chat*.py`/`test_ai_context_trimming.py`/`test_ai_glossary.py` tests passing unmodified bar one intentional fix (a `product_lookup` early-result was losing its own distinct `out_of_scope` intent label — see Errors). For a genuine multi-part question, the merged explain call is told to answer each `part_N` on its own line behind a fixed `⟦PART_n⟧` marker; the response is split deterministically and each segment is guardrail-checked (`validate_grounded`) against *only that part's own context* — stricter than one merged check — so one ungrounded part gets replaced with a safe fallback while the other parts keep their real, grounded text, instead of the whole multi-part answer being discarded over one bad claim. Falls back to a single coarse whole-answer guardrail check if the model doesn't follow the marker instruction cleanly (fail-closed, same posture as every other "don't trust the model's exact shape" spot in this module). Conversation memory stays deliberately single-exchange (unchanged — this module already regressed twice from classify-prompt token growth, per `_classify_intent`'s own `max_tokens` history) — the only addition is a new plural `previous_intents`/`ChatResponse.intents` field, purely structural, so a follow-up after a compound answer can recover via the existing `_CONTINUABLE_INTENTS` net against any part of the previous turn, not just the first. **Tests**: 8 new unit (`test_ai_multi_intent.py` — intents-array parsing, capping at `_MAX_SUBINTENTS`=3, the bare-object backward-compat fallback, malformed/empty-list handling) + 9 new integration (`test_ai_chat.py` — a real 2-lane compound question grounding both parts, one part failing grounding while the other survives, one part with genuinely unavailable data disclosed alongside a valid part, a 0-match lookup mixed with an aggregate intent, the marker-split-failure coarse fallback in both its grounded and ungrounded forms, an explicit single-intent regression guard, and `previous_intents` recovering against a non-first part). 911 backend tests passing (up from 895 before this round; some pre-existing `_parse_intent_json`-calling tests in `test_ai_explicit_date_parsing.py` updated for the new `list[ClassifyResult]` return shape, no behavior change). Clean frontend build — `ChatResponse.intents`/`ChatRequest.previous_intents` added, chat page hint copy updated to mention compound questions; no client-side marker-splitting needed, since the backend already strips markers and joins parts with a blank line before the answer ever reaches the frontend, which `whiteSpace: pre-wrap` already renders as separate paragraphs. **Errors found and fixed during build**: (1) the new `.format()`-templated classify prompt literal `{"intents": [...]}` needed escaping to `{{"intents": [...]}}` — caught immediately by the existing test suite (`KeyError: '"intents"'`), not live; (2) `_build_part`'s lookup-intent branch initially reused the outer `intent` variable for an early-result part instead of `early_result.intent` — `product_lookup` with no search term at all is meant to resolve as `"out_of_scope"` specifically (see `_dispatch_lookup`), not `"product_lookup"`, and the one pre-existing test asserting this caught the regression immediately. **Manual verification — live, real OpenRouter, real Test Bike Shop data**, via `docker compose exec backend python`: a plain single-intent question came back identical in shape to before (`intents=('financial_performance',)`, no stray markers); a real 2-part question ("how's my revenue doing, and what should I reorder soon?") came back as one coherent answer covering both, with the existing deterministic priority-order list correctly attached to the forecast half; a real 3-part question (revenue + reorder + biggest expense category) answered all three, `intents=('financial_performance','forecast','category_breakdown')`, still exactly 2 `ai_requests` rows per question regardless of part count (confirmed via direct query) — the cost-bound guarantee holding under real multi-part load, not just in mocked tests. This same live run surfaced a real near-truncation: the multi-part explain call's `max_tokens=1200` left `tokens_out` at 1191 and 1198 on the two real multi-part answers — single digits from the ceiling, not a comfortable margin — raised to 1600 afterward, verified the existing test suite still passes (a numeric-only change, no test asserts the exact value). |
| 1.19 | 06/08/2026 | **Supersedes v1.18.** Reframed per direct feedback: most shops export sales and their stock count together, daily, from the same POS — stock naturally reflects "final stock of the day" — and the platform will eventually ingest data automatically rather than by manual upload, where "upload order" isn't a coherent concept at all. Replaced v1.18's order-based reject/override guard with a general, order-independent fix: `InventoryMovement` gains `event_date` (the calendar date a movement's underlying event actually happened — `sale_date`/`purchase_date`, or a stock count's new optional `as_of_date`, defaulting to the upload's processing date) and `resulting_quantity_on_hand` (an adjustment row's authoritative reconciled total, stored directly rather than re-derived). `InventoryMovementRepository.sum_by_product_ids` — the one function nearly every dashboard/forecast number reads current stock through — is rewritten around these: the most recent stock count (by its own date, not upload order) becomes the baseline, and only sales/purchases dated *after* it are added on top; anything dated on or before is presumed already reflected in that count and excluded automatically, correctly, regardless of what order the underlying files arrived in. Same function name and signature, so every caller (`retail_operations.py`, `alerts.py`, `forecast.py`) gets the corrected math for free. v1.18's reject/override mechanism is retired entirely (its `include_purchases_before_last_stock_count` param and uploads-page checkbox both removed) — every purchase row now writes normally, always (movement, cost update, reference dedup all unaffected), with a new purely-informational warning (`purchase_excluded_from_current_stock`) replacing the old rejection. A new migration backfills `event_date` for existing rows: exactly, via a join to `sales.sold_at`, for `sale` movements; as a stated one-time approximation from `created_at` for `purchase`/`adjustment` movements, which never captured a real date before this. **Deliberately deferred, not bundled into this already-large change:** the undo-ordering guard (`ImportRecordRepository.has_later_completed_inventory_import` / `ImportSupersededByLaterInventoryImport`) may itself now be redundant under this same date-aware model — worth revisiting separately. Verified via the backend suite (310 passing, including a new dedicated `test_date_aware_stock.py` covering multiple reconciliations over time, an out-of-order backdated reconciliation, and the sales-side mirror case), a clean frontend build, a real migration run against the local dev Postgres (backfill confirmed on ~8,000 existing rows), and a live walkthrough against the running Docker stack proving the exact scenario reported: a stock count of 40, a stale purchase of 25 correctly excluded (with the new warning) and a fresh purchase of 10 correctly added — current stock 50, not 40, not 75. |
| 1.71 | 14/08/2026 | Undo-ordering regression audit, direct request via prompt file (`ORLA_Undo_Ordering_Date_Aware_Stock_Audit_Prompt.md`). **Confirmed as instructed**: `ImportSupersededByLaterInventoryImport` (the old v1.19-era upload-order guard) is genuinely absent — grep across the codebase finds it only in historical comments, never as a class/import; `undo_import`'s signature takes no order-related argument. `undo_import` is intentionally, correctly unconditional today, exactly as v1.20 left it. **One real, demonstrated regression found and fixed — not a date-aware-calculation bug, but in the reversal lifecycle itself**: `Return.sale_item_id` has no `ON DELETE CASCADE`, and `_undo_sales_import` (`app/imports/importer.py`) bulk-deleted `SaleItem` rows without deleting their `Return` rows first — a real `ForeignKeyViolation` in Postgres on undoing *any* sales import that included even one return, reproduced live against the real dev database. Completely invisible in this project's own SQLite-backed integration-test suite, which doesn't enforce foreign keys by default — the exact same blind spot this codebase's own comments already flagged for every other FK in this undo path, just not yet hit here. Fixed with a new `ReturnRepository.bulk_delete_by_sale_item_ids`, called before the existing `SaleItem`/`InventoryMovement` bulk deletes, in the same explicit-FK-order style already used for the rest of `_undo_sales_import` — no schema change, no cascade added at the DB level (kept consistent with this undo path's own stated preference for explicit, portable deletes over relying on cascades). **Test matrix**: 15 new (`backend/tests/integration/test_undo_ordering_stock_integrity.py`, new) covering the prompt's full 12-item required matrix plus an explicit guard-absence check — the return-undo regression itself (asserting persisted rows, not just a lack of exception, so the test is meaningful even under SQLite); undoing a backdated purchase (confirming `Product.cost_price` deliberately isn't reverted on undo, the same "current-state mutations aren't undone" policy already true for auto-created products and `sell_price`, now documented in code rather than left implicit); exact-delta restoration for post-count sale/purchase undo; undoing an older already-superseded import followed by the newest count, falling back correctly; undoing the real baseline after an out-of-order backdated count, letting the backdated one correctly take over; same-day sale-undo and count-undo, both directions; undoing the only count on file, falling back to plain signed arithmetic; a multi-product multi-line order undone with no partial deletion or cross-product leakage; re-import after undo staying correctly date-aware (complementing, not duplicating, the existing order-reference idempotency test in `test_importer_service.py`); tenant isolation on undo itself, not just import; every downstream consumer (`list_product_thresholds`/`get_retail_operations`/`global_search`) reading the post-undo value, not a stale pre-undo one; and the legacy-adjustment baseline surviving an entirely unrelated undo elsewhere in the same business. 879 backend tests passing (up from 864), no frontend files touched. **Manual verification — live, not backend-tests-only**: reproduced the exact return-undo regression against the real dev Postgres (crashed before the fix, confirmed via `docker compose exec backend python`, identical `Upload`/`ImportRecord` writes and `r2_client` object PUT the browser upload flow itself produces), fixed, then re-ran successfully against real Test Bike Shop data and confirmed live via the actual owner session — both the Product Reorder Rules page and the global search bar showed the correct post-undo stock (100, exactly as computed) with no crash. Separately reproduced and confirmed the prompt's own named "historically blocked order" (undoing an older, already-superseded import while a newer count still exists) against real data, live-correct at 100 throughout. All test artifacts cleaned up in both businesses used — no residue left in either. |
| 1.70 | 13/08/2026 | Audited and hardened the date-aware stock calculation against sales backdating, direct request via prompt file (`ORLA_Sales_Backdating_Stock_Integrity_Prompt.md`). **Investigation first, per the prompt's own explicit instruction not to duplicate a guard already handled by the baseline calculation**: traced a real multi-line sales order through parsing/grouping/`Sale`/`SaleItem`/`InventoryMovement`/undo/every downstream reader. The v1.19 date-aware model (`InventoryMovementRepository.sum_by_product_ids`) already correctly implements every row of the prompt's required-behaviour table — sales/returns/purchases all key off their own source calendar date (never upload time), same-day is treated as "already reflected," a backdated reconciliation processed out of order never overrides a newer-dated one, undo recomputes fresh with no separate guard needed. No sales-specific reject/override mechanism was added — none was needed. **One real, demonstrated bug found and fixed**, outside sales-backdating itself but inside the same legacy-compatibility investigation the prompt required: migration `8b3e6c1a4f92` correctly backfills `event_date` for pre-existing "adjustment" rows but, correctly, invents no `resulting_quantity_on_hand` it has no way to know — `sum_by_product_ids`'s old `resulting_qty or 0` coerced that missing value to a real "0 units counted" the moment such a row became a product's most recent adjustment, silently discarding its entire historical stock (proven live: a legacy row worth 100 units computed as 0). Fixed entirely within `sum_by_product_ids` — no schema change, no backfill: a legacy adjustment (`resulting_quantity_on_hand IS NULL`) is now excluded from baseline candidacy and instead folded into the ordinary additive pass using its own `quantity_delta`, subject to the same baseline-cutoff rule as every other movement — reconstructing the old pre-event_date flat-sum total exactly when no valid baseline exists, and correctly deferring to a real dated count once one supersedes it. Zero such rows exist in the current dev database (confirmed by direct query) — a latent, not active, corruption risk, closed before it could bite a real deployment. **One real consistency gap closed**: purchases already carried an informational `purchase_excluded_from_current_stock` import-summary warning when a row predates the last stock count; sales/returns never did, despite the identical situation. `_write_sales` (`app/imports/importer.py`) now shows the same warning (`sale_excluded_from_current_stock`), reusing `InventoryMovementRepository.list_latest_adjustment_event_dates` — which itself needed the identical `resulting_quantity_on_hand IS NOT NULL` filter as the fix above, or the warning could fire inconsistently with what `sum_by_product_ids` actually excludes. No frontend change needed — the warning-rendering UI is already generic per warning code. **Tests**: 22 new (`backend/tests/integration/test_sales_backdating_stock_integrity.py`, new — the prompt's full 12-item calculation matrix as full-pipeline `run_import`/`undo_import` tests against fixed dates: same-day/pre-count/post-count sales, mixed multi-line orders, return symmetry, multiple reconciliations, out-of-order upload, both undo directions, no-baseline arithmetic, sale/purchase warning parity, tenant isolation, and downstream regression proving `list_product_thresholds`/`get_retail_operations`/`global_search` all read the identical corrected number — plus 3 new direct repository tests in `test_date_aware_stock.py` for the legacy-adjustment fix specifically). 864 backend tests passing (up from 843), clean frontend build (no frontend files changed). **Manual walkthrough — real, not backend-tests-only**: executed via `docker compose exec backend python` directly against the real dev Postgres and real R2 (identical `Upload`/`ImportRecord`/`ImportMappingProfile` writes and `r2_client` object PUT the browser upload flow itself produces — this Browser tool has no native file-picker capability, disclosed plainly rather than claimed), then visually reconfirmed through the real authenticated session on `password_change_test` (the only business that session had access to — Test Bike Shop's identical script-based check was run first, then fully undone and cleaned up once this was discovered): a stock count of 40 (2026-01-10), a backdated sale of 3 (2026-01-09) correctly left stock at 40 while the transaction appeared in full on the Transactions page with the new exclusion warning, a next-day sale of 5 (2026-01-11) correctly dropped stock to 35 exactly once, and undoing all three imports in sequence correctly walked stock back 35 → 40 → 0 — every number confirmed both in the database and live on the Product Reorder Rules page. **Re-confirmed the same day, direct request, once the real owner session was available**: the identical script-based sequence was re-run against the real Test Bike Shop business itself (not just `password_change_test`), this time with full UI access — the count (40), the backdated sale (excluded, visible on Transactions and via the global search bar with its real order value), the next-day sale (35, exactly once), and the full undo sequence (35 → 40 → 0) were every one confirmed live in the actual owner-facing UI, not just the database. Test artifacts (the one placeholder product, its zero alert rows) cleaned up afterward in both businesses — no residue left in either. |
| 1.69 | 13/08/2026 | Closed the password-reset session-revocation gap disclosed since the Notifications/Security/Retention round, direct request via prompt file (`ORLA_Security_Account_Supabase_Gaps_Prompt.md`) — without the `SUPABASE_SERVICE_ROLE_KEY`/Admin API fallback the original gap assumed was required. **Investigated first, per the prompt's own explicit instruction not to assume Auth Hooks are a post-event webhook and not to build a service-role fallback before checking provider behavior**: researched Supabase's documented client SDK and found `auth.signOut({ scope: "others" })` — revokes every *other* session's refresh token using the account's own already-authenticated session, no elevated credential needed. `frontend/app/reset-password/page.tsx` now calls it immediately after `updateUser({ password })` succeeds, best-effort (a failure here is logged, not blocking, since the password itself is already saved). No backend code, no new secret, no new route — a pure frontend change. Full reasoning, alternatives considered (including why the service-role fallback was rejected as unnecessary), and what this does/doesn't prove recorded in new `docs/decisions/ADR-024-supabase-session-security.md`, registered in `docs/governance/12_Decision_Register.md` (v1.10). `backend/app/security/auth.py`'s own disclosure comment updated: this gap moves from "not built, needs a secret that doesn't exist" to "closed." **Also produced, not code**: new `docs/governance/20_Supabase_Security_Runbook.md` — the Supabase dashboard configuration this still needs (enabling Password Changed/Email Address Changed security notification emails, confirming Secure Email Change stays on, confirming the redirect-URL allowlist, locating and recording Auth Audit Log retention/reviewer) and the exact numbered two-real-browser-session test script that proves session revocation end-to-end. **Honestly not done, and why**: the empirical two-session proof itself (sign in on two devices, reset on one, confirm the other is locked out) needs an existing account's real password entered into two real login forms — this environment's own standing rule against an agent entering authentication credentials applies here exactly as it has every time a two-role walkthrough came up in this project's history (v1.62–v1.66), and no disposable account exists to substitute. The runbook leaves this as a fill-in-the-blanks checklist for whoever has real credentials to run once. Supabase dashboard settings (security notification toggles, redirect URL confirmation) are equally out of reach — confirmed no available tool, including the connected Supabase MCP server, exposes an auth-config-editing call, only read-only/project-management ones. 843 backend tests still passing (docstring-only backend touch, no logic changed), clean frontend build (`/reset-password` unchanged in size/shape). **Deliberately not built this round, per the prompt's own explicit scope**: the Section 5 "centralized ORLA security timeline" — correctly left undesigned, since no supported, authenticated, post-event delivery mechanism from Supabase was confirmed to exist on this project's plan; Supabase's own Auth Audit Logs remain the audit surface, exactly as the prompt's own fallback instruction says to do when that's the case. |
| 1.68 | 11/08/2026 | Two rounds of direct changes made by the founder outside this session, recorded here for the changelog's own sake — not built by ORLA/Claude, verified afterward rather than authored. **Commit `2a5a777` ("Fixed bug in PDF downloads")**: in substance a full rewrite of both report exporters, not a bug fix to the existing table-only output. `backend/app/exports/pdf.py` now renders real reportlab charts matching what the web report page shows — a `VerticalBarChart` for category/top-seller breakdowns and a `HorizontalLineChart` with a low/high confidence band for the forecast section — on top of a redesigned visual layout (a consistent ink/teal/blue/amber/red palette, shaded metric tables, a `_BUSINESS_WIDE_FINDING_TYPES` split mirroring the frontend's own `splitRecommendations` grouping). `backend/app/exports/docx.py` was similarly rebuilt: shaded header/zebra-striped tables, Arial styling with a matching heading palette, a real footer, and full chart-data tables for every section so the DOCX (which can't carry a live chart the way the PDF can) still loses no information from the payload. `backend/app/exports/formatting.py` gained `format_rate` (a stored 0–1 ratio formatted the same way as the frontend's own `formatRate`) and switched the no-value placeholder from an em dash to a plain hyphen. A new `backend/tests/unit/test_report_exports.py` (335 lines) covers both renderers directly. **Commit `5c8ecb8` ("Refine ORLA application layout")**: the app's first real, deliberate design system, replacing the "deliberately plain, no styling framework" approach this prototype used until now. `frontend/app/globals.css` (103 → ~690 lines) introduces CSS custom-property design tokens (`--app-bg`/`--app-surface`/`--app-panel`/`--app-text`/`--app-muted`/`--app-border`, plus `--app-green`/`--app-blue`/`--app-amber`/`--app-red` status colors each with a `-soft` background variant), styled buttons/inputs/focus-visible states, and widens the page body from a fixed 640px to a responsive `min(1180px, calc(100% - 32px))`. Dark-mode support (previously `color-scheme: light dark`) was deliberately dropped to `light` only. `frontend/components/AppNav.tsx` and `frontend/components/GlobalSearchBar.tsx` were restyled to match (pill brand mark, incident banner, search dropdown). `frontend/app/notifications/page.tsx` (280 lines) was restructured from a flat top-to-bottom filter row into a sidebar-plus-content layout (a "Categories" button rail with unread-count badges, filters moved into a toolbar) — every piece of state and logic from the previous round (`categoryFilter`/`severityFilter`/`statusFilter`/`dateFilter`/custom range, `handleMarkAllRead`, pagination) was confirmed unchanged, restructured presentation only. `dashboard/page.tsx`, `onboarding/page.tsx`, `uploads/page.tsx`, and `products/page.tsx` received matching but smaller markup/className updates. **Verified afterward, not built here**: full backend suite still green at 843 passing (up from 838, the +5 being the new export-renderer tests), a clean `npm run build`, and a live check confirming the new design renders correctly on `/dashboard`, `/reports`, a report detail page's Download PDF/DOCX buttons, and `/login` — the last of these confirming the new color/button/input tokens apply globally (styled consistently even on pages `5c8ecb8` never touched, since they're base-element rules in `globals.css`, not page-scoped classes) while the bespoke structural layouts (sidebar navigation, toggle switches) exist only on the five pages the commit actually restructured. |
| 1.67 | 11/08/2026 | ORLA Notifications/Security/Retention batch, direct request via prompt file — the deferred Notification Centre work, without notification spam. **1. Data freshness, generalized**: `check_data_freshness`/`resolve_data_freshness` (`app/application/notifications.py`) rewritten to cover every `app/imports/aliases.py::SUPPORTED_ENTITY_TYPES` (sales/purchases/inventory/repairs), not sales alone. Sales stays unconditionally expected (unchanged 3-day cadence, byte-identical wording for the sales-only case — all 12 pre-existing tests pass unmodified); purchases/inventory/repairs are only ever checked once a business has actually used them (`ImportRecordRepository.latest_completed_by_entity_type` already only returns types with a real completed import — no schema change needed), with a looser 14-day cadence ("respect the expected upload cadence... do not assume every dataset must arrive daily"). Multiple stale datasets consolidate into **one** notification, never one per type. **Live-found real bug during final verification, fixed**: the dedup_key rename (`data_freshness:{id}:sales` → `data_freshness:{id}`) orphaned any notification already open under the old per-entity key — the next tick created a second, duplicate row instead of updating the existing one, exactly the spam this feature exists to prevent. Fixed with a one-time, idempotent migration cleanup baked into `_reconcile_freshness` itself (dismisses any open legacy-key row on its very next run); regression test added; the live duplicate this surfaced against the real dev database was confirmed gone after the fix. **2. Notification pagination + date filters**: `NotificationRepository.list_for_business` now returns `(items, total)` with real `limit`/`offset` (matching the Transactions drill-down's own pagination style), plus Today/7d/30d/custom-range filtering (`resolve_notification_date_range`, reusing `app/analytics/period.py::resolve_period`, with 422s for reversed/excessive/malformed ranges). `unread_count` stays deliberately unfiltered by the request's own date/category/severity — a "Today" view never makes a real unread backlog look like it vanished. Frontend: Previous/Next controls, a Date dropdown + custom-range inputs, all wired into `frontend/app/notifications/page.tsx`. **3. Consolidated Monday stock review**: new `app/analytics/stock_review.py` (out-of-stock/stale/excess counts, reusing `app/analytics/retail.py`'s existing `classify_movers`/`find_dead_stock` — "stale" = dead stock or a known slow mover; "excess" = cover days ≥3× a product's own effective reorder threshold, only ever evaluated with real sales evidence behind it) + `app/application/stock_review.py` orchestration. One notification per business/branch, concise counts only (never a product list), linking to a new `?stock_filter=out_of_stock\|stale\|excess` deep link on the Product Reorder Rules page (client-side filter reusing fields the endpoint already returns — verified live). **4. Weekly Business Performance**: `notify_weekly_business_performance` reuses the weekly report's own already-computed `financial_performance.revenue` (current/previous/change_pct) — no second calculation. Requires ≥4 weeks of real sales history (`SaleRepository.earliest_sale_date`, new) and a ≥5% materiality threshold before claiming a trend; otherwise stays silent (the existing, unconditional `report_ready` notification already covers "report's ready," never duplicated). Both #3 and #4 fire from the exact same Monday weekly-report-success point in `app/scheduler/tick.py` — this codebase has no separate "stock processing" job the prompt's own wording assumes, so the report's own already-idempotent-per-period Monday trigger is the one real once-a-week hook, reused rather than a second scheduling mechanism invented. **5. ORLA System status** — no new incident-tracking table: `Notification`'s own `dedup_key`/status/`update_and_reopen` already is one. Upload-failed/upload-partial/report-failed already existed; two new signals added: `notify_report_delayed` (a report still incomplete >4h past its own due generation moment, `app/analytics/period.py::report_generation_moment`, new — doubles as "stuck beyond a timeout," the one real retryable job in this codebase) and `notify_ai_insights_unavailable` (platform-wide: the most recent 5+ AI provider calls *across every business* all failed — a single business's own bad call is never mistaken for an outage; `AIRequestRepository.recent_platform_wide_success_flags`, new). Both silently clear on resolution — no routine "everything's fine" spam. New `GET .../notifications/system-status` route + `AppNav` banner (polled the same way as the unread badge) shows the most-recent active incident everywhere in the app, Notification Centre stays the full history regardless. **6. Security & Account** — inspected and documented the actual auth architecture directly in `app/security/auth.py`: ORLA delegates 100% of authentication (sign-up/in, Google OAuth, password reset) to Supabase Auth — no password/token/session ever stored here. The existing forgot-password/reset-password/login pages already satisfy nearly every literal requirement (generic non-enumerating response, Supabase-owned secure single-use token, expiry, rate limiting) via that delegation, not something to rebuild in parallel. Employee role-changed/added/activated/payment-failed/removed notifications (owner-only `security_account`/`team` categories) already existed and are already tested from earlier rounds — confirmed sufficient, no gap. Genuinely, honestly **not built, and why**: revoking other sessions after a reset needs a `SUPABASE_SERVICE_ROLE_KEY` that doesn't exist anywhere in this deployment; a password/email-changed audit trail needs Supabase Auth Hooks, a dashboard-only setting outside API access — same category of gap as the Stripe webhook secret disclosed earlier in this project's history. Failed-sign-in/lockout, new-device sign-in, and MFA are all explicitly conditional in the prompt on data/capability that doesn't exist in this codebase, so correctly left unbuilt rather than guessed at; no owner-transfer feature exists to notify about either. **7. Report downloads, for real**: new `app/exports/pdf.py` (reportlab) and `app/exports/docx.py` (python-docx) — genuine, openable PDF/DOCX files (not the old browser-print button, removed), rendering straight from `Report.payload` (no new calculation). New `GET .../reports/{id}/download.pdf` and `.../download.docx` routes reuse the exact same membership/status/expiry availability check as the existing JSON route — permission is enforced at the route, never only a hidden frontend button (verified with a dedicated non-member-403 test). Filenames sanitized against header-injection/path-traversal (`_safe_filename_component`). Frontend: `apiGetBlob` (new, `lib/api/client.ts`) + two independent-state Download PDF/DOCX buttons on the report page, verified live end to end (real 2.6KB PDF, real 37KB DOCX, both downloaded successfully against a real generated report). **8. UI/UX sweep**: reviewed every new control added this round — plain semantic `<select>`/`<button>`/`<input>` throughout (no suppressed focus outlines anywhere in the codebase), `role="status"` on the new incident banner, loading/error/retry states on every new async control, `?business=` preserved on every new link. **Tests**: 838 backend tests passing (up from 815 at the start of this round — freshness +8, pagination/date +16, stock review +24 combined with weekly performance, system status +15, report downloads +7, plus the live-found freshness-migration regression test), clean frontend build throughout. **Verified live** against the real dev Postgres: the freshness-duplicate bug found and fixed as described above; the Product Reorder Rules `?stock_filter=stale` deep link correctly pre-filtered to real stale products; both report download buttons succeeded against a real generated weekly report; the system-status banner correctly rendered nothing with no active incident. Weekly-performance/stock-review notifications could not be triggered end-to-end live without forcing a regeneration of an already-existing real weekly report (idempotency correctly prevented a duplicate) — covered instead by 24 dedicated automated tests at the application layer. **Deliberately not implementation scope, per the prompt's own explicit instruction**: section 11's data retention/deletion policy — produced instead as a separate, non-code evidence-based inventory and proposal document (`docs/governance/19_Data_Retention_Proposal.md`), covering every storage location the prompt named (R2 uploads, deleted users/employee seats/sessions, deleted businesses, the operational ledger, reports, notifications, audit logs, billing/Stripe, email/telemetry logs, and backups/replicas explicitly flagged as outside this codebase's visibility), with every suggested window (72h/2y/2w) explicitly labelled a hypothesis needing legal/accounting validation, not a decision. No deletion job, TTL rule, or destructive migration was added anywhere. |
| 1.66 | 11/08/2026 | Notification Centre staff filter polish + new global search bar, direct request via prompt file. **Part 1 — role-aware category filter**: `frontend/app/notifications/page.tsx`'s category dropdown showed all 8 categories to every role, including the 4 owner-only ones (Team/Billing/Branches/Security & Account) — the backend already correctly blocked those rows (v1.62), but the filter UI itself didn't know that. Non-owners now see only the 4 operational categories (Stock/Data & Uploads/Reports/ORLA Insights); an owner-only category somehow already selected resets to "All categories" rather than silently filtering on a category the account can never actually see a row for. Intro copy is now also role-aware (owner version still mentions team/billing, staff/manager version only names the operational categories). **Part 2 — global search bar (new)**: `GET /businesses/{business_id}/search?q=&limit=` (`app/api/search.py`/`app/application/search.py`/`app/schemas/search.py`), grouped by Products/Sales/Purchases/Suppliers/Repairs, tenant-scoped via `get_current_membership` exactly like every other route — `membership.business_id` is the only business ever touched, so a branch can never search a sibling's or the parent's data. Products reuses `ProductRepository.search_by_name_or_sku` verbatim (already built for ORLA chat, already the right OR-based shape); Sales/Purchases/Suppliers/Repairs each get a new, narrow, purpose-built OR-across-fields search method (`search_sales`/`search_purchases`/`search_by_name`/`search_repairs`) rather than reusing the existing ORLA-chat lookup methods or the Transactions page's paginated methods, both of which use AND-per-named-filter semantics wrong for a free-text box — building fresh avoided any risk of regressing either. Repairs group is included only for `template == "bicycle_shop"` businesses, the same gate `app/application/report.py` already uses for Workshop Performance. No customer PII anywhere in a result: `Sale`/`ProductionEvent` carry no customer name/email, and `customer_id` is never selected. Queries under 2 characters return empty groups, not a 422 — a search box shouldn't error just because someone's typed one letter. Every query is parameterized ILIKE through SQLAlchemy, no raw SQL string interpolation anywhere. Frontend: `frontend/lib/hooks/useGlobalSearch.ts` (300ms debounce, a request-sequence guard so a slower earlier response can't clobber a faster later one — the classic race a debounce alone doesn't prevent) and `frontend/components/GlobalSearchBar.tsx`, wired into `AppNav` next to the other top-nav links, same `businessId &&` gate as everything else business-scoped there. Grouped dropdown with loading/error/no-results states, full keyboard support (↑/↓ moves a single flattened, ordered list spanning all five groups; Enter opens the highlighted result, defaulting to the first if none highlighted; Escape closes and blurs), every result linking to the best existing page (Products→Thresholds, Sales/Purchases/Repairs→Transactions pre-filtered by type and product where known, Suppliers→Suppliers) with `?business=<id>` preserved. **Real bug found and fixed during live verification, not in code touched by this feature**: the search bar's result links first used `next/navigation`'s `router.push` (a client-side transition) — live testing showed `frontend/app/transactions/page.tsx` then rendered its default Sales tab instead of the requested type, because that page reads `window.location.search` once at initial render and never reacts to a client-side URL change; a hard reload of the identical URL worked correctly, confirming the query-parsing logic itself was fine. Rather than reach into that unrelated page, `GlobalSearchBar` was changed to navigate via `window.location.href` instead — which also matches `AppNav`'s own convention: every other cross-page link in this frontend is already a plain `<a href>`, never a client-side transition, so this was a consistency fix as much as a bug fix. Re-verified live afterward: Enter on a repair result now correctly lands on the Repairs tab with the right columns. 16 new backend tests (`tests/integration/test_search_service.py`, `tests/tenant_isolation/test_search_isolation.py`): product name/SKU match, sale order-reference match, purchase-reference match, supplier-name match, repair reference/description match, no customer PII in any result row, per-group limit enforcement, sub-minimum query returns empty not an error, repairs omitted for a non-bicycle-shop template, cross-tenant 403, non-member 403, and a staff member assigned to one branch getting 403 on a sibling branch's search. 767 backend tests passing (up from 751), clean frontend build. **Verified live** against the real dev Postgres, including with a genuine non-owner account (this session's own test account holds "manager," not "owner," on Test Bike Shop — no account had to be created to get non-owner coverage): the category filter correctly showed only the 4 operational categories with the staff-facing intro copy; the search bar's debounce/grouped-results/no-results/keyboard-navigation/highlight all worked against real data (a "Tyre" query correctly matched 5 real repair rows by description). **Not built this round**: a genuine owner-vs-non-owner *comparison* walkthrough on the same account, since only one real account was available to this session — covered by the tests above plus the fact that the live account observed was itself already a non-owner, which is the harder-to-fake case. |
| 1.65 | 11/08/2026 | Fixed the previous round's own navigation-placement mistake, direct request via prompt file: the missing operational links (Dashboard, Upload data, Reports, Ask ORLA, Thresholds, Suppliers, Transactions, Notifications) got added under every Company Profile business/branch row instead of the top nav — duplicate, increasingly messy navigation the moment branches exist, not what was asked. **Reverted and fixed properly**: those links are removed from business rows entirely (`frontend/app/onboarding/page.tsx`) — rows now show only name/template/role/status, "View profile" (still the one exception, since it's about the Company Profile record itself, not a general app section), and owner-only controls. `AppNav` (`frontend/components/AppNav.tsx`) goes back to one `businessId` prop driving every link, same as every other page — the `notificationsBusinessId` split from two rounds ago is gone, no longer needed. Company Profile resolves and now passes a real `businessId` into the shared top nav: the `?business=` query param when it names one of the caller's own active businesses, else their first active one — and a new small "Business context for navigation" `<select>` (options limited to `businesses`, which `GET /businesses` already scopes to the caller's own memberships, so staff can never select a sibling branch they don't belong to) lets either role switch it without losing the pick on unrelated list updates elsewhere on the same page (add/delete a branch), fixed via a functional `setNavBusinessId` update that preserves an already-valid selection instead of resetting on every `businesses` change. **Branch-scoped access, verified not just asserted**: 2 new tests prove a staff member assigned to Branch A's employee-seats gets a real 200 on Branch A's own analytics route, a 403 on sibling Branch B's (same parent, different Membership — branches were already just separate `Business` rows with independent membership, so this needed no backend code change, only proof) and a 403 on the parent shop's; their own `GET /businesses` list contains only Branch A, never the sibling or the parent, even though the same owner can see and access all three. **Notification Centre recheck**: no code changes needed, full suite still passes. 2 new backend tests, 751 backend tests passing (up from 749), clean frontend build. **Verified live**: top nav now shows all 8 operational links plus the unread badge when Company Profile has a resolved business, business rows are clean (no repeated links), the new selector immediately re-scopes every top-nav link (confirmed switching Test Bike Shop → Galway updates all 8 hrefs and the badge in place). **Not built this round**: the staff side of the two-role browser walkthrough — same constraint as every prior round, creating a second account (even disposable) falls under this environment's policy against creating accounts or entering authentication passwords; covered instead by the 2 new tests plus the 19 from the last two rounds. |
| 1.64 | 11/08/2026 | Company Profile permissions, staff self-profile, and missing operational links, direct request via prompt file. **Owner-only company/branch profile**: `PATCH /businesses/{id}` now 403s for any non-owner (`app/api/businesses.py`) — company/branch contact/location data was previously editable by any member, which contradicted the actual product rule ("only owners can change anything related to the company or branches"). Checked before any audit write, so a rejected staff attempt leaves no trace. `frontend/app/onboarding/[id]/page.tsx` now renders a read-only `<dl>` of the same fields for a non-owner instead of the editable form — no more Save button that would just 403. **Staff self-profile (new)**: `GET/PATCH /businesses/{business_id}/me` resolves the caller's own `EmployeeSeat` via `(business_id, user_id)` — a `SelfProfileUpdate` schema with only `first_name`/`surname`/address fields (no `role`, `status`, or `email` field exists on it at all, so there's nothing for a client to even send that the route would honor). An owner has no `EmployeeSeat` row and gets a clear 404, not a confusing empty form — their own "profile" is the Business record's manager fields, still edited through the now-owner-only PATCH above. Company Profile gained a per-business "Edit my profile" action for non-owner rows, using this new route — first-ever way for staff to edit their own name/address in this app. **Missing operational links (new)**: every business row on `/onboarding` now links to Dashboard, Upload data, Reports, Ask ORLA, Thresholds, Suppliers, Transactions, Notifications, and View profile — previously only Upload data/View profile/Notifications existed, so staff (and the owner) had no direct path from Company Profile to five of the platform's core tools. Same row for every role — "do not hide operational tools from staff" is the explicit policy, and this list was already scoped out for deleted/archived businesses (that branch of the row never renders any actions at all, existing behavior, untouched). **Notification Centre recheck**: no code changes needed — re-ran the full isolation/freshness suite and it still passes exactly as designed from the last two rounds (staff sees operational categories, not owner-only ones; freshness still escalates/resolves/dedupes correctly). 6 new backend tests: a genuine same-business staff member (not a stranger) rejected from the company-profile PATCH with no audit entry; staff self-profile fetch/edit persists correctly; a self-edit never touches a different employee's row (structurally impossible — there's no seat id anywhere in the request to redirect it with); role/status/email sent in a self-profile request body are silently ignored, real `Membership.role` unchanged; an owner gets 404 from `/me`. 749 backend tests passing (up from 743), clean frontend build. **Verified live**: re-checked the real owner account — all 8 operational links render per business row with correct `?business=` context, the owner-only buttons and the full editable company-profile form still render exactly as before, no console errors. **Not built this round**: a genuine second staff account for the two-role browser walkthrough this prompt asked for — creating an account, even a disposable test one, falls under this environment's own policy against creating accounts or entering authentication passwords, so the staff side stays covered by the 6 new automated tests above (plus last round's 13) rather than an eyes-on session; no frontend test framework exists in this repo either, per this prompt's own fallback instruction to document that clearly when true. |
| 1.63 | 11/08/2026 | Fixed a real bug found in the previous round's own rescan, direct request via prompt file: logged in as staff, the Notifications link disappeared entirely from Company Profile. Root cause: `/onboarding` has no single "selected" business (unlike `/dashboard`/`/products`), so it always called `<AppNav />` with no `businessId` — and `AppNav`'s Notifications item (and unread badge) only ever rendered when `businessId` was set, for every role, not just staff. Decoupled the fix from `businessId` itself rather than just passing one in: `AppNav` gained a separate `notificationsBusinessId` prop (falling back to `businessId` everywhere else, so no other page's behavior changes) so Company Profile can supply a real business for the Notifications link without also surfacing Thresholds/Suppliers/Transactions there, which stay correctly hidden (no single business is "selected" on that page — those links would be misleading). `/onboarding` now resolves a business for the nav link itself: the `?business=` query param when it names one of the user's own active (non-deleted) businesses, else their first active one, computed once the business list has loaded. The existing per-business "Notifications" row link (added last round) is untouched. Frontend-only change (`frontend/components/AppNav.tsx`, `frontend/app/onboarding/page.tsx`) — no backend code touched, 743 backend tests unchanged, clean frontend build. **Verified live** against the real dev Postgres, both branches of the query-param logic: with no `?business=` param, the nav Notifications link correctly targeted the user's first active business (Test Bike Shop) and Thresholds/Suppliers/Transactions correctly stayed hidden; with `?business=<Galway's-id>`, the nav link correctly switched to Galway, showed its real unread badge "(1)", and clicking through landed on Galway's own notifications — including the real `branch_data_missing` freshness notification, now escalated to `data_outdated`/warning wording exactly as designed (re-confirming last round's escalation logic against real elapsed time). Re-ran the scheduler tick against the real data a second time and confirmed still exactly one notification for Galway — no duplicate created. Owner-only Company Profile controls (Manage billing, Add a branch, Add employee, Edit/Delete, Delete this shop) re-verified unaffected. Session persistence re-confirmed via a real page navigation staying signed in. **Not built this round**: a genuine second staff account for a live two-role walkthrough — creating an account (even a disposable test one) falls under this environment's own "never create accounts or enter authentication passwords" policy, so the staff side of the walkthrough stays covered by last round's 13 automated owner/staff-visibility tests (unchanged, still passing) rather than a fresh eyes-on session. |
| 1.62 | 11/08/2026 | Notification Centre owner/staff permissions batch, direct request via prompt file. **Notification visibility**: the five employee/team `notify_employee_*` functions (`app/application/notifications.py`) — added, activated, payment failed, removed, role changed — now pass `visible_to_role="owner"`, closing the actual gap the previous rounds left open (billing/branches were already owner-only; stock/data_uploads/reports/orla_insights stay unrestricted since staff need those for daily work, per the prompt's own explicit policy). **Billing API**: `POST .../billing/checkout-session` and `.../portal-session` now 403 for any non-owner (`app/api/billing.py`) — previously any role with a Membership could start a real Stripe Checkout/Portal session for the business, a genuine gap. `GET .../billing/subscription` deliberately left open to any role after checking its callers: `AppNav` and the onboarding list both read it to gate/label "Upload data" for every member regardless of role, and its response (`status`, `current_period_end`) carries no sensitive Stripe identifiers. **Company Profile UI**: "Manage billing"/"Subscribe"/"Complete payment", "+ Add a branch", and "Delete this shop" are now gated on `b.role === "owner"` (`frontend/app/onboarding/page.tsx`) — Add/Edit/Delete employee were already correctly gated from earlier rounds. Backend enforcement (above, plus the already-existing branch/delete-business/employee-seat 403s) is the real boundary; this closes the "staff sees a live-looking button that just 403s" dead-end the prompt flagged. 13 new backend tests: a genuine same-business manager (not just a cross-tenant stranger) rejected from checkout-session/portal-session/employee-seat-list; the full owner/staff notification-visibility policy matrix (staff sees all four operational categories, sees none of the four owner-only ones, unread count excludes owner-only rows, can't dismiss an owner-only row by guessed id even though the owner still can) — 743 backend tests passing (up from 736), clean frontend build. **Verified live**: re-checked the real owner account after these changes — every owner-only button (Manage billing, Add a branch, Add employee, Edit/Delete, Delete this shop) still renders and the Notification Centre still shows the real owner-visible rows exactly as before, confirming the new gating didn't regress the owner's own access. **Not live-verified**: the staff side of the two-account walkthrough — no second real login was available this round, so "staff sees operational notifications but not owner-only ones," "staff doesn't see owner-only buttons," and "staff can't call owner-only endpoints" are covered by the 13 new automated tests above (which exercise the identical HTTP calls the UI makes) rather than an eyes-on browser session. |
| 1.61 | 11/08/2026 | Notification Centre first batch, direct request via prompt file: data freshness notifications (must-have), a second authenticated UI walkthrough, session-persistence verification, backend filter validation, and the missing Company Profile Notifications link. **Data freshness (new)**: `app/application/notifications.py::check_data_freshness`, called once per active-subscription business on every scheduler tick (`app/scheduler/tick.py`) — reuses the already-existing, already-tested `ImportRecordRepository.latest_completed_by_entity_type` (the same query `/uploads/freshness` already serves), no new calculation. Scoped to `sales` only — the one entity every business template genuinely has, matching the prompt's own suggested copy. Three outcomes on one dedup-keyed row (`data_freshness:{business_id}:sales`), escalating in place as days pass rather than spamming new rows: `no_new_data_detected` (info — never uploaded, or not yet today), `data_outdated` (warning — 3+ days stale), `branch_data_missing` (a branch's own version of either, naming the branch). Resolves immediately on a successful sales import (`run_import`), not just on the next tick. Deliberately gated to businesses with an *active* subscription — a lapsed or never-subscribed business can't even upload (already blocked by `require_active_subscription`), so nudging it would be noise, not a reminder. `NotificationRepository.update_and_reopen` extended to also update `type_key` on reopen, since an escalating freshness notification is a genuine reclassification, not just new wording. **Walkthrough (clean this round)**: `/notifications` loads for the authenticated owner, unauthenticated access redirects to `/login`, category/severity/status filters all work, mark read/mark all read/dismiss all work, the nav badge updates immediately (the same-tab event bus shipped in the previous commit, `00c5595`, not re-documented there — noted here instead), mobile layout is clean. Owner-only billing/branch visibility re-confirmed via the existing automated tenant-isolation tests (a live two-account check wasn't possible — only one real login was available). **Session persistence**: confirmed already correct by default — the Supabase browser client (`frontend/lib/supabase/client.ts`) takes no `auth` override, so `persistSession`/`autoRefreshToken`/`localStorage` all use supabase-js's own defaults; verified live via a real page refresh staying signed in. No code change needed. Google re-auth's `prompt: "select_account"` (v1.55) confirmed still present on both login and signup. **Filter validation (new)**: `category`/`status`/`severity` query params are now `Literal[...]` types (`app/schemas/notification.py`) instead of bare `str`, so an invalid value 422s automatically via FastAPI/Pydantic rather than silently matching zero rows — frontend's own filter `<select>` options were already exactly these values, no drift to fix. **Company Profile link (new)**: a "Notifications" link added next to each business's own "View profile" on `/onboarding`'s per-business list (`?business=<id>` preserved) — `AppNav` renders no business-scoped links on this page at all (no single selected business), so this was a genuine gap, not a duplicate. 20 new backend tests (freshness never-uploaded/outdated/resolves-on-import/no-duplicate-on-rerun/escalates-in-place/branch-scoping/tenant-isolation, tick's active-subscription gating, invalid-filter 422s) — 736 backend tests passing (up from 724), clean frontend build. **Verified live** against the real dev Postgres: ran the scheduler tick for real, confirmed correct notifications for 3 real businesses (a standalone shop's generic nudge, a branch's own named-branch wording, both worded exactly as designed) — left in place, real state. |
| 1.60 | 11/08/2026 | Built the ORLA Notification Centre, direct request via prompt file, prefixed by a short implementation roadmap as required. New `notifications` table (`business_id`, `category`, `type_key`, `severity`, `title`, `body`, `action_label`/`action_url`, `related_entity_type`/`id`, `status` unread/read/dismissed, `visible_to_role`, `dedup_key`, `read_at`) — deliberately separate from `Alert` (low-stock only, no read state, no UI, unchanged) and from `AuditLog` (compliance/security trail, never a customer-facing action prompt), per the prompt's own "use audit logs for compliance, notifications for action prompts" distinction. New `app/application/notifications.py::notify()`: idempotent by `dedup_key` — a re-triggered condition (e.g. the low-stock count changing on a later import) updates the existing open row in place instead of spamming a duplicate, and reopens a previously-read row to unread since the underlying situation genuinely changed; a dismissed row is never reopened, a fresh one is created instead. Wired into six of the prompt's eleven suggested categories, each a real deterministic trigger at an already-existing, already-tested hook point — no new calculation, no AI, matching the prompt's own "implement the highest-value triggers first" escape hatch: **Stock** — a grouped whole-business low-stock summary (`refresh_low_stock_alerts`, reusing the same active-alert count `app/application/report.py` already reads, not just the products one import touched, so the number is always the real total). **Data & Uploads** — import completed/partial/failed (`run_import`, using the rejection summary it already computes). **Reports** — report ready / permanently failed (`app/scheduler/tick.py`, only notifying "failed" once retries are exhausted, never on a transient retry still under the cap). **ORLA Insights** — "ORLA found N opportunities," reusing the exact same report's own already-computed `findings.recommendations` count, zero new calculation. **Team** — employee added/activated/payment failed/removed (`app/application/employee_seats.py`, the billing webhook's `_apply_employee_seat_event`) plus a **Security & Account** role-changed notification (comparing old vs new role in `update_employee_profile`). **Billing/Branches** — business subscription status transitions (`app/billing/service.py`'s Stripe webhook, capturing the pre-upsert status to detect a real transition, not every webhook delivery) — a branch is just a second `Business` row on the same mechanism (`parent_business_id` set), so both categories share one hook, split only by which wording/category applies; owner-only visibility, enforced server-side in `NotificationRepository`'s list query, never trusted from the client. **Deliberately deferred, stated not built, no fake notifications**: Business Performance (revenue/sales-trend comparison — genuinely new comparative-analytics logic, not a reuse); Stock sub-types beyond low-stock (stock-out prediction, stale/excess stock, restock recommendation — same reason); Security & Account beyond role-change (email/password change happens Supabase-side, outside this backend's visibility); ORLA System status (no health-monitoring infra exists to report on). New `/notifications` frontend page: category/severity/status filters, mark-read/mark-all-read/dismiss, action links back to the relevant page, empty/loading/error states — plus an AppNav bell-style "Notifications (N)" link with a 60-second-polled unread badge (no websocket/SSE infra exists in this codebase to do better). 25 new backend tests (dedup/reopen/dismiss-then-recreate semantics, each trigger's exact wording/severity, tenant isolation, owner-only billing/branches visibility, mark-read/mark-all-read/dismiss, category filtering) — 724 backend tests passing (up from 703), clean frontend build (new `/notifications` route). **Verified live** against the real dev Postgres: ran the low-stock trigger for Test Bike Shop's real data and confirmed a correctly-worded, correctly-severity notification was created (3 products genuinely below their reorder point, matching the Product Reorder Rules table exactly) — left in place since it reflects real state, not a throwaway smoke-test row. Browser-based UI verification wasn't possible this round (no authenticated session available in this environment, a previously-stated limitation) — relied on the live backend check above plus the passing tenant-isolation tests, which exercise the identical HTTP calls the UI makes. |
| 1.59 | 10/08/2026 | Two gaps found in v1.58's "Product Reorder Rules" table, fixed, direct request via prompt file. **Actions-wording fix**: the manual-edit Save button and the ORLA-accept button both read as variants of "Accept," despite doing different things — a typed-in custom value showed `Accept: set reorder point to 7 days`, indistinguishable from actually accepting ORLA's own recommendation. Manual edits now say `Save reorder point`; accepting a recommendation now says `Use ORLA's 7d recommendation` — always ORLA's own suggested value, never a typed-in number, so the wording only ever means "adopt ORLA's suggestion." **Kept "days" deliberately**, against the prompt's own suggested default of dropping it: `Product.low_stock_threshold_days` genuinely stores a stock-cover day-count (Stage C12), not a unit count, so removing "days" would have made the number's unit ambiguous — the prompt's own escape hatch ("unless the underlying model truly stores days") applied here. **New "Recommended Restock" section**: lists every product currently below its own reorder point (reusing the exact `cover_days < effective_threshold_days` condition the table's existing insights already used, now also respecting the category filter), each with a recommended reorder quantity. Deliberately reused Stage C13's existing forecast engine (`suggested_reorder_quantity`, a real units-based figure already computed and tested) rather than a naive `reorder_point − current_stock` formula, which would have subtracted a unit count from a day count — dimensionally invalid. Backend needed zero new code: this is a pure frontend join (by `product_id`) between the already-existing, already-tenant-scoped `GET .../products/thresholds` and `GET .../analytics/forecast?horizon_days=14` routes. The forecast fetch is deliberately independent of the main table's data fetch (its own `.catch()`) so a forecast-specific failure degrades Restock rows to a generic "needs restocking" message rather than blocking the table itself. Empty state: "No products are below their reorder point." Frontend-only change (`frontend/app/products/page.tsx`) — no migration, no new backend route, no schema change. 703 backend tests unchanged (none touched), clean frontend build. No frontend test framework exists in this repo to add automated coverage for the button-label/Restock changes — verified instead via a clean TypeScript build and a live read-only script against the real dev Postgres joining real threshold and forecast data (3 products below their reorder point, correct suggested quantities, no SQL errors). |
| 1.58 | 10/08/2026 | Reworked the low-stock threshold table into "Product Reorder Rules," direct request via prompt file — the previous `/products` table worked but read like an internal settings dump rather than something an owner could act on at a glance. Renamed the page/table and added a column glossary explaining what each of the six columns actually answers (In stock / Sold last 30 days / Stock cover / Reorder point / ORLA recommends / Setting) — explicit that "reorder point" is measured in **days of stock cover** (the same already-tested metric behind the low-stock alert system), not a raw unit count, so the number's unit is never ambiguous. Replaced the bare "(inherited)" wording with a real `Setting` column showing one of four owner-facing labels — "Category default," "Product custom," "ORLA recommended," or "System default" — genuinely sourced from the data model, not guessed: `Product.low_stock_threshold_source` is a new nullable column (`"manual"` | `"orla_recommended"`, migration `c9a1e5f3b7d2`), set alongside `low_stock_threshold_days` on every write path (`update_product_threshold` sets it from the existing `accepted_recommendation` flag; `recalculate_thresholds_after_upload` always sets `"orla_recommended"`) and cleared back to `None` whenever the override itself is cleared. `ProductThresholdOut`/`ProductThresholdRow` also gained `category_threshold_days` (the category's own override, if any — what actually distinguishes "Category default" from "System default"). Added a category filter dropdown (client-side, over the already-fetched full list — reuses the existing `GET .../product-categories` route, "All categories" default, correct empty state when a category has no products) and a deterministic insights list above the table ("N products are below their reorder point," "ORLA recommends raising reorder points for N fast-moving products," "N products need more sales history before ORLA can recommend a rule") — computed entirely from fields the backend already returns, no new calculation and no AI. The Accept-recommendation button now states the actual value it will apply ("Accept: set reorder point to 7 days") instead of a bare "Accept recommendation." New `formatDays` frontend helper strips a trailing `.0`/`.00` from whole-number day counts (`"7.00"` → `"7"`) without rounding a genuine fraction (`"7.5"` stays `"7.5"`) — fixes the float-display complaint without touching the underlying `DECIMAL(6,2)` storage or any money formatting, which stays exactly as-is. 5 new backend tests (threshold source set by each write path, cleared on removal, exposed correctly alongside a category's own override) plus one existing tenant-isolation test's response-shape assertion updated for the schema's new field — 703 backend tests passing (up from 701), clean frontend build. **Verified live** against the real dev Postgres (read-only): 180 real products, 4 currently below their own reorder point, 62 still needing more sales history — all computed correctly with no SQL errors. |
| 1.57 | 10/08/2026 | Three follow-up fixes to v1.56's supplier/threshold/drill-down round, direct request via prompt file. **Real bug fixed**: saving a low-stock threshold from the new `/products` page always showed "Could not save this threshold," even though the backend wrote the value correctly. Root cause: `PATCH .../products/{id}/threshold` returned bare `204 No Content` (copied from `update_product_threshold`'s original `-> None` return), but the shared frontend `apiPatch()` helper unconditionally calls `response.json()` on every response — which throws on an empty 204 body — and every *other* PATCH route in this codebase (`businesses.py`, `employee_seats.py`, `suppliers.py`, and this round's own `product-categories/{id}/threshold`) already returns `200 + the updated resource`; this route was the one outlier. Fixed by matching the established convention instead of special-casing `apiPatch` — `update_product_threshold` now returns the updated `Product`, and the route returns a new minimal `ProductThresholdSaveOut` (`product_id` + `low_stock_threshold_days`) with `200`. A tenant-isolation test that had asserted the buggy `204` (written in the same round that introduced the bug, so it never caught it) now asserts `200` + the real body — a genuine regression-test gap closed alongside the fix. Also added an explicit success/failure notice to the Save and Accept-recommendation actions on `/products`, which previously had no visible confirmation at all. **Suppliers section explanation**: a concise paragraph under the `/suppliers` heading explaining what supplier tracking is for (powers the spend breakdown, sharpens low-stock recommendations once a lead time is recorded, unknown supplier is fine) — the page previously had no framing at all beyond its own field labels. **Upload-triggered threshold recalculation**: new `app/application/products.py::recalculate_thresholds_after_upload`, called from `run_import` right alongside the existing `refresh_low_stock_alerts` call (same "only after the main commit succeeds, log-and-continue on failure, never block or roll back a successful import" posture). For every product an upload touched: skipped if it already has a manual `low_stock_threshold_days` override (an automatic recompute must never clobber a human's explicit choice) or if no supplier lead time is known yet (nothing new to apply — the existing default-fallback already covers it); otherwise its threshold is set straight to the freshly computed recommendation, so a product picks up a data-driven threshold the moment enough purchase/supplier data exists, without waiting for a manual "Accept recommendation" click. Deterministic (`recommend_low_stock_threshold`, no AI), idempotent (a second run against the same inputs finds nothing left to do, since the first run's write is now indistinguishable from a manual override), tenant-scoped, and isolated per business — new `threshold_recalculation_completed`/`threshold_recalculation_failed` audit events. No scheduler/cron was built, per the prompt's own explicit instruction — recalculation runs only on the upload hook, exactly as asked. Deliberately not wired into `undo_import` — undoing removes data, it doesn't add it, so it falls outside "every time new data is uploaded." 23 new/updated backend tests (the threshold save fix's exact response shape, upload-triggered recalculation's manual-override/no-lead-time/idempotent/tenant-scoped/no-touched-products cases, and one real `run_import`-level integration test proving the hook actually fires) — 701 backend tests passing (up from 694), clean frontend build. **Verified live** against the real dev Postgres: called the fixed `update_product_threshold` directly against a real product, confirmed it now returns the updated resource instead of `None`, then restored the original value and deleted the two smoke-test audit rows to leave the dev database clean. |
| 1.56 | 10/08/2026 | Implemented three of the four deferred product gaps from `docs/governance/18_Product_Gaps_Roadmap.md` (Gaps 1, 4, 5 — Gap 2's repair tax fix and Gap 3's out-of-scope decision already closed in v1.55), direct request via prompt file, prefixed by a short implementation roadmap as required. **Supplier tracking (Gap 4)**: `suppliers` (schema-only since the initial migration — this gives it its first writer) gains `normalized_name`/`status`/`merged_into_id`; new `product_suppliers` join table (`product_id`, `supplier_id`, optional `supplier_sku`/`lead_time_days`); `inventory_movements.supplier_id` nullable FK. New optional `supplier` canonical field on the purchases upload, match-or-create by normalized name (`SupplierMatcher`, exact mirror of the existing `CategoryMatcher`) — never required, never cross-business (matching is always scoped to the importing business's own active suppliers). New `app/api/suppliers.py`: list/create/edit (owner or manager), deactivate (soft, never hard-delete), merge (owner-only — reassigns every `inventory_movements`/`product_suppliers` row from the merged-away supplier to the survivor, deduplicating on `(product_id, supplier_id)` rather than violating the unique constraint, idempotent on a re-run), a manual product↔supplier correction route (the only path `lead_time_days` is ever set through — no upload file reliably states it), and a deterministic spend-by-supplier analytics endpoint (`Unknown` bucketed and surfaced as its own row, never silently dropped). New `/suppliers` frontend page (list/create/edit/merge/deactivate + the analytics table), plus a `supplier` field added to the purchases upload mapping form. **Low-stock threshold UI + deterministic recommendations (Gap 1)**: new `app/analytics/replenishment.py::recommend_low_stock_threshold` — a known supplier lead time plus a fixed 3-day safety buffer, falling back to the existing global default when no lead time is known; deliberately simple and legible rather than velocity-based, since a threshold is fundamentally a lead-time question, not a sales-speed one (existing stock-cover/units-sold figures already answer "how fast does this sell," reused unchanged as explanatory context). ORLA/AI never calculates this — the number is fully computed backend-side before any AI touches it, and the UI shows it with a plain-language "why" (`basis: supplier_lead_time | default_fallback`) rather than a separate AI round-trip, since the formula is already self-explanatory; no new AI endpoint was built for this. New `GET/PATCH .../products/{id}/threshold` (owner/manager; any role can view) and `PATCH .../product-categories/{id}/threshold` (new write path — `ProductCategoryRepository`/`ProductRepository` gain their first `low_stock_threshold_days` writers since the column was added at Stage C12), a distinct `threshold_recommendation_accepted` audit action from a manual `threshold_updated` edit. New `/products` frontend page: per-product current stock, recent sales context, current vs. recommended threshold, Edit/Accept recommendation actions, an explicit "not enough sales history yet" state. **Deliberately deferred, per the prompt's own escape hatch**: an automatic scheduled recompute job — the calculation surface (`recommend_low_stock_threshold`) is fully built and callable, but no new recurring job was added to apply it automatically; today it's recomputed live on every page load instead. **Dashboard raw transaction drill-down (Gap 5)**: new paginated, filtered listing methods (`SaleItemRepository.list_paginated_for_business`, `InventoryMovementRepository.list_purchases_paginated`, `ProductionEventRepository.list_repairs_paginated`) — deliberately separate from the existing ORLA-chat-facing lookup methods (fixed low limits, no offset), which stay untouched. New `GET .../transactions/{sales,purchases,repairs}` routes: every filter optional (date range, product, category — repairs have no category, a repair has no `product_id`), a hard server-side page-size cap (`MAX_PAGE_SIZE = 100`, enforced both by FastAPI's `Query(le=...)` and again inside the application function itself, so a direct call can't bypass it either), stable `(date, id)` ordering so pagination never skips or repeats a row. No new PII surface anywhere — the row shapes carry no customer name/email/phone, matching every other read path in this app. New `/transactions` frontend page (type switcher, date filters, pagination, a note when arriving pre-filtered from a dashboard click) plus drill-down links added from Dashboard's Top Sellers rows (per-product), Retail Operations' category filter (per-category), and Workshop Performance (repairs). **Verified live** against the real dev Postgres (not just the SQLite test suite) with a direct read-only script run against a business with real data — 180 products, 5,587 sale line items, 2,436 purchases, 620 repairs all listed/paginated/filtered correctly with zero SQL errors; a throwaway smoke-test supplier was created and then deleted to leave the dev database clean. 54 new backend tests (supplier match/merge/tenant-isolation, the pure recommendation formula, threshold CRUD + role/tenant boundaries, transaction pagination/filter/PII-shape/tenant-isolation, and ingestion-level auto-create/auto-match/no-cross-business-match for the new `supplier` upload field) — 694 backend tests passing (up from 640), clean frontend build (3 new routes: `/suppliers`, `/products`, `/transactions`). |
| 1.55 | 10/08/2026 | Real logged-in browser walkthrough (first one this session that actually had a usable Supabase test account — signed up fresh, no environment blocker this time), a real session/logout bug fix, the repair-margin tax-inclusive fix, and a new detailed roadmap for four deferred product gaps, direct request via prompt file. **Bug fixed**: "Continue with Google" after Log out silently re-authenticated the previous session instead of requiring a fresh sign-in — root cause is that the app can only ever clear its own Supabase session (confirmed already correct: `signOut()` + `useRequireSession`'s auth-state listener already redirected every protected route to `/login` on sign-out), not Google's own separate browser-level SSO session, which is what was actually letting the re-auth go through silently. Fixed by adding `queryParams: { prompt: "select_account" }` to both `signInWithOAuth` calls (`frontend/app/login/page.tsx`, `frontend/app/signup/page.tsx`) — forces Google's account-chooser screen explicitly rather than a silent pass-through, satisfying the fix requirement to "make the app-side behavior explicit and safe" where the provider's own session can't be revoked from here. **Verified live, not just read**: real signup, business creation, employee add (redirected all the way to a genuine Stripe Checkout sandbox session, confirmed the correct €5/mo Employee Seat price — stopped short of entering payment details, per policy), the unified member list (`Alex Rider - Staff — Pending payment and signup`, exactly as designed), employee delete (row correctly flips to "Removed"), Log out, confirmed every protected route redirects to `/login` afterward, and — the actual bug scenario — clicking "Continue with Google" post-logout now shows Google's real account-picker screen instead of silently re-entering the app, using this environment's own already-authenticated Google session as the live proof. Test business soft-deleted afterward to leave the dev database clean. **Bug fixed, reported alongside it**: Workshop Performance's `gross_margin_pct` had the same tax-inclusive overstatement issue closed for sales in v1.13 — `price_charged` on a repair invoice is very often tax-inclusive. New optional `tax_amount` field on the repairs upload (`ProductionEvent.tax_amount`, migration `a7d2e9c14f5b`, mirrors `sale_items.tax_amount` exactly) feeds a new `net_gross_profit`/`net_gross_margin_pct`/`tax_data_coverage_pct` on `WorkshopMarginResult`, computed only over repairs where both labour cost and tax are known — same principle as `financial.py`'s existing fix, never blending a tax-unknown repair into a "confirmed net" figure. Dashboard and Reports' Workshop Performance sections now show the net-of-tax figure whenever available (new shared `workshopMarginDisplay` helper, mirrors `grossMarginDisplay`'s exact pattern). **New**: `docs/governance/18_Product_Gaps_Roadmap.md` — a detailed, per-feature build plan (UI/states/CRUD/security/audit/tests/data-model) for the four gaps found live: low-stock threshold UI + ORLA-recommends-from-deterministic-data (roadmap/future work), the repair-tax bug (this entry — fixed), a real-time repair-logging screen (explicitly out of scope, future idea only, confirmed not being built), supplier tracking on purchases (roadmap/future work), and dashboard drill-down to raw transactions (roadmap/future work) — none of the four "not implemented now" items were built, per explicit scope guidance. 26 new/updated backend tests (repair-row tax parsing, importer write-through, pure-formula tax-net math, a real-DB aggregate-query round trip) plus one existing test/fixture updated for the new optional `tax_amount` canonical field — 640 backend tests passing (up from 632), clean frontend build. No frontend test framework exists in this repo to add automated coverage for the logout fix itself (a genuine gap, not silently skipped — the fix was instead verified live in the browser, described above). |
| 1.54 | 09/08/2026 | Closed the remaining gaps from the v1.53 employee-seats redesign, direct request via prompt file: invite email, delete/deactivate, and a unified member list. New `app/email/` module (mirrors `app/geocoding/`'s `client.py`/`service.py`/`exceptions.py` split) sends a real invite email through Resend's REST API (`httpx`, no new SDK dependency) whenever `add_employee` creates a seat for an email with no existing account yet; gracefully no-ops (`employee_invite_email_sent` never logged, no error surfaced) when `RESEND_API_KEY`/`RESEND_FROM_EMAIL` aren't configured — both still blank in `.env`, so this is wired but not yet actually sending. Invite link is `/signup?email=...`, prefilling the frontend signup form's email field. `reconcile_pending_employee_seats` now also logs a new `employee_registration_completed` audit event the moment a pending seat links to a newly-synced account, independent of payment status. New `DELETE /businesses/{id}/employee-seats/{seat_id}` (owner-only, 204, matching the existing `DELETE /businesses/{id}` convention): cancels the seat's Stripe subscription if one exists, revokes its Membership immediately, and sets `status="canceled"` — a soft "remove," never a hard delete, consistent with this app's existing soft-delete posture elsewhere. "Owner can't delete themselves down to zero owners" is satisfied structurally, not by a runtime check: the new delete route only ever operates on `EmployeeSeat` rows, and the owner's own access was never modeled as one, so there's no code path that can touch it — removing a shop's owner remains the pre-existing, separate "Delete this shop" action. New `GET /businesses/{id}/members` (any role, unlike the owner-only seats-management route) returns one merged, read-only list — the owner (from `Business.manager_first_name`/`manager_surname`, always active) plus every employee seat including canceled ones (shown, not hidden) — deliberately excluding email, which stays only on the management route. Frontend onboarding page now renders this list per business ("Mauricio Ruiz — Owner", "Antonio Ruiz — Manager (Removed)", etc.) with Edit/Delete actions only on non-owner rows for owners. Found and fixed two bugs while wiring this: a monkeypatch in a new email test was patching the wrong module reference (the name-bound import site, not the original module — `app/application/employee_seats.py` does `from app.email.service import send_employee_invite_email`, so the original module's attribute isn't what's actually called); and the new DELETE route was originally returning `200 + body`, which the existing untyped `apiDelete()` frontend helper (hardcoded to assume a bodyless `204`) couldn't have handled — fixed by matching the existing convention instead of changing the shared helper. 15 new/rewritten tests (invite-email send/skip/failure-doesn't-block cases, delete + Stripe-cancel + idempotency + cross-tenant isolation, the full members-list shape and its any-role visibility, and an end-to-end "delete revokes access on all four protected route families" case) — 632 backend tests passing (up from 617), clean frontend build. No new Stripe objects created this round — reused the `STRIPE_EMPLOYEE_SEAT_PRICE_ID` Price created in v1.53. **Not live-verified in the browser this round** — same environment constraint as v1.52/v1.53 (no stored login session, no test credentials); relied on the backend's end-to-end HTTP tests exercising the identical calls the UI makes. **Deferred, stated not built:** real invite-email sending is untested against a live Resend account (no API key on hand); a token-based magic-link registration flow was considered and deliberately not built, since the existing email-match reconciliation from v1.53 already satisfies every functional requirement in the prompt at far lower complexity. |
| 1.53 | 10/08/2026 | Real bug reported directly: an owner adding an employee who'd genuinely already signed up via Google got "No account found for [email]." Root cause: `User.email`/the lookup query compared raw case, and email is conventionally case-insensitive (always true for Gmail) — a casing mismatch between what was typed and what Supabase/Google returned made a real account invisible. Fixed at the source (`get_current_user_synced` now normalizes to lowercase on write) and at every match site (`EmployeeSeatRepository` uses `func.lower()`/`.ilike()` throughout), not just papered over at one call site. Used as the trigger for the larger product-direction change requested alongside it: v1.52's "employee must already have an account" requirement is gone — the owner now creates the profile directly (first/surname/role/email, plus optional address fields with the same live Geoapify autocomplete as the owner's own profile), no pre-signup required. `EmployeeSeat.user_id` is now nullable, linked immediately if a matching account already exists, or automatically the first time that email later authenticates (`app/application/employee_seats.py::reconcile_pending_employee_seats`, called from `get_current_user_synced` on every request — cheap at this app's scale). Real access now needs BOTH conditions — payment succeeded (`status == "active"`) AND an account linked (`user_id` set) — whichever completes second is what actually creates the Membership (`try_activate_employee_seat`, shared by the webhook and the reconciliation path, avoiding a circular import between `app/billing/service.py` and `app/application/employee_seats.py` via one deferred/local import each way). New `employee_payment_succeeded` audit action, distinct from `employee_membership_activated` — payment can now genuinely succeed well before signup. Added a `PATCH .../employee-seats/{id}` route (owner-only) so an employee's profile can be edited after creation, including a live Membership's role if the seat is already active; the list route now returns `account_linked` so the frontend can show which of "paid" and "signed up" is still missing rather than a bare "Pending." Also, direct request: split `Business.manager_name` into `manager_first_name`/`manager_surname` (owner profile, not just the new employee one) — migrated with a best-effort backfill (split on first space) for the two existing real profiles that had one set. Created the real Stripe Product ("AI Operations Platform - Employee Seat") and a €5/month recurring Price via the API itself, using the existing restricted key's write access (confirmed it had Product/Price scope) — `STRIPE_EMPLOYEE_SEAT_PRICE_ID` is now genuinely configured in `.env`, closing the gap v1.52 flagged. Migration backfill verified directly against the real dev Postgres before proceeding. 39 new/rewritten backend tests covering both activation orderings (payment-before-signup and signup-before-payment), the case-insensitive match itself, profile editing (including the live-Membership-role-follows-edit case), and every existing employee-seat/business-profile test updated for the new shapes — 617 backend tests passing (up from 578 pre-v1.52), clean frontend build. **Not live-verified in the browser this round either** — same environment constraint as v1.52 (no stored login session, no test credentials); relied on the backend's end-to-end HTTP tests exercising the identical calls the UI makes. |
| 1.52 | 09/08/2026 | Paid employee seats (€5/month, up to 2 per business), direct request. Treats the existing `owner` role as the admin role (no new RBAC); an added employee gets `manager` or `staff`. Scoped via AskUserQuestion first: since there's no invite-by-email/magic-link flow in this codebase, an employee must already have signed up (looked up by email against the existing `users` table) before they can be added — chosen over building async invite-acceptance, which would have been a much larger, separate feature. New `employee_seats` table mirrors a branch's own dedicated-Stripe-subscription shape (not a quantity-based line item — zero multi-item Stripe precedent exists anywhere in this codebase) via a new `employee_seat_id` Checkout/webhook metadata key threaded alongside `business_id`. Critically, **no Membership row is ever created on the synchronous add-employee path** — only the webhook (`app/billing/service.py::_apply_employee_seat_event`, reusing `derive_subscription_status` unmodified) creates one, the moment the seat's own subscription reaches "active," and removes it the moment that status stops being active (payment failure or cancellation) — access tracks live payment status, not just its first success, idempotently (a repeated identical transition creates no second Membership and no duplicate audit entry). Since Dashboard/Uploads/Reports/Ask ORLA already gate on plain `get_current_membership` with no extra check, this required zero changes to any of those four route families — "no employee access before payment" holds by construction (no Membership row exists to grant it), not by adding new authorization logic to routes that already worked. Audited via the existing centralized `record_audit_event` (PR-6.5): `employee_invited`, `employee_payment_started`, `employee_membership_activated`, `employee_payment_failed`, `employee_payment_canceled`. `billing/client.py::create_checkout_session` gained a generic `extra_metadata` param (backward compatible) rather than a one-off employee-specific parameter, so a future add-on billed the same way doesn't need its own bespoke checkout function. New `STRIPE_EMPLOYEE_SEAT_PRICE_ID` setting — **not yet configured** (blank in `.env`), so live Checkout for a real employee seat 503s with a clear message until a real Stripe test-mode Price is created, mirroring exactly how `STRIPE_BRANCH_PRICE_ID` was bootstrapped earlier. 29 new backend tests (webhook activation/idempotency/revocation, `add_employee` orchestration rules, and a full HTTP-level tenant-isolation suite proving a pending employee gets 403 on all four protected route families and a paid one doesn't, end to end through a real simulated webhook) — 607 backend tests passing (up from 578), clean frontend build. **Not live-verified in the browser this round** — no stored login session in the environment and no test credentials on hand; verification relied on the backend's own end-to-end HTTP tests exercising the identical API calls the new UI makes, plus the frontend's own clean type-checked build. |
| 1.51 | 09/08/2026 | Branch creation now collects the full business profile up front, direct request: "the Profile section to come first when they create a new branch and make all fields mandatory." Replaced the branch-creation form's old two-field (name + timezone) shape with the same nine profile fields the `/onboarding/[id]` edit page has (manager/owner name, contact email + phone, location label, address with live Geoapify suggestions, city, postal code, country, timezone) plus the branch name — all ten now `required`, native browser validation blocks submission on any blank field before anything is created. Address suggestions are looked up scoped against the *parent* business's id, since the branch doesn't exist yet to scope them against and that endpoint's `business_id` is only ever used for its own membership gate (`app/api/businesses.py::get_address_suggestions`), never to filter the data — safe given the caller already has to be the parent's owner to reach this form at all. Save now does two calls in sequence (`POST .../branches` once, then `PATCH` the new branch with the rest of the profile) before redirecting to Stripe Checkout exactly as before; a `branchId` set the moment creation succeeds means a retry after a failed profile save only PATCHes that same branch rather than creating a second one. Cancel best-effort soft-deletes an already-created-but-incomplete branch (fire-and-forget, closes the form immediately either way) instead of leaving an orphaned, blank-profile "Pending Payment" row behind. Extracted two genuine duplications in the process rather than copy-pasting them a second time: `useAddressAutocomplete` (`frontend/lib/hooks/`, the debounce/dropdown/apply-suggestion logic `[id]/page.tsx` already had inlined) and the shared field key/label list (`frontend/lib/businessProfileFields.ts`) — `[id]/page.tsx` itself refactored onto both with no behavior change there (still every field optional, unlike the new mandatory branch flow). No backend changes; 578 backend tests unaffected, clean frontend build, and the whole flow live-verified against the real running dev stack end to end: blank-submission validation blocks correctly, a real Geoapify address search on Patrick Street, Cork returned live suggestions that correctly filled address/city/postal/country/timezone, Save created the branch with its full profile (confirmed via its own profile page) and reached the real Stripe Checkout page for "Additional Shop (Branch)" at the correct €30/month-equivalent price, and Cancel on an unfinished form correctly created nothing. |
| 1.50 | 09/08/2026 | Multi-location upload bypass check (BD-007), direct request: a client uploading combined data from more than one physical location into a single shop's file avoids paying the €30/month fee each additional branch should be billed separately for. New optional `location` canonical field (`app/imports/aliases.py`, aliases: store/branch/shop/outlet/site/location) added to all four upload entity types (sales/inventory/purchases/repairs — unlike `category`, no FK gap excludes repairs here, since location is a repair-level attribute, not a product-level one) — deliberately never persisted anywhere, purely a pre-import signal. `app/imports/service.py::confirm_mapping` now scans the mapped location column (over the same bounded detection-window grid already read, no second file read) for distinct values; more than one returns a new `needs_location_confirmation` status instead of creating anything, with the distinct values found — zero DB writes on a block, cleanly re-triable. Scoped via AskUserQuestion first: user chose "block, require explicit confirmation" over a softer warn-only option. `confirm_mapping` now returns a `ConfirmMappingResult` dataclass (was a bare tuple) with an explicit `confirm_multiple_locations` override param for the case where a file's location-like column genuinely repeats one shop's own name/code; `ConfirmMappingResponse`'s `import_record_id`/`mapping_profile_id` are now nullable (both `None` specifically on the block). Frontend: `location` added to the field-mapping form for all four entity types with a shared hint explaining the branch-fee implication; a `needs_location_confirmation` response renders a block-with-override prompt (the locations found, an "Add a branch" link to Onboarding, and a "Yes, this is genuinely one location" button that resubmits with the override flag) instead of silently failing or silently saving. Adding the new canonical field required updating 9 existing test files' hardcoded field-mapping dicts (same exact-match-key mechanism that caused the `category`-field bug fixed earlier this session) — mechanical, zero logic changes, all pre-existing tests still pass unmodified in behavior. 4 new dedicated tests cover: block on >1 distinct location, override pushes it through, a single repeated value never blocks, and an unmapped location column is never scanned at all. 578 backend tests passing (up from 574), clean frontend build. **Honest limitation, stated before building:** only catches a file that itself carries a location/store/branch column with multiple values within the bounded ~215-row detection window — cannot detect commingled data with no such column at all, or one whose distinct values only appear past that window. |
| 1.49 | 08/08/2026 | Replaced address validation's click-to-validate flow with live, as-you-type suggestions, direct request ("like modern websites"). New `app/geocoding/client.py::autocomplete` calls Geoapify's dedicated Autocomplete endpoint (distinct from the single-best-match `/search` endpoint `geocode()` still uses) for up to 5 partial-match candidates; `app/geocoding/service.py::suggest_addresses` resolves each candidate's own timezone via `timezonefinder` up front (cheap, offline, no second round trip needed once one is picked) and fails quietly (empty list, never an error) for a blank query, an unconfigured key, or a provider failure — a live-suggestion field going quiet while someone is mid-sentence is normal UX, not something to interrupt with an error banner. Replaced `POST /businesses/{id}/validate-address` outright with `GET /businesses/{id}/address-suggestions?text=...` (an idempotent lookup, not an action) — old route, request/response schemas, and their tests all retired rather than left orphaned alongside the new ones. Frontend: the business profile page's Address field now debounces 350ms after typing stops (minimum 3 characters) and shows a live dropdown; clicking a suggestion fills address/city/postal_code/country/timezone directly, still editable by hand afterward, still requires the normal Save click like any other edit. Dropdown styled with CSS system colors (`Canvas`/`CanvasText`), not a hardcoded white — this app has no custom theme variables of its own, relying entirely on `color-scheme: light dark` for automatic dark-mode adaptation, and a hardcoded background would have broken that. 574 backend tests passing, clean frontend build, live-verified against the real Geoapify account for both the search and autocomplete endpoints. |
| 1.48 | 08/08/2026 | Fixed a real bug reported directly, live-reproduced 3/3 times before touching anything: "what's my revenue in Galway?" got "I don't have that information" — even though classify correctly picked `financial_performance` and `_fetch_context` correctly fetched Galway-only data (confirmed live: Galway's real revenue was €43,688.43, sitting right there in the context the model was given). Root cause: the explain-step context payload never named which business/branch it represented at all — just bare figures — so a question that explicitly asked about one location left the model unable to tell whether what it was looking at was already scoped to that location or a whole-account total with no breakdown, and it guessed wrong every time. `_generate_answer` now takes a `scope_label` (the resolved business's name, or `"Combined across: X, Y"` under `all_branches`) and states it outright in the system prompt ahead of the data — re-verified live, 3/3 identical runs now correctly answer "€43,688.43," and the `all_branches` combined case correctly labels itself and sums to the same figure as adding each business's own revenue by hand. Separately, confirmed live: the second symptom in the original report (an identical question later returning the generic out-of-scope refusal) is downstream of already-documented free-tier `classify` model-routing variance (v1.34), not this bug — not solved further here. Building the regression test for the not-configured `validate-address` path (v1.46) surfaced a real test-isolation gap once a real `GEOAPIFY_API_KEY` was actually added to `.env`: that test relied on the key being *absent* from the real environment rather than mocking the boundary, so it silently started making a live Geoapify network call on every test run instead of testing the degradation path at all — fixed to mock `app/geocoding/service.py` directly, independent of whatever's really configured. 573 backend tests passing, no regressions. |
| 1.47 | 08/08/2026 | Ask ORLA gained branch awareness, direct request scoped via a clarifying question first: (1) a welcome greeting ("Hello, I'm ORLA — Operational Reporting and Logic Analytics...") prompting for a branch when the account has more than one; (2) an "All branches" option in the chat page's business dropdown, reusing v1.45's `app/application/business_group.py` aggregation exactly as the dashboard's own checkbox does — same five combinable domains (Financial/Retail/Workshop/Forecast/Findings), same graceful mixed-timezone/not-a-member messages, zero AI cost either way since both checks happen before the classify call; (3) a question naming one specific branch by itself (`"what was revenue at the Galway branch?"`) always answers about that branch — confirmed via direct question to be **deterministic name-matching**, not classify-prompt-based: `_detect_named_business` does a whole-word, case-insensitive check against the caller's own real business names (never an LLM call, never touches the classify prompt, which has already regressed twice this session from growing past its token budget), only matches a business the caller is actually a member of, and only acts on an unambiguous single match — two real businesses both named in one question falls through rather than guessing. Explicit naming wins over "All branches" for that turn, and works with no toggle at all — asking about a branch by name from any other business's chat page just works. Building the test coverage for this surfaced a real, pre-existing, undocumented behavior worth flagging: `answer_question`'s injectable `now` parameter only affects the `last_completed_week`/`last_completed_month` period types (via `compute_report_period`) — the default `"default_recent"` period (the common case) always resolves its 30-day window against real wall-clock time regardless of what `now` is passed, because `_resolve_dates` returns `(None, None)` for it and the downstream `resolve_period` call is never given `now` at all. Not a bug introduced here (the single-business path has always worked this way) and not fixed in this pass — just newly visible because this is the first test suite to seed dated sales data and assert on a specific resulting figure rather than a zero/empty-state answer. 573 backend tests passing (up from 562), clean frontend build, live-verified against the real dev database (Test Bike Shop + its Galway branch) — a question naming "Galway" correctly resolved to that branch specifically. |
| 1.46 | 08/08/2026 | Added an address-and-timezone validator for the business profile page, per direct proposal (Geoapify address geocoding -> timezonefinder lat/lon-to-IANA-zone lookup -> server-side validation -> Neon), reviewed and endorsed with two refinements before building: keep a manual timezone override (geocoding fails or guesses wrong sometimes), and it needed its own new API key (can't be provisioned by this assistant). New `app/geocoding/` module: `client.py` (the only Geoapify caller, mirrors `app/ai/client.py`'s "one file owns the provider boundary" shape), `timezone_lookup.py` (a module-level `TimezoneFinder()` singleton — constructing one per call would be wasteful, its own docs warn against it), `service.py` orchestrates the two and degrades gracefully (a clear "not configured" / "no match found" / "provider request failed" result, never a raised error) when `GEOAPIFY_API_KEY` is unset or the request fails — same PR-5.4-style posture as the AI provider gateway. New `POST /businesses/{id}/validate-address` — deliberately an explicit action, not autocomplete-as-you-type (Geoapify's free tier caps at 3,000 requests/day; editing a profile is infrequent, there's no case for per-keystroke volume here); returns a suggestion only, applied into the profile form via a new "Use this address" button, still requires the normal Save click like any other edit — never writes anything itself. Live-verified `timezonefinder` directly: it covers the *entire* globe including ocean (via `Etc/GMT+N` nautical zones), not just land, correcting an initial doc-comment assumption that "no match" was a real, if rare, outcome — in practice it never happens for a valid coordinate. 562 backend tests passing (up from 550, including a mocked-provider-boundary suite for every degradation path plus two live geographic lookups), clean frontend build. **Not yet live-verified end-to-end**: no real `GEOAPIFY_API_KEY` exists yet (the user's own account, not committed) — the whole pipeline up to and including the Geoapify HTTP call itself is untested against the real provider; every other path (not-configured, no-match, provider-error, and the full match-through-timezone flow with a mocked provider response) is verified. |
| 1.45 | 08/08/2026 | Added an "All branches" combined dashboard view — direct request, scoped via two clarifying questions first: covers every section (Financial, Retail, Workshop, Forecast, Findings — the full answer, not just Financial), and only offered when every business in the group shares one timezone (never silently blends two different "this week"s into one number; a mismatch surfaces as a plain 409 naming the timezones instead). New `app/application/business_group.py`: `resolve_authorized_group` resolves a business's whole standalone-shop-plus-branches family (`app/repositories/business.py::get_business_group`, symmetric — works starting from the parent or any branch) and checks membership *per business*, not just the group relationship, before combining anything — a manager added to only one branch, not the primary shop, correctly can't pull in data from a business they don't have access to. Every aggregation function sums or concatenates **raw** per-business inputs (repository rows, before any ratio is computed) and then calls the exact same, unmodified formula from `app/analytics/*.py` exactly once over the combined data — deliberately never merges already-computed per-business percentages, which would silently average unrelated rates instead of computing one true combined rate. A product/category is already a UUID scoped to one business's own table, so combining per-product tables (top sellers, stock cover, forecast reorder rows) is a plain, collision-free union, never an identity merge — two branches' own "Chain Lube" stay two separate rows. The revenue forecast's combined confidence band isn't a separate statistical formula either: raw daily sale amounts are concatenated across the group *before* bucketing into a daily series, then run through the same `compute_baseline_forecast` untouched — mathematically identical to the group being one shared sales table. All five existing analytics routes (`app/api/analytics.py`) gained an opt-in `all_branches` query param (default `false`, every existing call site unaffected); dashboard gained a "Combine all branches into one view" checkbox, shown whenever the account has more than one business (the one-shop-per-account limit guarantees that's always exactly the group). 550 backend tests passing (up from 540) — new coverage proves the sum-not-average property directly (two businesses of different sizes, summed revenue checked against each one's own individually-computed figure) plus the membership/timezone/wiring paths end-to-end through the real routes — clean frontend build, and the whole pipeline live-verified against the real dev database (Test Bike Shop + its Galway branch) before any test was written. |
| 1.44 | 08/08/2026 | Fixed a real, significant bug reported as "'Could not save this mapping' even though I checked all fields" for a new branch — root-caused live against the branch's actual stuck uploads (verified directly, then rolled back, no data changed) rather than guessed at: **it wasn't branch-specific at all.** `frontend/app/uploads/page.tsx`'s `FIELD_ORDER` for sales/inventory/purchases never listed `"category"`, even though `backend/app/imports/aliases.py::CANONICAL_FIELDS` has required it (as an optional field within the set) since v1.26's product-categories feature — and `confirm_mapping` rejects anything whose field-mapping keys aren't an *exact* match to the canonical set. Net effect: confirming a sales, inventory, or purchases mapping has been silently guaranteed to fail for every business, not just this branch, since v1.26 shipped (Aug 7) — repairs was the only entity type unaffected, since it has no category field. Live-reproduced both ways against the branch's real stuck upload to confirm the exact mechanism before fixing: the same suggested mapping succeeds with `category` included, fails with `InvalidFieldMapping` without it. Fixed by adding `category` to `FIELD_ORDER`/`FIELD_LABELS`/`FIELD_HINTS` for the three affected entity types, matching the backend's set exactly. Separately, and independently worth fixing: `handleConfirmMapping`'s error handling (plus three sibling catch blocks in the same file) always showed one fixed guessed message regardless of the real failure — the backend already returns an accurate, specific `detail` per exception type (missing required fields, a stale/renamed column, upload not ready, ...) but the frontend discarded it every time. Now surfaces `ApiError.message` like the rest of this frontend already does, so a genuinely different failure won't masquerade as the same "check your mapping" message again. **Known residual fragility, not solved here:** `FIELD_ORDER` and `CANONICAL_FIELDS` are two independently-maintained lists in different languages with no shared source of truth and no automated check that they stay in sync — this exact class of drift could recur the next time a canonical field is added on one side only. No backend changes; 540 backend tests still passing, clean frontend build, and the fix verified against real data (not just the type-checker) before shipping. |
| 1.43 | 08/08/2026 | Reworked the onboarding page into a proper "Company Profile" area, per direct feedback covering four points. **(1)** The "Add a branch" form had no Cancel button of its own (only the toggle button re-labelling itself) — added an explicit one next to "Add branch," clearing the form. **(2)** Top nav's "Onboarding" link renamed to "Company Profile" (same `/onboarding` route). **(3)** The list's inline 9-field "Edit profile" form (one per business, all expanding in place) is replaced by a "View profile" link to a new dedicated page, `/onboarding/[id]` — the full profile, editable, with **Save** (PATCH, then back to the list) and **Cancel** (back to the list, nothing sent) buttons, closing the list itself back down to one line of quick actions per business. **(4)** Each business now shows one of exactly four statuses — **Subscribed / Pending Payment / Cancelled / Deleted** — collapsed from the raw Stripe subscription status plus this app's own soft-delete flag. Showing "Deleted" required a real, scoped backend change: `GET /businesses` gained an opt-in `include_deleted` query param (default `false` — every existing caller, every other page's business selector, is unaffected) and `BusinessOut` gained `deleted_at`; only the Company Profile list passes `include_deleted=true`. A deleted business is listed read-only (no actions — the backend already 404s any PATCH/DELETE against one), closing a real gap where "was this shop actually removed?" had no answer short of querying the database directly. "Upload data" next to each business continues to gray out unless status is exactly "Subscribed" (v1.39/v1.41's fix, unchanged, now reusing the same four-state label). 540 backend tests passing (up from 538, new coverage for `include_deleted` and its tenant isolation), clean frontend build, live-verified against the real dev database including one business the account owner had genuinely deleted mid-session — its "Deleted" status rendered correctly. |
| 1.42 | 08/08/2026 | Reported directly: "we are missing the button to add more branches" on the onboarding page. Verified live before changing anything — the button was genuinely present in the served code (checked directly inside the running container) and the real dev-DB data was correct ("Test Bike Shop" confirmed standalone and not soft-deleted, which is exactly what makes the button render) — this was not a missing feature or a broken condition. Root cause: each business had grown into one long, unstyled, run-on line of inline buttons (upload link, subscription status, delete, edit profile, add-a-branch, all separated by "—"), and the branch button specifically sat at the very end of it, easy to lose. Restructured each business's row into clearly separated lines (name/role, upload link, subscription status, then a dedicated "+ Add a branch (€30/mo)" line for standalone shops, then delete/edit-profile below) so it can't be scanned past. Also restarted the frontend dev container as a precaution — its own logs showed several "Fast Refresh had to perform a full reload due to a runtime error" events from earlier in the session, raising the possibility of a stale client bundle compounding the visibility problem. No backend changes — clean frontend build. |
| 1.41 | 08/08/2026 | Two bugs reported directly. **(1) "Delete this shop does nothing"**: replaced the `window.confirm()` dialog with a plain inline "Delete X? Yes / No" prompt (the same expand-in-place pattern already used for the branch and profile forms) — native confirm dialogs are known to silently no-op in some embedded/webview browser contexts rather than actually showing anything, which is consistent with "no action" on a real click; the inline version has no such dependency and also matches the explicit "Yes/No" wording asked for, rather than the browser's own "OK/Cancel." **(2) "Upload page's business dropdown still shows a shop pending payment"**: the dropdown (and the page's default business selection) previously listed every business on the account regardless of subscription status, even though uploads there always 402 for one that isn't active. `frontend/app/uploads/page.tsx` now fetches every business's subscription status itself (`Promise.all`, so the dropdown only ever filters once real data is in, never flashing empty first), restricts the dropdown to actively-subscribed businesses only, auto-corrects the selection off a pending/unsubscribed business onto a subscribed one once available, and shows a clear "none of your shops has an active subscription yet" message with a link to Onboarding if none qualify — deliberately scoped to this page alone (`useBusinessSelector` itself stays unfiltered, since dashboard/reports correctly keep showing every business, subscribed or not, per v1.36's read-routes-stay-open design). No backend changes — 538 backend tests still passing, clean frontend build. |
| 1.40 | 08/08/2026 | Added a per-business profile — direct request: "a full profile for owners in each branch: name of the owner/manager, Location, Time Zone, address, contact email, and some other necessary fields." Scoped via a direct question first: descriptive contact/location record-keeping only, not a second login (a real per-branch-manager account would need an invite/permissions system that doesn't exist anywhere in this codebase yet — confirmed out of scope). New nullable `Business` columns (migration `f3c7b2e9a1d4`, applied to the real dev Postgres): `manager_name`, `contact_email`, `contact_phone`, `location_label` (a short label like "Dublin - Rathmines", distinct from the formal shop `name`, which is often identical across a primary shop and its branches), `address_line1`, `city`, `postal_code`, `country` — `timezone` already existed and is now editable through the same form rather than only at creation. New `PATCH /businesses/{id}` (any member, not owner-only — unlike delete/add-branch, editing contact info has no financial or destructive weight), `exclude_unset` semantics so an omitted field is left alone while an explicit null clears it, and a defensive field whitelist in the repository layer independent of the request schema. Refactored all four `BusinessOut`-constructing routes onto one shared `_to_business_out` helper — this codebase already hit a real bug once from that exact inline-construction pattern (a field silently defaulting to `None` in two routes that forgot to pass it through), so a field added to the model now only needs updating in one place. Onboarding page gained an "Edit profile" expandable form per business, same pattern as "Add a branch." 538 backend tests passing (up from 533), clean frontend build, migration verified live against the real dev database. |
| 1.39 | 08/08/2026 | v1.38 only fixed the onboarding page's own per-business "Upload data" link — reported directly, and confirmed live, that the *shared* `AppNav` component's own "Upload data" link (present on every page's top nav — dashboard, uploads, reports, chat) was still unconditional, so it kept implying real upload access for an unsubscribed business (verified concretely against a real newly-added, payment-not-completed branch, `Test_Branch_Added`, which correctly has no `Subscription` row at all — `require_active_subscription` was, and remains, correctly enforced server-side the entire time; this was purely a second instance of the same frontend false-impression gap). `AppNav` now fetches the given `businessId`'s subscription status itself and only renders a working "Upload data" link when `status === "active"`, otherwise "Upload data (subscribe first)" text — mirroring the onboarding page's own fix exactly, the one other place this link exists. Clean frontend build; no backend changes. |
| 1.38 | 08/08/2026 | Two follow-ups reported directly right after v1.37 shipped, against the same two real dev-DB businesses. **(1)** Archived `Test_Shop` (soft-deleted directly, per explicit instruction — it had no subscription and no data, so nothing else was touched) rather than keeping it around as a `parent_business_id`-linked branch, which is what v1.37 had turned it into. **(2)** Closed a real, confirmed UX gap: the onboarding page's "Upload data" link rendered unconditionally for every business regardless of subscription status, which looked like real access even for a business the backend would 402 on the moment an actual upload was attempted (`require_active_subscription` was, and remains, correctly enforced server-side the whole time — verified directly against the real dev DB: "Test Bike Shop" has no active Stripe subscription today and its uploads route does 402). The link now only renders when `status === "active"`; otherwise it's plain "Upload data (subscribe first)" text, matching the real gate instead of contradicting it. **(3)** Reworked the "Add a branch" flow per direct feedback: submitting the inline form now redirects straight into Stripe Checkout for the branch price (`redirectToCheckout`), rather than leaving a newly-created, unpaid branch sitting in the list as a separate manual "Subscribe" step. A branch that exists but hasn't completed payment (abandoned Checkout, or paid Stripe hasn't confirmed the webhook yet) now reads "payment incomplete" with a "Complete payment (€30/mo)" button — recoverable, not a dead end — rather than either silently granting access or vanishing; the underlying create-before-pay shape is deliberately unchanged (matches every standalone shop's existing, already-tested pattern, and avoids a materially riskier redesign of the Stripe webhook handlers, which currently all key off a `business_id` already existing in event metadata and would need out-of-order-delivery-safe branch creation logic duplicated across three separate event handlers to defer business creation to the webhook itself — flagged as a real, deliberately-not-taken alternative, not silently avoided). No backend changes this round — 533 backend tests still passing, clean frontend build. |
| 1.37 | 08/08/2026 | Tier 2 branch billing + nav consistency, direct request. **(1) Paid branch add-on (€30/month)**: a branch is a second full `Business` row (`parent_business_id` set, schema groundwork already laid in v1.36) with its own separate `Subscription` row, reusing the exact same checkout/webhook/cancel infrastructure as any standalone business — a deliberate simplification over the originally-stated "one shared subscription, quantity line item" idea (confirmed via direct question: today's checkout is hardcoded to one fixed-price, quantity-1 line item, zero multi-item precedent anywhere in the billing code). New `settings.stripe_branch_price_id`, a new Stripe test-mode Price created directly via the app's own already-configured `rk_test_` key (a safe, reversible config action, not a financial transaction); `billing/service.py::start_checkout` selects it automatically whenever `business.parent_business_id is not None`. New `POST /businesses/{id}/branches` (owner-only, `create_branch_business` in the repository layer — exempt from the one-standalone-shop limit, gated on parent ownership via `Membership.role == "owner"`, not by any pre-paid quantity). Frontend: `frontend/app/onboarding/page.tsx` shows each branch labelled "↳ Branch of {parent}", offers "Add a branch (€30/mo)" on every standalone shop via an inline form, and the standalone-shop-limit check/hint text now correctly excludes branches (previously would have wrongly counted them). **(2) Nav consistency**, direct request: "Log Out" and "Onboarding" (renamed from "Manage shops") now live in the shared `AppNav` component itself rather than only on the onboarding page — every screen that renders `AppNav` (dashboard/uploads/reports/chat) gained both; the onboarding page itself gained `AppNav` for the first time, closing the reported gap where Dashboard/Reports/Ask ORLA were unreachable from it. **(3) Real-data correction, reported honestly rather than assumed**: live-checked the two named dev-DB businesses ("Test Bike Shop", "Test_Shop") against real Stripe test-mode data before touching anything — found neither has a matching Stripe subscription today, despite the stated expectation that the first was already paid; see this turn's reply for the full finding. The one safe, non-financial correction made directly: `Test_Shop.parent_business_id` now points at Test Bike Shop's id, so it's correctly recognized as its branch going forward — no subscription/billing state was fabricated for either business. 533 backend tests passing (up from 523), clean frontend build. **Not yet live-verified end-to-end in the browser**: doing so needs the account owner's own login (Supabase session) or a real Stripe test-mode Checkout completion, neither of which this assistant can perform on the user's behalf. |
| 1.36 | 08/08/2026 | Subscription enforcement, prioritized from a larger backlog: uploads worked regardless of subscription status until now (`app/billing/access.py::require_active_subscription` existed since an earlier stage but was never wired to a route), and there was no limit on how many businesses one account could create, nor any way to delete one. **(1) Uploads blocked without an active subscription**: `require_active_subscription` now gates the two routes that actually create new business value — `POST /businesses/{id}/uploads` (`request_upload`) and `POST .../uploads/{id}/import` (`run_import`) — leaving every read/cleanup route (list, freshness, detect/confirm-mapping, undo) on plain `get_current_membership`, so a lapsed account can still see and fix what it already imported. Frontend gained a typed `ApiError` (previously `apiGet`/`apiPost` discarded the HTTP status into a generic string — zero status-aware handling existed anywhere in this frontend before this) surfacing the backend's actual `detail` message, and `frontend/app/uploads/page.tsx` shows a specific "subscribe to continue" prompt on a 402 instead of a generic failure. **(2) One shop per account**: `create_business_with_owner` now rejects a second standalone business for the same owner (409, `BusinessLimitReached`) — confirmed via a live-reported design tension during planning that "branches billed as a line item on the same subscription" doesn't match today's billing model (`Subscription` is one row per business, and Stripe checkout is hardcoded to one fixed-price, quantity-1 line item with zero multi-item precedent anywhere in the codebase); scoped this pass to the limit itself plus schema groundwork only (`businesses.parent_business_id`, unused by any route yet), with the actual paid-branch checkout flow explicitly deferred as a separate follow-up rather than guessed at. **(3) Delete shop**: soft-delete only (`businesses.deleted_at`) — confirmed live that none of the ~20+ `business_id` foreign keys in this schema have `ON DELETE CASCADE`, so a hard delete would fail outright the moment any child row exists; archiving cancels the business's Stripe subscription (new `app/billing/client.py`/`service.py::cancel_subscription`) but leaves every sale/product/upload/audit row untouched, consistent with this codebase's audit-log-conscious posture. New `DELETE /businesses/{id}`, owner-role-gated — the first real enforcement of `Membership.ROLES` anywhere in this codebase (defined since the model was introduced, never checked until now). Found and fixed a related gap while building this: `GET /businesses/{id}` didn't check `deleted_at` either, so a soft-deleted business was still directly fetchable by id. Frontend: `frontend/app/onboarding/page.tsx` now hides the create-business form once a standalone business exists (showing the 409's own message instead) and adds a confirm-gated Delete button per business; `frontend/components/AppNav.tsx` gained a persistent "Manage shops" link — confirmed there was previously no way back to onboarding/billing once a user had ≥1 business short of typing the URL. 523 backend tests passing (up from 508), clean frontend build, live-verified against the real Postgres dev database (not just SQLite) end-to-end: limit enforcement, soft-delete, data-not-touched, and slot-freed-on-delete all confirmed directly against production-shaped data. |
| 1.35 | 08/08/2026 | A third live-reported "period follow-up" failure ("what is the last period?") made clear v1.34's prompt-only fix (telling the classify model to infer the same intent as the previous turn) doesn't generalise reliably across phrasings on this free-tier model — it held for the one wording it was tuned against and failed on the next two. Replaced with a deterministic recovery, the same proven shape as the existing reorder-question recovery: `ChatRequest`/`answer_question` gain `previous_intent` (the prior turn's own `ChatResponse.intent`, echoed back by the frontend — untrusted client input, only ever checked against a new fixed `_CONTINUABLE_INTENTS` allow-list before use, never trusted directly). A new `_looks_like_a_period_follow_up_question` keyword check (mirroring `_looks_like_a_reorder_question`) gates the recovery: an unresolved `out_of_scope` classification for a period-referencing question with a valid, allow-listed `previous_intent` recovers to that exact intent instead of refusing — can never grant a more sensitive intent or override a genuine refusal for an unrelated question, matching the existing recovery's own safety invariant exactly (verified with a dedicated test asserting a genuine topic change right after a valid `financial_performance` turn still refuses correctly). Also closed the deeper data gap this kept surfacing: `FinancialPerformanceSummary`/`FinancialPerformanceOut` gain `previous_period` (the exact date range `revenue.previous` was computed over) — previously only the previous period's revenue *figure* was ever in the payload, never its actual dates, so even a correctly-routed answer could only honestly say "I don't have the dates." Frontend (`frontend/app/chat/page.tsx`) now tracks and resends the last answer's intent alongside its question/answer. 508 backend tests passing (up from 501), clean frontend build, live-verified against the exact reported scenario. |
| 1.34 | 08/08/2026 | Two fixes reported directly, both live-confirmed against real traffic from the user's own session — backend logs showed the exact 429s during their test, timestamped before this fix landed. **(1) "ORLA is temporarily unavailable"**: `app/ai/client.py`'s `chat_completion` gained a bounded retry — on a 429 from every model in the fallback chain, wait 2s and retry the whole chain once more (OpenRouter's routing isn't perfectly sticky, so a second attempt can land on a freed-up backend) before finally degrading to the existing graceful fallback (PR-5.4 kept as the genuine last resort, never removed). **(2) "when was the previous period?" still hitting the generic out-of-scope fallback**: confirmed the frontend was already serving v1.33's memory changes (real traffic in the access logs), reproduced the exact phrasing live, and found classify wasn't reliably inheriting the previous exchange's intent for this wording specifically. Strengthened `_CLASSIFY_SYSTEM_PROMPT_TEMPLATE`'s memory instruction from a passive "may resolve an ambiguous reference" note into an explicit directive — a short follow-up that only makes sense in light of the previous exchange should default to the SAME intent as the previous answer, with a worked example. Re-verified live: now correctly classifies `financial_performance` and answers honestly ("I don't have the dates for the previous period in the data supplied") instead of refusing outright — surfacing a separate, smaller, genuine gap (the previous period's date range isn't in the payload at all, only its revenue) flagged, not fixed, here. Attempting fix (1) also **surfaced two real, pre-existing problems while diversifying the fallback chain away from OpenAI+Google to reduce simultaneous rate-limiting**: first shipped a 3rd fallback on NVIDIA, which live-testing caught immediately breaking classify *entirely* — every question, not just follow-ups, silently defaulted to `out_of_scope` with zero trace in the logs. Root cause, live-verified: `nvidia/nemotron-3-nano-30b-a3b:free` 404s outright under this app's own `data_collection: deny` privacy restriction ("No endpoints found matching your data policy (Free model training)" — that provider's free tier requires allowing training on the data), and the model that had been in the fallback chain since before this fix, `inclusionai/ling-3.0-tiny:free`, turned out to 400 on every classify call for an unrelated reason — it doesn't support structured-outputs mode at all, a silent gap this investigation surfaced rather than introduced. Replaced both with `cohere/north-mini-code:free`, confirmed live to return clean, valid JSON under both restrictions — every fallback candidate is now live-tested against this app's actual request shape before being added, not picked from the public catalog on trust. Separately hit and fixed OpenRouter's own hard cap on the `models` array (400: "'models' array must have 3 items or fewer") after briefly shipping 4; capped back to 3 and added a defensive `models[:_MAX_MODELS_PER_REQUEST]` slice in `client.py` itself so a future config edit can't silently reintroduce this. Added logging to `_parse_intent_json`'s previously-silent failure paths (no content, non-JSON, non-dict, unrecognised intent) specifically because their silence is what let the NVIDIA regression hide as ordinary "not about this business" refusals — now distinguishable in the logs. Building test coverage for that logging then surfaced one more real, pre-existing test-infrastructure issue: Alembic's own CLI calls `logging.config.fileConfig()`, which defaults to `disable_existing_loggers=True`, silently disabling `app.ai.service`'s logger for the rest of any pytest session that runs a migration first — fixed with a small local fixture that re-enables it for the duration of the affected tests. 501 backend tests passing (up from 489). |
| 1.33 | 08/08/2026 | Added last-exchange-only conversation memory to ORLA chat, closing a repeatedly-flagged gap: "what was the previous period?" as a follow-up used to hit the generic out-of-scope fallback since ORLA had nothing to resolve "previous" against. Per direct scope decision: only the immediately preceding question/answer is resent, never the whole thread (bounded token cost by design), and it still counts against the existing per-business daily cap with no new cost mechanism. `ChatRequest` gains optional `previous_question`/`previous_answer`; `answer_question`/`_classify_intent`/`_generate_answer` thread them through as real prior `user`/`assistant` turns in the `messages` array (not spliced into a system prompt as text — the standard, injection-safe shape for conversational context with a chat-completions API). The classify call gets the previous answer truncated to 500 chars (classify only needs to know the topic, and this module already hit a real regression once from prompt growth exhausting its reasoning-token budget — see v1.24); the explain call gets the full text. `validate_grounded` gained a `previous_answer` parameter (additive-only, same shape as the `question` parameter from v1.30) so a correct follow-up naturally restating a figure ORLA already gave isn't wrongly flagged as an invented number. `orla_constitution.md` updated to state the memory boundary explicitly: prior exchange may resolve what a question refers to, never supply a new fact beyond what's already grounded. Frontend (`frontend/app/chat/page.tsx`) tracks the last real exchange in its own state (separate from the displayed thread, which can also hold an error message) and resets it whenever the selected business changes — a stale exchange from a different business would be actively wrong to carry over, not just stale, now that it's threaded into the AI call. 489 backend tests passing (up from 483), a clean frontend build, and live re-verification against Gate B confirming the exact previously-broken scenario now resolves correctly. |
| 1.32 | 08/08/2026 | Scoped a subset of a proposed "ORLA AI Skills, SOPs, Grounding & Personality" architecture plan (user-supplied PDF) — assessed the full plan first and found roughly two-thirds of it already built under different names (its "Findings Engine" = the existing `app/analytics/findings.py`; its "AI Skill Router" = the existing `_classify_intent`/`ALLOWED_INTENTS` dispatch, ADR-020; its "Grounding Validator" = the existing `app/ai/guardrail.py`; its OpenRouter privacy restriction = already set via `data_collection: deny`). Per direct instruction not to replace existing analytics/recommendation logic, deliberately built only the two genuinely new, low-risk pieces the user selected: **(1)** a formal, versioned Constitution — the grounding/scope rules that previously lived as an inline f-string in `_EXPLAIN_SYSTEM_PROMPT_TEMPLATE` are now a standalone `backend/app/ai/orla_constitution.md`, reviewable and diffable independent of code changes, loaded once at import time. **(2)** a Personality layer, `backend/app/ai/orla_personality.md` — warm, professional, lightly Irish, explicitly subordinate to the constitution (tone only, never changes a fact or number). Deliberately declined the plan's larger Skills/SOPs YAML framework and a rebuilt "Findings Engine"/"Skill Router" — no real scaling pain exists yet against the current ~11-intent inline dispatch, and building it now would mean duplicating already-working, already-fire-tested infrastructure. Switched the explain prompt's assembly from `str.format()` to plain concatenation (the constitution/personality files are free-form prose that may contain a literal `{`/`}`, which `.format()` would misparse as a placeholder). Deliberately NOT applied to the classify step — that prompt already hit a real regression once from growing too large for its reasoning-token budget (see v1.24); adding a second block of prose there for no functional benefit (classify has no free-text "voice" to carry) wasn't worth the risk. 483 backend tests passing, plus a new regression test asserting both governance files actually reach the explain call's system prompt. |
| 1.31 | 08/08/2026 | Fixed a real finding-detection gap reported directly: "Which products are hurting my profitability even though they sell well?" got "I don't have any profitability figures in the data" — false; per-product margin data exists, there was simply no rule that could answer this specific question shape. `evaluate_products_at_loss` only ever catches an outright net loss (`gross_profit < 0`), and `rank_products_by_margin`'s top-5/bottom-5-by-gross-profit slices can leave a strong-revenue, thin-but-positive-margin product outside both windows entirely — neither the most nor least profitable in absolute terms. New `evaluate_thin_margin_high_revenue` rule (`app/analytics/findings.py`): re-ranks the full, unsliced product list by revenue (a new 4th return value on `rank_products_by_margin`, threaded through `FinancialPerformanceSummary` internally — never exposed on the API schema, an unbounded list has no place on the wire) and flags a top-5-by-revenue product whose margin sits 10+ points below the business's own overall average, skipping anything already caught by the at-a-loss rule. Flows automatically into the dashboard's existing Findings & Recommendations section and ORLA's `findings_recommendations` chat intent — no frontend changes needed (the existing generic finding/recommendation rendering already handles any new `finding_type`). A second issue in the same report — a follow-up question ("what was the previous period?") falling back to the generic out-of-scope message — is not a new bug: ORLA is deliberately single-turn (no conversation history resent as model context, a stated cost-control decision from Stage E, documented in `frontend/app/chat/page.tsx`'s own comment) and has no memory of the prior answer to refer back to. 483 backend tests passing (up from 477), including a dedicated integration test reproducing the exact "one dominant thin-margin bestseller among otherwise-healthy products" scenario by hand-computed numbers. |
| 1.30 | 08/08/2026 | Ran a live 20-question ORLA fire test against Gate B (representative subset of a formal 56-question stress-test document) — cross-tenant isolation, hallucination resistance, and prompt-injection resistance all held with zero findings. Found and fixed five real bugs: `out_of_scope`→`forecast` recovery for reorder-shaped questions; explain-step provider failures now correctly tagged `provider_unavailable` instead of the stale prior intent; the guardrail's `validate_grounded` gained an additive-only `question` parameter so a number the user themselves supplied (e.g. "€2,000") isn't wrongly flagged when the model echoes it back; numbered-list markers ("1.", "2.") in a multi-item answer are stripped before the numeric-claim scan, fixing a 100%-reproducible false rejection on every "give me N actions" style question; and `ClassifyResult` gained an optional `horizon_days` so "the next two weeks" correctly fetches a 14-day forecast instead of always the 7-day default. Also raised explain `max_tokens` 500→800 and added currency-formatting guidance to the explain prompt. See F29's changelog entry above for full detail. 477 backend tests passing (up from 470). |
| 1.29 | 07/08/2026 | Fixed three real ORLA chat issues found via a pasted user transcript, each diagnosed live before fixing: (1) a SKU-annotated product lookup ("...WorkshopPro Lubricant Plus (SKU-00175)...") returned zero matches — the combined "name (sku)" format is exactly what ORLA's own disambiguation labels use, so `ProductRepository.search_by_name_or_sku` now also tries a trailing "(...)" annotation split from the rest of the query; (2) "what's my biggest cost/expense" had no intent to land in — new `category_breakdown` chat intent reusing the existing `app/application/category_breakdown.py`, no new calculation logic; (3) a bare many-match follow-up reply (assumed by the user to be a "lost memory" bug) was actually the guardrail correctly rejecting an ungrounded explain-step answer — ORLA never had conversation memory to lose; the many-match message now models a real example follow-up question instead of "could you be more specific?" See F29's changelog entry above for full detail. 463 backend tests passing (up from 459). |
| 1.20 | 06/08/2026 | Removed the undo-ordering guard flagged as a likely-redundant follow-up in v1.19 — confirmed, then acted on. Under the old flat-sum stock model, a reconciliation's movement was a *delta* computed against whatever the running total happened to be at write time; undoing an earlier import afterward would silently invalidate that frozen delta, which is exactly what `ImportRecordRepository.has_later_completed_inventory_import` / `ImportSupersededByLaterInventoryImport` existed to block. Since v1.19 made a reconciliation store its own absolute `resulting_quantity_on_hand` instead — current stock is recomputed fresh from whatever's in the ledger every time it's read, filtered by `event_date`, never by write order — that hazard no longer exists: deleting a movement dated on/before the latest reconciliation changes nothing (it was already excluded from the total); deleting one dated after it correctly reduces the total by its own contribution. Verified this directly by replaying the exact scenario the guard was originally built for (sale, then a later reconciliation, then undo the sale) and confirming the resulting stock is correct, not silently wrong — the bug the guard prevented simply can't happen anymore. Removed the guard method, the exception class, its API handler, and the now-untestable tenant-isolation test for the guard's business-scoping (its whole premise no longer applies); rewrote the two "undo is blocked" tests into "undo succeeds and stock is still correct" tests instead of deleting them, so the exact original bug scenario stays under regression coverage going forward. Also fixed a bug introduced and caught during this same change: an edit to `app/imports/exceptions.py` briefly left an orphaned `__init__` override sitting inside `ImportNotReversible` (a leftover from the removed exception class) that silently broke its real constructor — caught immediately by the full test suite, fixed before commit. Verified via the backend suite (308 passing) and a live walkthrough against the running Docker stack: purchase dated before a later stock count, undo request now returns 200 (previously 409), stock correctly unchanged afterward. |
| 1.21-1.23 | — | Not recorded as separate rows here — these versions' changes (the Gate B synthetic-data pass, the schema-detection tie-breaking fix, the returns-support fix, and the `provider_unavailable` fix) were documented inline in F29's own changelog entry above instead of as table rows; noted here only so the version numbering isn't a silent gap. |
| 1.24 | 07/08/2026 | Extended F29's Gate B follow-up work: ORLA can now answer questions naming a specific date/range (`explicit_date` period, resolved against the business's actual local "today") and questions about one specific product, purchase/order, or repair by name/SKU/reference (`product_lookup`/`purchase_lookup`/`repair_lookup`, a shared zero/one/many-match deterministic dispatch — never a silent guess). `_classify_intent` now returns a `ClassifyResult` dataclass instead of a bare tuple. Found and fixed a real regression during live verification: the larger classify prompt this required pushed some questions into exhausting the classify call's reasoning-token budget with no visible output left — raised `max_tokens` 300 -> 700. See F29's changelog entry above for full detail. 424 backend tests passing (up from 384). |
| 1.28 | 07/08/2026 | Reports: Download PDF button (browser print, no new dependency), a Sections table of contents, and a real bug fix — "Top recommendations"/"Action Plan" showed the generic rule text with no product name at all, missing the finding-message pairing the dashboard already had. Dashboard: category filter + labels added to Forecast's "Products to watch" and to Findings & Recommendations (`get_findings`/`get_forecast` both gained `category_id`, but whole-business rules — revenue decline, low margin, incomplete cost data, high return rate — always read unfiltered figures, per direct instruction); Findings & Recommendations (and the report's Action Plan) now render as two visually separate groups, "Business performance" and "Stock & products," rather than one mixed list. Shared recommendation-list logic extracted into `frontend/lib/findings.ts` so both pages use one implementation. See F29's changelog entry above for full detail. 459 backend tests passing (up from 455). |
| 1.27 | 07/08/2026 | Fixed the category filter's real discoverability gap, found by direct feedback right after v1.26 shipped: with zero categories anywhere yet, the filter dropdown and every category label hid themselves entirely, making "no data yet" indistinguishable from "not built." Dropdown now always renders (disabled + hinted when empty). Assigned real categories to all 180 Gate B products via a genuine no-op inventory reconciliation import, and one-time-backfilled `sell_price` for 174 of them from their own sale history (the v1.26 fix only applies to sales imported after it shipped) — Category Breakdown now shows real, non-zero figures. See F29's changelog entry above for full detail. |
| 1.26 | 07/08/2026 | Product categories: data capture (new optional "category" upload field, match-or-create against `ProductCategory`, "latest wins" assignment — matches the existing `Product.category_id` schema that had zero writer before this), per-section dashboard filters (Financial Performance + Retail Operations, each independent), and a reports Category Breakdown table (revenue, expenses = purchase cost not COGS, stock value at sell price not cost). New `InventoryMovement.unit_cost` column closes a real gap found while confirming the "expenses" formula with the user: no per-purchase cost history existed anywhere before this. Two real bugs found live and fixed: every one of 180 real Gate B products had `sell_price = NULL` (new `ProductRepository.update_sell_price`, the missing counterpart to `update_cost_price`'s own v1.11 fix); loading an already-stored report crashed the page entirely on a field added earlier this session, fixed with defensive guards rather than assuming every stored report matches today's schema. See F29's changelog entry above for full detail. 455 backend tests passing (up from 424). |
| 1.25 | 07/08/2026 | Raised `ai_daily_request_limit_per_business` (PR-5.5) from 30 to 200, per direct instruction, after diagnosing a real point of confusion: adding real OpenRouter account credit does *not* unlock a paid model or spend that credit — the configured model (`openai/gpt-oss-20b:free`) is always `$0`/call by construction, and paying only raises OpenRouter's own free-tier daily quota (50 -> up to 1000/day). The thing actually blocking testing was this app's own, much lower, unrelated 30/day cap, confirmed live (`count_today_for_business` returned exactly 30/30 for the test business). 200 stays comfortably under OpenRouter's own new ceiling with zero cost impact at the current free-tier model. Updated `test_daily_usage_cap_blocks_further_ai_calls` to read the real configured limit instead of a hardcoded 30. Recorded the real production cost-control design for later, in "Future Improvements" below — this count-based cap is explicitly *not* that; a real launch on a paid model needs a spend-based per-business cap (using the already-logged `AIRequest.cost_eur`) tied to a fixed share of the €80/month plan price, tiering a client down to a free/cheaper model past ~50% of their allotted budget rather than hard-blocking. 424 backend tests still passing. |
