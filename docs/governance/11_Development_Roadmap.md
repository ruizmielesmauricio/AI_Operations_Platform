# 11_Development_Roadmap.md

**Version:** 1.36 (Draft)
**Status:** Draft
**Phase:** Phase 1 – Company Foundation
**Author:** Founder & CTO
**Last Updated:** 08/08/2026

---

# Document Contract

## Purpose

This document defines the delivery roadmap at a governance level: the phases, what each phase must produce, and the gates that must be passed before moving on.

It is written to **map directly onto Jira epics** so that this document remains the strategic source of truth while Jira handles day-to-day execution tracking. Each phase below corresponds to an epic; each deliverable corresponds to a story.

**Jira project:** `COP` — https://mauricio-copilot-app.atlassian.net/jira/software/projects/COP/boards/1/timeline

*Note: the Jira instance is private and cannot be read from outside the authenticated session. This document does not attempt to mirror Jira's current state — it defines what the roadmap should contain, so Jira can be populated from it.*

---

## Audience

* Founder
* Engineering Team
* Future Employees
* Investors

---

## In Scope

* Delivery phases and their objectives
* Deliverables per phase, structured as Jira epics and stories
* Milestone gates and exit criteria
* Sequencing rationale

---

## Out of Scope

This document intentionally does **not** define:

* Requirements detail (see `10_Product_Requirements.md`)
* Technical implementation (see `03_System_Architecture.md`)
* Sprint-level task breakdown (lives in Jira)
* Commercial forecasting (see `09_Business_Model.md`)

---

## Related Documents

* 10_Product_Requirements.md
* 15_Customer_Discovery.md
* 12_Decision_Register.md

---

# Executive Summary (TL;DR)

The roadmap deliberately separates **product validation** from **commercial launch**. The founder builds a working prototype and validates it with real bike shops *before* taking on company registration, legal costs, and production billing.

Seven phases, four gates. Nothing proceeds past a gate without evidence, not optimism.

The most important sequencing decision: **customer discovery comes before serious build.** Phase 1 interviews should inform what gets built in Phase 2, not the other way round.

---

# Jira Mapping Convention

| This Document | Jira |
|---|---|
| Phase | Epic |
| Deliverable | Story |
| Acceptance criterion | Story acceptance criteria |
| Gate | Epic-level "Definition of Done" / milestone on timeline |

**Suggested Jira labels:** `phase-0` … `phase-7`, `gate-a` … `gate-d`, `governance`, `backend`, `frontend`, `data`, `ai`, `billing`, `discovery`

---

# Phase 0 — Documentation and Decisions

**Epic:** `COP — Phase 0: Foundation Documentation`
**Status:** Substantially complete

| Deliverable | Status |
|---|---|
| Company Constitution | Complete |
| Project Vision | Complete |
| Operational Domains | Complete |
| System Architecture | Complete |
| Technology Stack | Complete |
| AI Architecture | Complete |
| Database Design | Complete |
| Deployment Guide | Complete |
| Cost Analysis | Complete |
| Business Model | Complete |
| Product Requirements | Complete |
| Development Roadmap | Complete (this document) |
| Decision Register consolidated | Complete |
| Branding Strategy | In progress |
| Repository structure created | Complete |

**Outstanding housekeeping:**

- [x] Consolidate the former decision registers into `12_Decision_Register.md`
- [x] Update `01_Project_Vision.md` to remove the import-template guidance (superseded by PD-006; replaced by the "Low-Friction Use" product principle)
- [x] Update `06_Database_Design.md` and `10_Product_Requirements.md` if ADR-016 is accepted — done 04/08/2026; `10_Product_Requirements.md` reviewed and needed no change (no repairs-specific content exists there)
- [ ] Generate remaining individual decision files in `docs/decisions/` — partially started 04/08/2026: ADR-016, ADR-022, ADR-023 now have files; full historical backfill remains

---

# Phase 1 — Customer Discovery

**Epic:** `COP — Phase 1: Customer Discovery`
**Priority:** Highest. Nothing else matters until this is done.

| Deliverable | Acceptance Criteria |
|---|---|
| Interview script finalised | `15_Customer_Discovery.md` reviewed and ready to use |
| Interview target list built | 25–30 Irish independent bike shops identified with contact details |
| 15–20 interviews completed | Each recorded in `docs/business/interviews/` |
| POS/export formats catalogued | Which systems shops actually use, and what their exports look like |
| Sample data files collected | At least 5 real (anonymised) export files for schema-detection testing |
| Pain point register populated | Top 10 pain points ranked by frequency and severity |
| Assumptions scored | Every assumption in `15_Customer_Discovery.md` marked Validated / Not / Unsure |
| Willingness-to-pay evidence gathered | Test commercial response to the accepted €80/month price |
| Module priority ranked | Which of the five domains customers actually value most |

**Why sample data files matter more than they look:** PD-006 commits the platform to schema-agnostic ingestion. That is impossible to build or test without real, messy export files. Collecting these during interviews is the single highest-value technical output of Phase 1.

---

## Gate A — Problem Validation

Proceed only when:

- Interviews confirm repeated, specific pain points (not vague dissatisfaction)
- Businesses can and will provide usable data
- At least one module shows clear, articulated value
- At least some evidence exists that owners would pay for it

**If Gate A fails:** stop and reconsider the segment or the product, before writing production code.

---

# Phase 2 — Technical Prototype

**Epic:** `COP — Phase 2: Technical Prototype`
**Environment:** Local or private only. No customer data, no billing.

| Deliverable | Notes |
|---|---|
| Repository scaffolding | Backend, frontend, Docker, CI skeleton |
| Authentication | Supabase Auth integration; session verified by API |
| Business creation and template selection | Bicycle shop template first |
| Tenant isolation | Enforced and tested from day one, not retrofitted |
| File upload to object storage | Signed URL flow via R2 |
| **Schema detection engine** | Alias dictionary + structural heuristics; tested against real Phase 1 files |
| Column mapping confirmation UI | Plain language, sample values shown (PR-2.4) |
| Mapping profile persistence | Second upload requires no input (PR-2.5) |
| Validation and normalisation | Deterministic; plain-language errors (PR-2.8) |
| Transactional import | Idempotent, reversible |
| Core KPI calculation | Revenue, gross margin, stock cover, repair turnaround |
| Basic dashboard | ECharts, browser-rendered |
| AI explanation layer | Behind gateway; structured input only |
| Import deletion / undo | PR-2.11 |
| Unit tests for all formulas | ED-007 |
| Tenant isolation integration tests | ED-008 |

**Explicitly deferred from Phase 2:** billing, email, monitoring, multi-user roles, forecasting.

---

## Phase 2/3 Detail — Prototype Build Sequence

The founder-agreed, ordered build checklist for the free-tier prototype (spans Phase 2 and pulls forward a few Phase 3 items — conversational agent, alerts, Stripe test mode — since testing the full service offering end-to-end was judged more valuable at this stage than strictly following the phase boundary). This is the sequence actually followed; update it in place as items complete rather than tracking progress elsewhere.

**Stage A — Foundation**
- [x] A1. Create the database (local Postgres via docker-compose)
- [x] A2. Create the full schema — shared core, canonical entities, bicycle-shop template extension (`06_Database_Design.md`)
- [x] A3. Authentication (Supabase Auth — backend JWT verification + frontend client)
- [x] A4. Business signup + template selection (PR-1)
- [x] A5. Tenant isolation enforcement + automated tests (PR-6.1/6.2, ED-008) — built now, not retrofitted

**Stage B — Getting real data in**
- [x] B6. Upload data interface (signed URL to R2)
- [x] B7. Schema/column detection engine — alias dictionary + structural heuristics, tolerant of any POS export format (PR-2, the "critical section") — supports both `sales` and `inventory` entity types (entity type chosen explicitly at upload time, not auto-detected)
- [x] B8. Clean data — deterministic normalisation, validation, plain-language rejected-row reporting, transactional import, undo (PR-2.6-2.11) — includes SKU/name product matching (auto-create on first sight) and inventory_movements recording per sale, needed for stock-level/popularity tracking; frontend "Run import"/"Undo" trigger UI added, and PR-2.2's manual header-row-picker escape hatch built for files auto-detection can't confidently place — both verified live end-to-end
- [x] B8b. Inventory upload entity type — stock-count snapshot uploads reconcile against derived stock (stock is never stored directly, only summed from `inventory_movements`) by writing a single `adjustment` movement per product equal to `uploaded_count - current_derived_stock`; guards against corruption if a sales import is undone *after* a later inventory reconciliation has baked its effect into a subsequent adjustment (blocked with a clear error instead of silently under/over-counting stock) — verified via automated tests and a live end-to-end browser walkthrough exercising the undo-guard scenario for real
- [x] B8c. Purchases and repairs upload entity types, plus a restock-freshness indicator. **Purchases** — restocking as its own transactional entity type (the `"purchase"` reason on `inventory_movements`, reserved since the model was first designed, now has its first writer): each row an independent `+quantity_received` movement, `unit_cost` (optional) seeds a new product's `cost_price` or overwrites an existing one via a new `ProductRepository.update_cost_price` — the first update path `cost_price` has ever had. **Repairs** — a shop's own workshop log/export ingested the same way as sales, one row per finished repair into `production_events` (`event_type="repair"`, `status="completed"`, no parts-consumed detail in v1); `customer_id`/`performed_by_id` deliberately left NULL, same data-minimisation precedent as sales never getting customer-matching. `run_import`/`undo_import`'s entity-type dispatch refactored from a hardcoded if/else into dispatch tables (pure refactor, no behavior change for `sales`/`inventory`) now that there are four entity types. Found and fixed a real bug during testing: the undo-ordering guard was blocking `repairs` undos unconditionally whenever *any* later inventory reconciliation existed, even though repairs write zero `inventory_movements` and can never be affected by one — narrowed the guard to exempt `repairs`. A new read-only `GET /businesses/{id}/uploads/freshness` endpoint and uploads-page indicator show "last uploaded N days ago" per entity type (no push/email notification — no scheduler exists yet, Stage D17). Verified via the full test suite (239 passing) and a live end-to-end browser walkthrough: uploaded and imported a purchases file (cost seeded/overwritten correctly, `reason="purchase"` movements written) and a repairs file (`production_events` rows, no stock impact), then ran an inventory reconciliation and confirmed the exact undo-guard fix live — undoing the purchases import was correctly blocked (409), undoing the repairs import was correctly allowed, exactly as the bug fix intended.
- [x] B8d. Remap a confirmed-but-not-yet-run upload. Found live: a user testing B8c's purchases/repairs mapping picked the wrong column for a field, confirmed it, and had no way back — `detect-mapping`/`confirm-mapping` refused to run again once `Upload.status` left `"uploaded"`, and the frontend had no button for it anyway. Fix scoped deliberately to `status == "mapped"` (confirmed, not yet run) — an upload whose import already ran can't be remapped even after undo, since the source file is deleted from R2 right after a successful import (ADR-008); that case's recovery stays "re-upload the file," unchanged. `detect_mapping_for_upload` skips the saved-profile auto-reuse fast path on a remap (`upload.status == "mapped"` is itself the signal) — without this, `confirm_mapping`'s own `upsert()` would have already saved the wrong mapping as this source's profile, and reuse would silently reapply the same mistake before the user ever saw the form again. New `ImportRecordRepository.delete_for_upload` replaces the previous attempt's record on a remap, preserving the "exactly one `ImportRecord` per Upload" invariant `get_for_upload`/`map_by_upload_id` both assume. New "Remap" button on the uploads page next to "Run import." Verified via the full test suite (250 passing) and a live browser walkthrough: confirmed a deliberately wrong mapping (SKU/product-name swapped), remapped, confirmed it re-detected fresh rather than reapplying the wrong saved profile, ran the corrected import, and verified the resulting `sale_items` had the right SKU/product name, not swapped.

**Stage C — The actual product value**
- [x] C8b. Schema foundation for multi-vertical calculations (ADR-016, ADR-022, ADR-023) — `repairs`/`repair_parts_used` (bicycle-specific, unused) replaced by the canonical `production_events`/`production_event_inputs`/`production_event_outputs` pattern, covering bike-shop repairs and cafe kitchen production/recipes under one shape; added canonical `inventory_lots` (lot/batch + expiry-date tracking, requested by a real pharmacy prospect) and a pharmacy `prescription_details` template extension. Models + migration only — no repository/service/API/calculation logic yet, that's C9. Verified via the full test suite, the dedicated migration-chain structural test, and applying to local dev Postgres.
- [x] C9. Core calculations — Retail + Financial Performance, unit-tested per formula (PR-3, ED-007). Workshop (repair) profitability deferred until `production_events` gets a real-time entry workflow and starts generating data. Revenue, gross margin (with a PR-3.5/3.6 cost-data-completeness flag and PR-3.6 revenue/profit distinction), and top/bottom-margin products cover Financial Performance; stock cover, sell-through rate, dead-stock detection, and inventory value at cost cover Retail Operations. Pure formulas live in `app/analytics/`, orchestration in `app/application/`, exposed via two new tenant-scoped GET endpoints. Verified via unit tests on every formula plus an integration test against a real (SQLite) database.
- [x] C10. Findings & recommendations engine (PR-4). Six deterministic rules over C9's output (revenue decline, low gross margin, incomplete cost data, products selling at a loss, low stock, dead stock), each producing a `Finding` (evidence, severity, rule version — PR-4.1/PR-4.4) mapped to a `Recommendation` from a fixed approved library (PR-4.2), ranked by severity then a dollar-denominated impact score (PR-4.3). Not persisted — computed on demand, same as C9, via a new `GET /businesses/{id}/analytics/findings`. The `low_stock` rule (`app/analytics/findings.py::evaluate_low_stock`) is deliberately standalone (just a stock-cover list + threshold, no DB/summary object) so C12 reuses it directly instead of reimplementing the threshold logic. Verified via unit tests per rule plus an integration test seeding a scenario that trips every rule at once — which caught a real bug during design (a loss-making product with too few peers to appear in `bottom_margin_products` was being missed; fixed by scanning `top_margin_products` too).
- [x] C11. Charts/dashboard section, browser-rendered from structured API payloads (PR-3.3), with drill-down scoped to the per-product/per-repair breakdown rows the existing analytics endpoints already return (PR-3.4) — full drill-down to individual `Sale`/`SaleItem` rows deferred, no endpoint lists those yet. Also added, since B8c's repairs upload now writes real `price_charged`/`labour_cost` per completed job: a **Workshop Performance** calc mirroring C9's pattern — `ProductionEventRepository.aggregate_completed_repairs_in_range`, `app/analytics/workshop.py` (labour-only profitability, honestly caveated the same way C9 caveats missing cost data — no `ProductionEventInput`/parts-cost writer yet), `app/application/workshop_performance.py`, exposed via `GET /businesses/{id}/analytics/workshop-performance`. Frontend: `frontend/app/dashboard/page.tsx` — Financial Performance, Retail Operations, Workshop Performance, Findings & Recommendations, and Active Alerts sections, a shared period control, ECharts bar charts (`echarts`/`echarts-for-react`, per ADR-010) for margin and stock-cover breakdowns, plain-language metric definitions on hover (PR-3.7) and data-completeness notes (PR-3.5). Extracted the business-selection logic shared with the uploads page into `useBusinessSelector`, and added the first shared nav (`AppNav`) now that there are two real app pages to move between. Verified via the backend test suite (246 passing, including new Workshop unit/integration tests), a clean frontend production build, and a live browser walkthrough against the running Docker Compose stack confirming all five sections render correctly (including a genuinely empty-data business, proving the empty-state paths) with real API responses.
- [x] C12. Low-stock alerts — real-time, deterministic (PD-009, PR-9). Reuses C10's `evaluate_low_stock` rule unmodified — `app/application/alerts.py::refresh_low_stock_alerts` groups touched products by resolved threshold (product override -> category override -> default, `resolve_low_stock_threshold`) and calls it per group. Fires from `app/imports/importer.py`'s existing post-commit points in both `run_import` and `undo_import` (try/except-wrapped, never fails an otherwise-successful import), for exactly the products that import/undo touched. Persisted to the existing `alerts` table with create/update-in-place/resolve state-transition logic (no duplicate alerts for an ongoing condition), exposed via `GET /businesses/{id}/alerts`. Added `low_stock_threshold_days` (nullable) to `Product`/`ProductCategory` and a composite `alerts` index for PR-9.3. **Follow-up flagged, not built here:** no product-management API/UI exists yet to let an owner actually set those threshold columns — the data model and rule resolution are ready, the settings screen isn't. Verified via unit tests (threshold resolution, JSON-evidence conversion) and integration tests (repository CRUD, full create/update/resolve cycle, and a real `run_import`/`undo_import` round-trip creating then resolving a persisted alert).
- [x] C13. Simple forecasting baseline (seasonal/moving average) — deterministic (PR-10), no AI. `app/analytics/forecasting.py::compute_baseline_forecast` picks day-of-week seasonal naive averaging once at least 3 full weeks of history exist, falling back to a plain moving average between 14-20 days, and refuses to forecast at all under 14 days (`insufficient_data`, no fabricated number). Confidence range is the sample standard deviation of the historical values behind each forecast day (`Decimal.sqrt()`, never float). Covers both business-wide revenue (`GET /businesses/{id}/analytics/forecast`) and per-product unit demand, the latter feeding a `suggested_reorder_quantity` (confidence band's high end minus current stock) — explicitly labelled as a starting point, not a lead-time/safety-stock-aware reorder system. A product/business only starts its averaging window from its own first-ever observed sale, not a synthetic zero-filled `MAX_LOOKBACK_DAYS` window — the first version of this logic zero-filled blindly back 90 days regardless of actual history, which would have made the insufficient-data gate never trigger for a genuinely new business (caught before shipping, during design). **Known, stated limitations:** only day-of-week seasonality is attempted — monthly/yearly seasonality needs 1-2+ years of history no new pilot business will have yet, which is exactly what C14/C15's weather-signal augmentation is for; the reorder suggestion has no supplier lead-time or safety-stock model. Verified via the backend suite (296 passing, including hand-computed variance fixtures), a clean frontend build, and a live walkthrough against the running Docker stack.
- [ ] C14. Weather data API integration (free tier — relevant to Irish cycling demand)
- [ ] C15. ML forecasting models (weather-enhanced, built on the Stage C13 baseline)
- [ ] C16. Test forecasting model results against held-out data

**Stage D — Reports**
- [x] D17/D18. Scheduled weekly/monthly reports (PR-8, PD-007, ADR-019) — built together since one engine generates both (only the comparison period differs). **Scheduler**: a new, separate lightweight `scheduler` service (`docker-compose.yml`, same backend image, `python -m app.scheduler` entrypoint) polling every 15 minutes — no Redis/job-queue dependency added. Its single reconciliation tick (`app/scheduler/tick.py::run_tick`) deliberately does three PR-8 jobs at once: scheduling (PR-8.1/8.2 — generates a business's previous completed week/month once its local 08:00 generation moment has passed), retry (PR-8.9 — a `failed` row under a 5-attempt cap is still "not completed," so the next tick just tries it again), and recovery (PR-8.10 — a tick resuming after any downtime notices and catches up, no separate recovery process needed). **Generation** (`app/application/report.py::generate_report`) assembles one JSON payload per report entirely by calling the existing C9-C13 application-layer functions for the resolved period (`get_financial_performance`, `get_retail_operations`, `get_findings`, `get_forecast`, and `get_workshop_performance` gated on `Business.template == "bicycle_shop"` — omitted, not faked, for any other template, satisfying PR-8.3's own requirement) — no calculation logic of its own, no AI anywhere (PR-8.7), the payload sections serialized via the exact same Pydantic schemas the dashboard API uses so a report and the live dashboard can never disagree about a number's shape. Two small new deterministic additions: `app/analytics/retail.py::classify_movers`/`compute_inventory_turnover` (fast/slow-mover buckets off existing stock-cover data, and a turnover ratio explicitly caveated as using current, not period-average, inventory value) and `app/analytics/report_narrative.py::build_executive_narrative` (a handful of template sentences with numbers plugged in — not free text generation). Idempotency (PR-8.8) is a real DB unique constraint on `(business_id, report_type, period_start)`, not just an application convention. `Report.status` moves `pending` -> `completed`/`failed`; a row is never hard-deleted even after its 7-day customer-facing availability window (PR-8.5) expires — it's its own audit record (PR-8.12), just excluded from list queries. New `GET /businesses/{id}/reports` (list, active only) and `GET /businesses/{id}/reports/{report_id}` (full payload) routes, and new `frontend/app/reports/page.tsx`/`frontend/app/reports/[id]/page.tsx` pages added to `AppNav` — the detail page reuses the dashboard's own `Chart`/`Section`/`Stat` components and chart-option builders (newly extracted into `frontend/components/Section.tsx`/`frontend/lib/chartOptions.ts`/`frontend/lib/format.ts` so both pages share one implementation, not two). **Explicitly deferred, not silently solved:** revenue by category/brand/supplier and the "Operational Opportunities" section (no such data is ingested by any upload path yet); a Pharmacy near-expiry section (`inventory_lots` from ADR-022 has a schema but no writer — a real ingestion feature, not a report-engine task); any business-specific section beyond Bike Shop; PDF/Word export (PR-8.6 is explicitly on-demand/decoupled — in-app viewing only for this pass); richer chart types from the original mock (treemap, Pareto, heatmap) beyond the existing bar/line patterns. Verified via the backend suite (339 passing, including new unit tests for the mover/turnover/narrative/period functions, integration tests for `generate_report` and `run_tick`, and a tenant-isolation test), a clean frontend production build, and a live walkthrough against the running Docker stack: applied the new migration to real dev Postgres, started the `scheduler` container fresh, and watched it generate real weekly/monthly reports end-to-end for both existing dev businesses from actual imported history — then confirmed a second tick left them untouched (`already_done: 4, generated: 0`), proving idempotency live, not just in tests.

**Stage E — The AI layer (deliberately last — it only explains what Stages C/D already calculated)**
- [x] E19/E23/E24/E25. AI Provider Gateway + business-data Q&A chat (PD-008, ADR-020, PR-5.1/5.3/5.6) — built as one conversational agent rather than separate pieces, since a chat window was the user's explicit ask over a one-off "explain this report" button. **Gateway** (`backend/app/ai/client.py`, the only file importing an AI-provider HTTP SDK, per CLAUDE.md): connects to OpenRouter's OpenAI-compatible chat-completions endpoint via `httpx` (promoted from a dev-only to a main dependency), sets `provider: {"data_collection": "deny"}` on every request — OpenRouter's own concrete mechanism for restricting routing to providers that don't retain/train on request data, operationalizing 05_AI_Architecture.md's "EU regulatory fit" requirement rather than a per-model judgment call — and passes an optional `models` fallback array for free failover. **Routing, per ADR-020** ("route every question through an intent classifier that selects one of a fixed, approved set of deterministic query functions... before any AI generation occurs"): `app/ai/service.py::answer_question` runs a two-call pipeline — a small classify call constrains the model's output (via JSON mode plus server-side re-validation against a fixed Python enum, never trusted blindly) to one of eight approved intents, each mapping straight to an existing C9-C13/D17-18 application function (`get_financial_performance`, `get_retail_operations`, `get_workshop_performance`, `get_forecast`, `get_findings`, or the latest stored `Report`) or a static glossary; then a second call phrases the answer from only that fetched, real data. A cheap deterministic keyword pre-check (`app/ai/glossary.py::match_definition_question`) catches obvious "what does X mean?" questions before any AI call at all — genuinely zero cost, not just a cheap one. **Guardrail** (`app/ai/guardrail.py`, PR-5.3, pure functions with no I/O): every numeric claim in an AI answer is extracted and must equal some number found anywhere in the structured context it was given (Decimal equality, so "39.9%" matches "39.90" but a rounded "€11,908" does not match a true 11907.76 — deliberately fails closed on any ambiguity); a claim that doesn't trace back gets the whole answer rejected in favour of a safe fallback message, never shown to the user. **Usage & degradation** (PR-5.4/5.5): a per-business rolling-24-hour request cap (`AIRequestRepository.count_today_for_business`, default 30/day) blocks further AI calls with a clear message, checked before any call is dispatched; any OpenRouter network failure/timeout is caught and degrades to a friendly "temporarily unavailable" message, never a 500 — nothing else on the page depends on this succeeding. Every real AI call (successful or not) is logged to the previously-unused `ai_requests` table (`AIRequestRepository`, its first writer). **Frontend**: new `frontend/app/chat/page.tsx` ("Ask ORLA" in `AppNav` — ORLA is this assistant's product name, used throughout its user-facing copy and system prompt) — a plain single-turn message thread (no conversation history resent as model context, a stated limitation to keep token cost down), reusing the existing `apiPost` pattern, no new chart/streaming infra. **Explicitly deferred, stated not solved:** E20/E21/E22's full vision — dynamic cost/quality-threshold model auto-selection — wasn't built; the model is a single plain config value (`openrouter_model`, trivially swappable) rather than a scored selector, since no real evaluation test set exists yet to justify one algorithmically (05_AI_Architecture.md's own open question) — ADR-015 stays "Proposed." E26/E27 (the product/support lane) is untouched — it needs real help-content to retrieve from, which doesn't exist yet. Multi-turn conversation memory isn't built. **Live-verified end-to-end once a real key was added** (not just mocked): queried OpenRouter's real model catalog directly and found almost every free-tier model requires accepting "free model training" as the price of being free — `data_collection: "deny"` correctly filters those out, narrowing the live default to `openai/gpt-oss-20b:free` (a reasoning model with real hidden-token overhead, fine for testing, worth swapping for a plain instruct model at launch per the cost goal) plus two verified-compliant fallbacks. Live testing caught and fixed two real bugs the mocked suite couldn't: (1) `AIProviderError` wasn't surfacing the actual response body, making a live failure undebuggable — fixed to include it; (2) the guardrail rejected a genuinely correct, fully-grounded answer because the model phrased a decline as "down 35.7%" while the real data stored `-35.7` — the literal minus sign was never in the prose, exactly how this codebase's own `report_narrative.py` already phrases a decline (`abs(change_pct)` + the word "decreased"). Fixed by also accepting a negative context value's absolute form as grounded, with a new regression test. A follow-up real question — "what should I order this week?" — surfaced two more: (3) the classifier picked `findings_recommendations` (which has no per-product order quantities) over `forecast` (which does) for a reorder question — fixed by adding a short per-intent guide to the classify prompt describing what data each one actually contains; (4) the resulting answer named specific products ("CityRoll MTB 200") and got rejected anyway — a product name containing a model number is legitimate context, but the guardrail only recognised a value as numeric if the *entire* field parsed as one, so a number embedded inside a name was invisible to it. Fixed by also extracting embedded numbers from non-UUID strings (UUID-shaped strings are excluded — their digit runs are structurally meaningless and would only dilute protection against a real invented number). Debugging that also surfaced the actual cost problem: this test business's un-trimmed `forecast` context (152 products x a 7-day daily curve each) was 141,660 characters, and its stored report payload (which nests the same forecast) was 300,573 characters — a real explain call logged **69,006 prompt tokens and still failed**. Added `app/ai/service.py`'s `_trim_forecast`/`_trim_retail_operations`/`_trim_findings`/`_trim_report_payload` — capping large per-product lists to the most urgent/highest-impact rows (mirroring the dashboard's own `.slice(0, 15)` display cap, applied before the data reaches the model rather than only at render time) and dropping chart-only day-by-day curves entirely. The same question re-run afterward: correctly classified, grounded, real reorder quantities for correctly-named products, using 2,268 prompt tokens — a 97% reduction. That trimming raised an immediate follow-up question: capping a list means ORLA is no longer looking at the whole picture, so what happens if someone asks for a full/complete list, or a count, of something that got capped? `_cap_rows` now adds a `{field}_shown_of_total` note ("15 of 152") to the trimmed context whenever truncation actually happens (and never when it doesn't) — a real, grounded fact the model can cite. But live testing showed a model asked point-blank for "the full list" answered correctly from the 15 shown rows without mentioning the other 137 existed — a prompt is a hint, not a guarantee, the same reasoning already behind the numeric guardrail. So the disclosure is enforced deterministically instead: `_append_truncation_disclosure` always appends a plain-Python sentence pointing to the Dashboard/Reports page whenever any `_shown_of_total` note exists anywhere in the context (including nested inside a stored report), regardless of what the model itself said — verified live afterward, both when the model already disclosed it (a little redundant, by design — the backstop doesn't know or trust that it did) and when it didn't. Two direct follow-up requests refined this further: (1) reorder suggestions should be ordered by urgency, since that's the actually valuable ordering — rather than trust the model's prose to preserve the order it was given (already shown unreliable above), `app/ai/service.py::_build_priority_order_list` deterministically formats every product needing reorder in guaranteed urgency order (get_forecast's own sort), built from the *untrimmed* product list (free — it's local formatting after the AI calls are done, not another prompt) so it's genuinely complete unless it itself exceeds a much higher 30-row display cap, gated behind a cheap keyword check (`_looks_like_a_reorder_question`) so a plain revenue-forecast question doesn't get an unrelated product list appended. (2) A clickable link to the Dashboard/Reports for the full picture — implemented as a small fixed-enum `links` field (`AnswerResult`/`ChatResponse`, values from `{"dashboard", "reports"}` only, computed deterministically from whether anything was actually truncated) rendered by `frontend/app/chat/page.tsx` as real `<a>` elements, deliberately never by treating any part of the model-generated `answer` text as HTML (an XSS risk this design avoids entirely by construction, not by sanitising). Checking that link would actually deliver "the full list" surfaced one more real gap: the Dashboard's and Report's own Forecast/Purchasing-Recommendations *tables* also capped display at 15 (`frontend/app/dashboard/page.tsx`, `frontend/app/reports/[id]/page.tsx`) — pointing someone there wouldn't have solved anything. Removed that cap on both (the underlying API already returns the complete list; only the table rendering was artificially short) — left the stock-cover *chart's* cap alone, since a 150-bar bar chart is genuinely unreadable regardless of correctness, unlike a scrollable table. 369 backend tests passing (all of the above with regression tests), a clean frontend build, and `backend/scripts/verify_ai_chat_live.py` for future live spot-checks.
- [ ] E20. Test candidate AI models under GDPR/EU compliance — partially addressed: every request already enforces OpenRouter's `data_collection: "deny"` regardless of which model is selected (see above), but no real comparison of candidate models has been run — there is no live OpenRouter key yet to test against.
- [ ] E21. Select the quality/price threshold (ADR-015) — deferred; no evaluation test set exists yet to define one.
- [ ] E22. Code automatic cheapest-model-above-threshold selection — deferred; the model is a static config value for now (see above).
- [ ] E26. Build support knowledge base content
- [ ] E27. Conversational agent — support lane, with human handoff when unresolved (PR-5.7)

**Stage F — Payments and wrap-up**
- [x] F28. Simulate Stripe payments (test mode — PR-7)
- [x] F29. Test prototype end-to-end (Gate B checklist, below) — a genuine pass, not a synthetic smoke test: created a fresh business and imported four real files (stock, sales, purchases, repairs — 8,255 total rows) from a standardized synthetic-data source (`TEST_SYNTHETIC_DATA`), replacing the ad-hoc test fixtures accumulated across earlier stages, which had themselves started polluting shared dev businesses with debris (five zero-activity products named after their own test scenarios — deleted). Against each Gate B criterion: **real files import end-to-end** — all 4 did, with sensible, explainable rejections, not failures (undelivered purchase orders, a handful of malformed sales rows). **Calculated metrics verifiably correct** — hand-calculated total sales revenue from the raw CSV and traced a small discrepancy against the API to its exact root cause (the importer's own documented per-row cent-rounding when reconstructing a unit price from an authoritative total) — confirmed correct, not a bug. **Dashboard findings understandable** — read the full rendered dashboard end to end. **Tenant isolation** — covered by the existing automated suite. **Schema detection** — this is where the pass earned its keep: stock and sales auto-mapped 100%, but purchases and repairs each had a real, distinct problem, both found and fixed (see below), re-verified against the same real files afterward at 100% auto-mapped, zero manual intervention.

  **Fix 1 — schema detection could confidently pick the wrong money column** (`app/imports/detection.py`). Repairs' `price_charged` auto-mapped to `labour_amount` instead of `total_amount`; `labour_cost` auto-mapped to `labour_hours` — an hours-worked column, not a cost, which would have silently corrupted every workshop margin figure computed from it. Root cause: when multiple structural candidates tied at the exact same score, the winner was whichever column happened to appear first in the file — an accident of Python's stable sort over a column-ordered list, never a deliberate ranking. Fixed with three changes: (1) a genuine tie among a money field's candidates is now never auto-picked — the field comes back unmapped, with every tied candidate still shown at its real confidence for a human to choose between, mirroring this file's own pre-existing identifier-header veto reasoning ("a wrong assignment silently corrupts every margin number downstream... hence a veto, not a reduced score"); (2) a soft "looks like currency" signal (fraction of values with genuine 2-decimal-place formatting) blended into the score, separating an hours/rate column from a real money column when the header's token overlap alone can't; (3) token overlap for money fields is now proportional (fraction of the field's token set matched) rather than binary any-match-at-all, so a strong multi-token match ("Total Amount" matching both "total" and "amount") correctly outranks a weak single-token one ("Labour Amount" matching only "amount") — this alone resolved `price_charged` cleanly, no tie left to break. Plus the missing aliases the real files also exposed (`purchases.purchase_reference` had zero candidates for `purchase_order_id`/`supplier_order_reference`/`supplier_invoice_number` — none matched anything in the alias list, which is exact-string-match only; `repairs.description`/`price_charged`/`labour_cost` gained `"issue description"`/`"total amount"`/`"labour amount"` respectively). 16 new regression tests.

  **Fix 2 — a sale row with a negative quantity was rejected outright, silently excluding real return/refund data from every calculation** (`app/imports/importer.py`). 211 of 5,798 rows in the synthetic sales file were exactly this shape — negative-quantity "Sale" rows (the file's own unused `transaction_type` column confirms them as `"Return"`), a common real-world POS export pattern, previously just discarded as `non_positive_quantity`. Fixed to accept `quantity < 0` as a return (only an exact zero is still rejected, as `zero_quantity`) — and it turned out almost nothing else needed to change: revenue/margin/stock math were *already* sign-agnostic (every downstream `SUM()` has no sign filter), and `InventoryMovement.REASONS` already reserved an unused `"return"` value, with a complete-but-never-written `Return` model/table from the very first migration — deliberately scaffolded and left for this. New `ReturnRepository` (`sum_refund_amount_in_range`/`count_in_range`, mirroring `SaleRepository`'s exact shape) feeds a new `ReturnsSummary` (`gross_revenue`/`returns_amount`/`return_count`/`net_revenue`/`return_rate_pct`) — a new sibling dataclass, not a change to `RevenueTrend`, added to `FinancialPerformanceSummary`/`FinancialPerformanceOut` and flowing automatically into the dashboard, the stored report payload, and ORLA's `financial_performance` chat context (all three already reuse this one schema — no changes needed in any of them beyond adding "returns/refunds" to ORLA's own intent-routing guide, found live: a "how much have I refunded?" question initially came back `out_of_scope` since the guide didn't mention the topic existed). New `evaluate_high_return_rate` finding (>10% of gross revenue refunded), matching C10's existing six-rule pattern exactly. Live-verified end-to-end against the real Docker stack: imported a fresh return alongside a normal sale, confirmed the `Sale`/`Return`/`InventoryMovement` rows were all individually correct, confirmed the dashboard's new disclosure line, the finding, and ORLA's grounded answer all matched. **Explicitly not addressed**: `rank_products_by_margin`'s pre-existing exclusion of zero/negative-revenue products from top/bottom ranking (a product whose only activity was a return could be absent from that list — unchanged, noted not fixed); purchase-side returns (to a supplier) are a different, unaddressed concept — this is sales-side customer returns only, matching what the real data actually contained.

  A follow-up live check surfaced one more real bug: OpenRouter's free-tier *daily* quota (separate from this app's own 30/business/day cap) was exhausted from the session's own testing, and every question — including perfectly in-scope ones like "how much was my revenue on the 24th?" — came back with the generic "I can't help with that topic" message. Root cause: `_classify_intent` silently mapped a failed provider call (network error, rate limit, timeout) to the exact same `"out_of_scope"` value a genuine "the model looked at this and it's not about your business" verdict produces — conflating "your question is out of scope" with "the AI service is down right now," two very different situations the user needs told apart. Fixed with a distinct `"provider_unavailable"` sentinel, routed to the existing `_UNAVAILABLE_MESSAGE`/`grounded=False` path (PR-5.4) instead — live-reverified against the real (still-exhausted) 429 afterward: an in-scope question now honestly says ORLA is temporarily unavailable, not that the topic is unsupported.

  A further direct ask — "clients will have questions about dates, days, specific amounts, products, orders, deliveries, repairs" — surfaced a real coverage ceiling: ORLA could only answer *aggregate* questions over 3 fixed periods (recent/last-week/last-month), with no way to ask about a specific date or one specific product/purchase/repair by name or reference. Research first confirmed the underlying data already supported this per-record, just never made queryable: `InventoryMovement` (a purchase is one row) already stored `purchase_reference`/`event_date`/`quantity_delta` per row, and `ProductionEvent` (a repair) already stored `repair_reference`/`opened_at`/`price_charged`/`labour_cost` — a schema change was never needed, only new repository search methods and classify-prompt coverage. **Explicit dates**: `ALLOWED_PERIODS` gained `"explicit_date"`; the classify call's JSON gained optional `start_date`/`end_date`, extracted by telling the model today's actual date (business timezone) in the system prompt — otherwise it has no way to resolve "the 24th of June" to a real year. Never trusted blindly: a new `_parse_explicit_dates` parses via `date.fromisoformat`, enforces `start <= end`, and bounds both to a sane 10-years-back/1-year-forward window — any failure falls back to `default_recent`, the same fail-closed posture every other classify field already had. **Three new per-record lookup intents** — `product_lookup` (new `ProductRepository.search_by_name_or_sku`, case-insensitive substring on name, exact-normalized match on SKU — deliberately not fuzzy on SKU, a partial match there is usually noise not a real hit), `purchase_lookup` (new `InventoryMovementRepository.list_purchases`, tries a purchase/PO reference first, falls back to resolving the term as a product name), `repair_lookup` (new `ProductionEventRepository.find_repairs`, tries a repair/job reference first, falls back to a description substring match — repairs have no stored customer name in this data at all, so a "which repairs did customer X have" question is correctly routed `out_of_scope` rather than guessed at). All three share one new dispatch pattern (`_dispatch_lookup`): zero matches is an honest "couldn't find X" (zero further AI cost); several matches is a deterministic "found several matching X — which one?" capped at 5 (also zero further AI cost — never silently guesses, the same reasoning behind this session's numeric guardrail); exactly one match proceeds through the normal fetch-explain-guardrail pipeline like every existing intent, just with a far smaller single-record context. `_classify_intent`'s return grew from a bare 3-tuple to a small `ClassifyResult` dataclass (`intent`/`period`/`start_date`/`end_date`/`metric`/`search_term`) now that the field count no longer fit readable positional unpacking. Live testing against the real Gate B Synthetic Shop business caught one more real regression before it shipped: the classify prompt's growth (10 intents, date extraction, search-term extraction) pushed some real questions into consistently burning the entire `max_tokens=300` reasoning budget with zero tokens left for the visible JSON — reproduced deterministically (temperature=0) across repeated calls, not a one-off — which `_parse_intent_json` had no way to tell apart from a genuine out-of-scope verdict. Raised to `max_tokens=700`. Live-verified afterward against real Gate B data end-to-end, with a real (no-longer-exhausted) OpenRouter key: a product-stock lookup by name, a purchase lookup by PO reference (correctly triggering the many-match disambiguation — one real PO in this data covers several different products, exactly the shape the design anticipated), a repair-cost lookup by job reference, and a specific-date revenue question — all four returned grounded, correct answers matching the underlying database rows exactly. 40 new tests (explicit-date parsing/bounds, the three new repository search methods' 0/1/many-match cases, and end-to-end chat-pipeline coverage per lookup intent).

  424 backend tests passing (up from 384; all of the above with regression/unit/integration coverage), a clean frontend build (no frontend changes required — `ChatResponse.intent` was already a plain string).

  Direct request: product categories — data capture, per-section dashboard filters, and a reports table (revenue, expenses, stock value per category). `ProductCategory`/`Product.category_id` already existed in the schema since Stage C8b/C12 but had zero writer anywhere — nearly every product showed "Uncategorized" because nothing ever set it. Confirmed with the user before building: "expenses by category" means purchase cost (unit cost × qty received), not COGS — a genuinely different figure this deliberately doesn't reuse — which surfaced a real gap: no per-purchase cost history existed anywhere (only `Product.cost_price`, a single always-overwritten "current" value), so a new `InventoryMovement.unit_cost` column was added (mirrors `SaleItem.cost_price_at_sale`'s exact role, captured going forward only — existing purchases stay `NULL`, no retroactive backfill possible). "Stock value" is deliberately at **sell price**, not cost — a different number from Retail Operations' existing `inventory_value` (which stays at cost, unchanged). A new optional "category" column on sales/purchases/inventory uploads (not repairs — no product link exists there) resolves against `ProductCategory` by normalized name, match-or-create, mirroring `ProductMatcher`'s exact shape (new `CategoryMatcher`); a product's category is set on creation and unconditionally overwritten on every later sighting that maps one, the same "latest wins" semantics `ProductRepository.update_cost_price` already established. New `app/analytics/category.py::compute_category_breakdown` groups already-fetched per-product data by category in Python (no new SQL joins — matches `sum_by_product_ids`'s own "fetch raw, finish in Python" convention), with an `expenses_data_coverage_pct` completeness flag (PR-3.5 pattern) disclosing what fraction of purchased quantity actually has a known cost. Financial Performance and Retail Operations dashboard sections each get their own independent category filter dropdown (confirmed: one per section, not a shared control) — Workshop Performance and Forecast stay unfiltered, confirmed out of scope (repairs have no product link at all; forecast explicitly excluded). Every product-row table (top sellers, stock cover, dead stock, margin products) now shows its category name beside the product name, and per-product findings (low stock, dead stock, selling-at-a-loss) carry it in their evidence — whole-business findings like revenue decline are untouched, per direct instruction. New `GET /businesses/{id}/product-categories` (list only — no create/edit endpoint yet, same "no product-management UI" gap already flagged as of C12) and `GET .../analytics/category-breakdown`; `category_id` query param added to the existing financial-performance/retail-operations routes. **Explicitly not solved**: returns stay whole-business even under a category filter (`Return` has no product/category-aware query) — `gross_revenue`/`return_rate_pct` are therefore not strictly accurate for a filtered category, stated in code rather than silently wrong-but-unflagged.

  Two real bugs found live, both fixed before shipping. **(1)** Checking the new "stock value at sell price" figure against the real Gate B business showed **every one of its 180 real products had `sell_price = NULL`** — `sell_price` was only ever set once, at product-creation time in `_write_sales`; a product first created via a purchases/inventory file (no price concept in those rows) never got one at all, even once later actually sold, and even a sales-created product's price never reflected a later real change. Fixed with `ProductRepository.update_sell_price`, called from `_write_sales` on every existing-product sighting — the exact missing counterpart to `update_cost_price`'s own "latest wins" pattern (which had this identical gap until v1.11). **(2)** Loading an already-stored report (generated before this session) crashed the whole page with `Cannot read properties of undefined (reading 'returns_amount')` — a report payload is a JSON snapshot taken at generation time, never re-serialized against a later schema, so `financial.returns` (added earlier this session) was simply absent on that older row. The identical risk applied pre-emptively to the brand-new `category_breakdown` key. Fixed with defensive guards at both access sites (`frontend/app/reports/[id]/page.tsx`) rather than assuming every stored report matches today's shape — live-reverified against the exact report that crashed: renders fully now, with the Category Breakdown section correctly showing "No category data for this period" instead of erroring.

  Live-verified end-to-end against the real Gate B Synthetic Shop business (real Postgres, not just SQLite tests): a purchases file with a category column imported through the real pipeline auto-created the category and assigned it to a new product; `retail-operations`'s `category_id` filter correctly scoped to just that product; the dashboard and a previously-crashing stored report both reload cleanly with the new backend live. 455 backend tests passing (up from 424), a clean frontend build.

  A third real gap found live, this time by direct feedback rather than self-testing: after all of the above, the filter dropdown and category labels were reported as simply not visible anywhere on the dashboard or reports. Root cause — a real design flaw, not a broken feature: every business genuinely had zero categories at that point (the one created during live verification was deliberately deleted afterward as cleanup), and `CategoryFilterSelect` hid itself entirely whenever `categories.length === 0`, with `CategoryLabel` doing the same per-product — making "the feature has no data yet" indistinguishable from "the feature isn't built." Fixed by always rendering the dropdown (disabled, with an explanatory hint, when there are zero categories) rather than hiding it — the control needs to be discoverable regardless of data state. Separately, assigned real categories (Bikes/Accessories/Components/Maintenance & Consumables/Workshop Supplies/Tools, derived from each product's own name) to all 180 real Gate B products via a genuine "inventory" reconciliation import through the real pipeline — chosen specifically because reconciling to each product's own already-current stock is a true no-op on quantity (verified: inventory value unchanged at €671,355.00 before and after), so only the category assignment landed. This surfaced one more consequence of the `sell_price` bug fixed earlier in this same entry: the fix only takes effect on sales imported *after* it shipped, so Gate B's pre-existing 180 products still had `sell_price = NULL`, leaving the new "stock value" figure at €0 for all of them. One-time backfill (not a permanent code path — a bounded migration script, matching the precedent of v1.19's `event_date` backfill): each product's `sell_price` set from its own most recent real sale line, 174 of 180 products backfilled (the other 6 have no sale history at all to derive one from — correctly left `NULL`, not guessed). Re-verified live afterward on both the dashboard (both category dropdowns populated with all 6 categories, filtering correctly scopes revenue, every product-row table shows its category, per-product findings show category while "Investigate the revenue drop" correctly doesn't) and on a freshly force-regenerated report (Category Breakdown table now shows real, non-zero revenue/stock-value figures across all 6 categories).

  A fourth round of direct feedback, covering both remaining pages. **Reports**: (1) a "Download PDF" button, deliberately using the browser's own print-to-PDF (`window.print()` plus a new `@media print` stylesheet hiding nav/controls and avoiding mid-row/mid-chart page breaks) rather than a new client-side PDF-generation library — no new dependency, and the browser already renders the report's ECharts canvases and tables correctly in print; (2) a "Sections" table of contents at the top linking to each section (`Section` gained an optional `id` prop for the anchor targets); (3) a real bug: "Top recommendations" and "Action Plan" showed only the generic rule description ("At the recent sales rate, this product is projected to run out shortly.") with **no product name at all** — both sections were built without the finding-message pairing the dashboard's own Findings section already had (a `Finding.message` is what actually names the specific product; `Recommendation.description` alone never does). Fixed by reusing that exact pairing. **Dashboard**: the category filter was missing from three places it was expected — Forecast's "Products to watch" table, and Findings & Recommendations entirely (both filter and per-product category labels). Root cause for Findings specifically: `get_findings` had no `category_id` parameter at all. Added one, but deliberately not by re-scoping everything blindly — per direct instruction ("Investigate the revenue drop... is more like a different thing than anything related to stock itself"), whole-business rules (revenue decline, low gross margin, incomplete cost data, high return rate) always read from an *unfiltered* `get_financial_performance`/`get_retail_operations` call, while the three per-product rules (low stock, dead stock, selling at a loss) read from a second, category-filtered call made only when a filter is active — one extra pair of already-existing queries, not new query logic. The same "investigate revenue drop is different" instruction is now also a real visual split, not just an implicit one: Findings & Recommendations (and the report's Action Plan) render as two labeled groups, "Business performance" and "Stock & products," with the category dropdown attached only to the latter. `get_forecast` gained the identical `category_id` treatment for its own per-product table — the revenue-forecast figure itself stays whole-business always (a forecast model run over a filtered history is a real, separate computation this pass doesn't attempt, not just a filter on an already-computed result — same reasoning as Retail Operations' `inventory_value` staying whole-business under a filter). The dashboard's `RecommendationList`/`findingKey`/business-vs-stock split logic was extracted into shared `frontend/lib/findings.ts`/`components/Section.tsx` so the reports page reuses the exact same implementation rather than a second copy. 10 new backend tests (forecast/findings category scoping, including a test proving a whole-business finding's evidence is byte-identical whether or not a category filter is active). Live-verified end-to-end: Forecast and Findings & Recommendations both now show independently-working category dropdowns and per-product category labels on the real Gate B business; the Action Plan/Top recommendations business-vs-stock split and product-name fix confirmed on a freshly regenerated report; the TOC and Download PDF button both render correctly. 459 backend tests passing (up from 455), a clean frontend build.

  A real user pasted three ORLA chat transcripts showing wrong/confusing answers. Diagnosed each live against the actual pipeline rather than guessed at, per this project's own established practice — one turned out to be a different root cause than the user's own guess. **(1) A SKU-annotated lookup found nothing**: "how much of this WorkshopPro Lubricant Plus (SKU-00175) i have left?" came back "I couldn't find anything matching...". Confirmed live: `ProductRepository.search_by_name_or_sku("WorkshopPro Lubricant Plus (SKU-00175)")` returned zero rows, while either half alone ("WorkshopPro Lubricant Plus" or "SKU-00175") matched correctly — and that exact combined "name (sku)" format is what ORLA's *own* many-match disambiguation labels use (`app/application/lookups.py`), so a user copying one back verbatim was guaranteed to fail. Fixed by also splitting a trailing "(...)" annotation off the query and trying the two halves separately. Re-verified live: now returns a correct, grounded answer. **(2) "Biggest cost"/"largest expense" questions were unsupported** — confirmed live: classified either `out_of_scope` outright, or (inconsistently) `financial_performance`, which has no per-line-item cost breakdown to actually ground an answer in. Added a new `category_breakdown` chat intent, reusing `app/application/category_breakdown.py` (already built for the dashboard/reports) as-is — no new calculation logic. Re-verified live: correctly classifies and answers honestly from real data ("all categories show zero expenses" — accurate for Gate B, since expenses require the `unit_cost` capture added in v1.26 and Gate B's real purchase history predates it, a known, already-documented gap, not a new bug). **(3) A many-match reply the user assumed lost context** — the user's own diagnosis was "model lost memory of what I was looking for," but ORLA never had conversation memory to lose (a stated design limitation from Stage E, kept deliberately to control cost) — live-repeated the exact same bare follow-up 5 times and it classified correctly as `product_lookup` every time; what actually failed was the *explain* step producing an ungrounded answer from a context with no real question framing, correctly caught and safely rejected by the existing guardrail (not a wrong answer shown as confident). Since full conversation memory is a genuinely separate, larger, cost-affecting feature (still explicitly deferred, not silently ignored), the practical fix here is cheaper and immediate: the many-match message now models a real, complete follow-up question using one of the actual matched items ("Ask me again naming just the one you mean — for example, \"how much stock of X do I have?\"") instead of a bare "Could you be more specific?" — live-verified this exact suggested phrasing round-trips to a correct, grounded answer. 463 backend tests passing (up from 459).
  Ran a live fire test against Gate B — a representative 20-question subset of a formal 56-question stress-test document (10-Question Fire Test, the Brutal Executive Test, 4 Direct AI Attack Tests, 2 Hallucination Traps, a cross-customer isolation test, 2 Bad-Reasoning traps), chosen to cover every critical category while skipping the multi-turn conversational tests (no conversation memory exists — a stated, deliberate limitation, not a gap to chase) and the tests requiring deliberately corrupted business records (not requested, too invasive to real data). Cross-tenant isolation, hallucination resistance, and prompt-injection resistance all held cleanly with zero findings. Found and fixed four real, distinct bugs, each diagnosed live before fixing: **(1)** a reorder/stock-out-shaped question ("what's likely to run out of stock") occasionally classified `out_of_scope` even though the classify prompt's own "forecast" description covers it — added a narrowly-scoped `out_of_scope` → `forecast` recovery using the same keyword check already built for the priority-list feature (can only ever recover a real question, never grant a more sensitive intent). **(2)** an explain-step provider failure (as opposed to a classify-step one) kept whatever intent had already been classified instead of being tagged `provider_unavailable` — misleading for anything inspecting `result.intent`, even though the user-facing message was already correct. **(3)** the guardrail's `validate_grounded` only checked the model's numeric claims against the fetched business-data context, never against the user's own question — so a question like "if I had €2,000 to invest, what would you recommend?" got its own echoed-back "€2,000" flagged as an invented number. Fixed with an additive-only `question` parameter: a number the user themselves supplied is a safe premise to reference back, not a business fact needing grounding — confirmed this doesn't weaken protection against a genuinely invented number (a second test asserts a real invented figure is still rejected even with a question supplied). **(4)** live-verified separately, a second guardrail gap: a numbered-list answer ("1. Reorder Widget... 2. Review pricing...") — which the explain prompt's own instructions require for a "give me N actions/products" question, since markdown/bullets are otherwise forbidden — had every list marker parsed as an unsupported Decimal claim, rejecting an otherwise fully-grounded answer 100% of the time for any multi-item question. Fixed by stripping a numbered-list-marker token (a 1-2 digit number immediately followed by "." or ")" and whitespace, whether at a true line start or inline after a lead-in phrase) before the numeric-claim scan ever sees it — verified a genuine number sitting right after a marker, and a genuine decimal like "3.5%", both still get checked correctly. **(5)** a fifth, related bug found via the same live run: "what's likely to run out in the next two weeks" always fetched the forecast's default 7-day horizon, so the model's own correct "14 days" phrasing had nothing to trace back to in the fetched context — not a guardrail problem, a real data gap. `ClassifyResult` gained an optional `horizon_days`, extracted at classify time when the question names a specific timeframe ("next two weeks" → 14, "next month" → 30) and server-side bounded to the same [1, 90] range the `/forecast` API route already enforces; an out-of-range or absent value falls back to the existing default of 7. Also raised `_generate_answer`'s `max_tokens` 500 → 800 (fixed mid-sentence truncation on complex multi-part questions) and added explicit currency-formatting guidance to the explain prompt (a money value must render with a € prefix and thousands separators, never rounded or re-digited). 477 backend tests passing (up from 470), including new regression tests reproducing each of the five bugs above verbatim. Live re-verified against the running Docker stack after every fix: the €2,000 question, the "next two weeks" stock-out question, and a "give me the five actions" question all now return grounded, correct, fully-detailed answers where they previously failed.

- [ ] F30. Video tutorials — deferred past prototype validation; UI will likely change once tested

---

## Gate B — Prototype Validation

Proceed only when:

- A real (anonymised) customer file imports successfully end-to-end
- Calculated metrics are verifiably correct against manual calculation
- Dashboard findings are understandable to a non-technical reader
- Tenant isolation tests pass
- Schema detection succeeds on a meaningful proportion of real files without manual mapping

---

# Phase 3 — Pilot-Ready MVP

**Epic:** `COP — Phase 3: Pilot-Ready MVP`

| Deliverable | Notes |
|---|---|
| Production deployment | VPS + Coolify per `07_Deployment_Guide.md` |
| Production database | Neon, EU region, backups verified |
| Backup restoration tested | Not assumed — actually tested |
| Monitoring | Sentry + Uptime Kuma |
| Transactional email | Resend: invitations, import results |
| Scheduled reporting | Weekly (Monday) and monthly (1st-of-month) in-app reports — never emailed — per PR-8/PD-007/ADR-019 |
| User roles and permissions | Owner, manager, staff |
| Import history and error recovery | Visible, reversible |
| Stripe test mode | Checkout and webhook flow proven, not yet live |
| Privacy controls | Retention settings, deletion, data export |
| Audit logging | Privileged actions recorded |
| Forecasting module | Simple methods first (seasonal/moving average) |
| Conversational agent — business Q&A lane | Intent classifier + approved deterministic query functions (PD-008, ADR-020); extends Phase 2's AI explanation layer |
| Conversational agent — product/support lane | Retrieval over an approved help-content knowledge base; hands off to human support when unresolved (PR-5.7) |
| Low-stock alerting | Real-time, deterministic, in-app (PD-009, PR-9) |
| Pilot onboarding material | Setup guide, not a template |
| 3–5 pilot businesses recruited | From Phase 1 interview relationships |

---

# Phase 4 — Pilot Validation

**Epic:** `COP — Phase 4: Pilot Validation`
**This is a measurement phase, not a build phase.**

| Metric to Measure | Why It Matters |
|---|---|
| Import success rate | Validates PD-006 (no-template approach) |
| Manual mapping rate | If high, schema detection needs work |
| Time to first value | Onboarding friction indicator |
| Dashboard usage frequency | Are they actually coming back? |
| AI question volume and cost | Validates the €3/customer cost assumption |
| Support requests per customer | The hidden cost that kills solo-founder SaaS |
| Most-used modules | Informs what to build next |
| Recommendation usefulness | Do they act on them? |
| Data quality problems encountered | Real-world ingestion edge cases |
| Willingness to continue paying | The commercial answer |

**Outputs:** revised data model, onboarding, pricing evidence and any future pricing recommendation, metrics, module priority, and support process — plus real numbers to replace the estimates in `08_Cost_Analysis.md` and `09_Business_Model.md`. The current price remains €80/month unless a later accepted decision supersedes BD-005.

---

## Gate C — Pilot Validation

Proceed only when:

- Pilot businesses use the product repeatedly and unprompted
- Support effort per customer is sustainable for one person
- Data import is reliably standardisable
- Customers express genuine willingness to pay

---

# Phase 5 — Company and Commercial Readiness

**Epic:** `COP — Phase 5: Commercial Readiness`
**Deliberately placed after validation, not before.**

| Deliverable |
|---|
| Legal structure and company registration |
| Revenue/tax registration |
| Business banking |
| Accounting setup |
| Insurance |
| Customer contracts and Terms of Service |
| Privacy notice and data-processing terms (GDPR) |
| Production Stripe account |
| Brand identity finalised (see `13_Branding_Strategy.md`) |
| Domain and trademark clearance completed |

---

# Phase 6 — Public Launch

**Epic:** `COP — Phase 6: Public Launch`

| Deliverable |
|---|
| Production billing activated |
| Public pricing published (€80/month, BD-005) |
| Public marketing website live |
| First paying customers onboarded |
| Application health monitoring in place |
| Churn and support tracking |
| Change log maintained |

---

## Gate D — Launch Validation

Proceed only when:

- Billing works end-to-end, verified with a real transaction
- Legal and privacy documents are in place
- Monitoring and backups are proven
- Unit economics are acceptable against real data

---

# Phase 7 — Product Expansion

**Epic:** `COP — Phase 7: Expansion`
**Only after evidence.**

| Deliverable | Trigger |
|---|---|
| Direct POS integrations | Customer demand for the specific POS |
| Automated recurring imports | Manual upload friction proven |
| Second business template | Validated demand in a new vertical |
| ~~Production Events generalisation (ADR-016)~~ | ~~Second template being built~~ — completed early, in Phase 2/3 (Stage C8b, 04/08/2026), once cafe and pharmacy were both being actively scoped simultaneously rather than waiting for Phase 7 |
| Advanced forecasting (e.g. ML models incorporating external signals such as weather — relevant to Irish cycling demand) | Simple methods proven insufficient |
| Multi-location support | Customer with multiple locations |
| Custom/additional report scheduling (beyond the default weekly/monthly cadence in PR-8) | Requested repeatedly |
| Mobile workflows | Usage data shows mobile need |
| Real-time repair-entry screen (staff-facing "log a repair" workflow writing directly into `production_events`, `status="open"` → `"completed"`) | Shops want live workshop tracking, not just the periodic repairs-file upload (Stage B8c) — would also be the natural place to reliably capture `customer_id`/`performed_by_id`, which the upload path deliberately leaves NULL |
| Supplier tracking on purchases (matching/creating `Supplier` records, wiring the currently-unused `suppliers` table to purchase rows) | Shops need per-supplier reporting — deliberately out of scope for Stage B8c's purchases upload v1, mirroring sales' own precedent of not doing customer-matching |

---

# Sequencing Rationale

**Why discovery before build:** the schema-detection engine (the hardest part of Phase 2) cannot be built well without real customer files. Building first means guessing.

**Why validation before company registration:** legal and accounting costs are real and recurring. Incurring them before knowing anyone will pay converts a cheap experiment into an expensive commitment.

**Why billing is deferred to Phase 3:** it adds meaningful complexity and no learning value during prototyping.

**Why forecasting is deferred:** it is the most technically interesting module and the least necessary for proving core value. That combination makes it a classic trap.

---

# Business Perspective

The gates exist to protect founder time and money. Their purpose is to make stopping — or changing direction — a legitimate, planned outcome rather than an admission of failure.

---

# Customer Perspective

Pilot customers should feel like collaborators, not test subjects. Phase 4 should return value to them, not just extract data from them.

---

# Technical Perspective

Tenant isolation and formula testing are built in Phase 2, not added later. Retrofitting either is disproportionately expensive and error-prone.

---

# Commercial Perspective

Revenue is not expected before Phase 6. The scenarios in `09_Business_Model.md` reflect this — modest MRR by month 12 is the realistic base case, not a disappointment.

---

# Current Decisions

* Separate product validation from commercial launch (BD-004, Accepted)
* Customer discovery precedes serious build (Accepted)
* Payments deferred until Phase 3, production billing until Phase 6 (Accepted)
* Jira `COP` is the execution tracker; this document is the strategic source of truth (Accepted)

---

# Risks

* **Skipping or shortening Phase 1** is the most likely and most damaging failure mode. It is tempting because building feels productive and interviewing feels slow.
* Solo-founder capacity means phases will likely take longer than planned; the roadmap deliberately has no fixed dates for this reason.
* Gate criteria can be rationalised away under pressure. They should be assessed honestly, ideally with a second opinion.

---

# Future Improvements

* Add estimated durations per phase once Phase 1 gives a realistic sense of pace.
* Populate Jira epics and stories directly from this document.
* Add a lightweight weekly review checkpoint to catch drift early.
* **Real per-client AI cost control before launch (PR-5.5 follow-up).** `ai_daily_request_limit_per_business` (currently 200, `app/settings/config.py`) is a *request-count* cap sized for the free-tier testing model, not a real cost-control mechanism — once launched on a paid, non-`:free` model this needs to become a genuine *spend* cap instead: only a set percentage of the €80/month plan price can be allocated to a given client's token usage without eroding margin. Direct instruction for the intended design: track actual spend per business (the already-logged `AIRequest.cost_eur` per call gives this for free, just unused for limiting today), and once a client crosses roughly 50% of their allotted per-plan token/cost budget for the period, automatically move them to a free/cheaper model for the remainder rather than hard-blocking further questions outright — degrade gracefully, don't cut off. Not designed or built yet: needs the actual allocated-€-per-plan number decided, a spend-based (not count-based) check replacing/supplementing `count_today_for_business`, and a per-business "current model tier" that `_classify_intent`/`_generate_answer` read instead of the single static `openrouter_model` setting.

---

# Phase 3 Detail — Scheduled Reporting Implementation

Implementation checklist for the "Scheduled reporting" deliverable in Phase 3's table above.

- [ ] Store and validate each customer's IANA timezone.
- [ ] Build separate weekly and monthly timezone-aware schedules.
- [ ] Implement the reusable cross-industry in-app report template.
- [ ] Implement top/bottom products, revenue, profit, expenses, comparative charts, projections, rule-based recommendations, low-stock warnings, and data-quality warnings.
- [ ] Add in-app notifications that show the seven-day expiry date.
- [ ] Add idempotency using tenant + report type + reporting period.
- [ ] Implement bounded retries and an independent missing-report recovery job.
- [ ] Add persistent-failure alerts and operational audit records.
- [ ] Implement seven-day expiry of the customer-facing report payload.
- [ ] Add explicit on-demand PDF and Word export; do not generate files on the normal schedule.
- [ ] Add tests for daylight-saving changes, timezone boundaries, weekly/monthly date collisions, duplicate prevention, forced recovery, expiry, and inapplicable business-template sections.
- [ ] Meter report compute, recovery attempts, temporary storage, notifications, and requested exports during the pilot.

This work is governed by PD-007 and ADR-019.

---

# Questions Still Open

* How many interviews are genuinely enough before Gate A — 15, or fewer if patterns emerge clearly?
* Should pilot customers be charged during Phase 4?
* Should the second business template be chosen during Phase 4, to inform ADR-016 before Phase 7?

---

# Revision History

| Version | Date | Changes |
|---------|------|---------|
| 0.1 | 29/07/2026 | Initial draft; structured for Jira epic mapping. |
| 0.2 | 30/07/2026 | Removed the completed €79-to-€80 housekeeping item and clarified that pilots test the accepted €80/month price. |
| 0.3 | 30/07/2026 | Marked the `01_Project_Vision.md` import-template housekeeping item complete; added Scheduled Reporting to Phase 3 (PD-007/PR-8 baseline weekly/monthly cadence); clarified Phase 7's "Report scheduling" as custom/additional scheduling only; synced version header with revision history. |
| 0.4 | 30/07/2026 | Removed the self-referential "(technical set)" Related Document. |
| 0.5 | 30/07/2026 | Fixed the Phase 3 "Scheduled reporting" line, which incorrectly described email delivery — corrected to match the accepted in-app-only design (PD-007/ADR-019). |
| 0.6 | 30/07/2026 | Renamed "Change 8 Delivery Work — Scheduled Reporting" to "Phase 3 Detail — Scheduled Reporting Implementation" and labelled it as elaboration on the Phase 3 table, so the heading follows this document's Phase/Gate structure instead of an internal work-item name. |
| 0.7 | 30/07/2026 | Added the conversational agent's business Q&A and product/support lanes, and low-stock alerting, to Phase 3 (PD-008, PD-009, ADR-020). Named weather-augmented ML forecasting as the concrete example of Phase 7's "Advanced forecasting" trigger. |
| 0.8 | 30/07/2026 | Added "Phase 2/3 Detail — Prototype Build Sequence": the founder-agreed, ordered 30-item checklist (Stages A-F) actually followed while building the prototype, tracked in place with checkboxes. |
| 0.9 | 03/08/2026 | Marked F28 (Stripe test-mode payments) complete — Checkout, webhooks, Customer Portal, subscription/invoice edge cases, and dispute handling all verified via live Stripe test-mode testing on localhost. |
| 1.0 | 03/08/2026 | Marked B6 (upload interface) and B7 (schema/column detection engine) complete. B7 v1 is scoped to sales transactions only, with entity type chosen explicitly at upload time; alias dictionary + structural heuristics + confirmation UI + mapping-profile reuse (PR-2.1-2.5) all verified via automated tests and a live end-to-end browser walkthrough, including reuse on a second upload from the same source. |
| 1.1 | 03/08/2026 | Marked B8 (clean data / transactional import + undo, PR-2.6-2.11) complete. Includes SKU/name product matching (auto-create on first sight, never falling back from a SKU miss to a name guess) and inventory_movements recording per sale — the previously-missing link needed for stock-level and product-popularity tracking. Verified via automated tests and a live API walkthrough (no frontend trigger UI built yet — flagged as a follow-up). |
| 1.2 | 03/08/2026 | Closed the two follow-ups flagged in 1.1/1.0: (1) added the frontend "Run import"/"Undo" trigger UI, exposing each upload's import summary and rejected-row reasons; (2) built PR-2.2's manual header-row-picker fallback for files where auto-detection can't confidently place the header, including persisting the picked row so B8's import step (which re-detects fresh each time) falls back to it instead of failing on the same file it already needed help with. Both verified via automated tests and a live end-to-end browser walkthrough. |
| 1.3 | 04/08/2026 | Added B8b: the `inventory` entity type (stock-count snapshot uploads), extending B7's detection engine and B8's import/undo pipeline beyond sales-only. Reconciles a snapshot against derived stock via a single adjustment movement per product, and closes an undo-ordering hazard found during design review that could otherwise let undoing a sales import silently corrupt stock after a later inventory reconciliation. Verified via 156 passing automated tests (unit, integration, tenant isolation) and a live end-to-end browser walkthrough that deliberately exercised the undo-guard bug scenario, confirming it is correctly blocked rather than silently succeeding. |
| 1.4 | 04/08/2026 | Added C8b: schema foundation for multi-vertical calculations. Accepted ADR-016 (Production Events, replacing bicycle-specific `repairs`/`repair_parts_used`), added ADR-022 (`inventory_lots`) and ADR-023 (pharmacy `prescription_details`). Checked off the Phase 0 housekeeping item this depended on; retired the now-moot Phase 7 "Production Events generalisation" trigger row (completed early). Schema/migration only — Stage C9 calculation work is next. |
| 1.5 | 04/08/2026 | Marked C9 complete: deterministic core calculations for Retail Operations and Financial Performance (PR-3), reading from `sales`/`sale_items`/`inventory_movements`/`products`. Workshop (repair) profitability intentionally deferred until `production_events` has a real-time entry workflow generating data. Every formula unit-tested (ED-007); verified against a real database via a new integration test. |
| 1.6 | 04/08/2026 | Marked C10 complete: deterministic findings & recommendations engine (PR-4) over C9's output — six rules, an approved recommendation library, impact-based ranking. Deliberately built the `low_stock` rule as a standalone function so C12 can reuse it directly instead of re-deriving the threshold logic; updated C12's line to say so. |
| 1.7 | 04/08/2026 | Marked C12 complete: real-time low-stock alerts (PD-009/PR-9), reusing C10's `evaluate_low_stock` rule unmodified. Fires from the existing import/undo commit points, persists to `alerts` with create/update-in-place/resolve dedup logic, adds per-product/category threshold columns. Flagged PR-9.3's owner-facing threshold configuration UI as a follow-up — no product-management API/UI exists yet to build it against. |
| 1.8 | 05/08/2026 | Added B8c: purchases and repairs upload entity types (the `"purchase"` `inventory_movements` reason, reserved since the model was designed, gets its first writer; repairs are ingested from a shop's own workshop log into `production_events`, same pattern as sales), plus a restock-freshness indicator. Refactored `run_import`/`undo_import`'s entity-type dispatch from a hardcoded if/else into dispatch tables for four entity types. Found and fixed a real bug during testing: the undo-ordering guard was unconditionally blocking `repairs` undos whenever any later inventory reconciliation existed, even though repairs write zero `inventory_movements` and can never be affected by one. Recorded two deferred feature ideas in Phase 7: a real-time repair-entry screen, and supplier tracking on purchases. |
| 1.9 | 05/08/2026 | Marked C11 complete: the first user-facing dashboard, plus a Workshop Performance calc (unplanned but roadmap-consistent, unblocked by B8c's repairs data) so it covers all three operational domains from day one. Charts render browser-side from structured API payloads (ADR-010, ECharts) with drill-down scoped to per-product/per-repair breakdown rows — full raw-record drill-down flagged as a follow-up, no endpoint lists individual sales rows yet. Verified via the backend suite (246 passing), a clean frontend build, and a live walkthrough against the running Docker Compose stack. |
| 1.10 | 05/08/2026 | Added B8d: remapping a confirmed-but-not-yet-run upload, closing a real gap found live (a mapping mistake had no way back short of re-uploading). Deliberately scoped to `status == "mapped"` only — remapping after a completed (even undone) import stays unsupported, since the source file is already deleted from R2 by then (ADR-008); recovery there is still re-upload. Found and fixed a second-order trap while building it: naively allowing remap to re-run the normal saved-profile reuse check would have silently reapplied the very mapping mistake the user was trying to fix, since `confirm_mapping` had already saved it as that source's profile — fixed by skipping the reuse fast path on a remap. Verified via the full test suite (250 passing) and a live walkthrough deliberately swapping SKU/product-name columns, remapping, and confirming the corrected data landed right. |
| 1.11 | 05/08/2026 | Two gaps found using C11's dashboard, fixed: (1) `rank_top_sellers` conflated "makes the most money" with "actually sells the most" — a high-price, low-volume item could outrank a genuinely popular one on revenue alone. Split into `rank_top_sellers_by_units`/`rank_top_sellers_by_revenue`, surfaced as a dashboard filter toggle rather than one ambiguous list. (2) Inventory (stock-count) uploads never touched `Product.cost_price` — only sales-at-creation and purchases did — so any product first seen via an inventory upload showed "Value at Cost: unknown" indefinitely. Added an optional `unit_cost` column to the inventory upload, mirroring purchases field-for-field (same alias list, extracted to a shared constant; same "optional, last value in file wins" semantics; reuses `ProductRepository.update_cost_price`). Verified via the backend suite (258 passing), a clean frontend build, and a live walkthrough confirming the units/revenue toggle produces genuinely different rankings against real data. |
| 1.12 | 05/08/2026 | Flipped sales import precedence: `total_amount` (what was actually charged) now wins over `unit_price × quantity` whenever both are mapped and disagree — previously the reverse, which silently discarded the file's total in favour of a derived figure. Real-world impact this fixes: a POS export's "total" is very often tax-inclusive while "unit price" isn't, so *every* row would mismatch and get silently overridden to the pre-tax figure; the warning read as a data-quality problem when it usually wasn't. Reworded the warning to explain the likely tax cause. Also added a `quantity <= 0` rejection for sales rows (mirrors purchases' `non_positive_quantity`, reworded to entity-agnostic wording since the message template is shared) — closes a `ZeroDivisionError` crash risk this change made far more reachable (division by quantity now happens whenever `total_amount` is present, not only when `unit_price` is absent). **Known limitation, not solved here:** `cost_price_at_sale` is untouched by this change and is typically tax-exclusive (a supplier cost), so gross margin is now computed against revenue that may include tax when `total_amount` was the winning source — margin % can read a few points higher than the true economic margin for shops whose exports include tax in "total." Proper VAT-aware margin (a distinct net-amount/tax-rate field) is future scope. Verified via the backend suite (260 passing). |
| 1.13 | 05/08/2026 | Closed v1.12's flagged limitation: added an optional `tax_amount` field to the sales upload (mirrors `cost_price_at_sale`'s exact shape — new nullable `sale_items.tax_amount` column). `app/analytics/financial.py`'s `GrossMarginResult` gains `net_gross_profit`/`net_gross_margin_pct`, computed only over lines where both cost *and* tax are known (never blending an uncertain line into a "confirmed net" figure), plus `tax_data_coverage_pct` — the same PR-3.5 completeness pattern used everywhere else, one level stricter. The dashboard shows one headline "Gross margin" number: the net figure whenever any tax data exists, falling back to the original tax-inclusive figure with an honest caveat when it doesn't — never two competing percentages. Also sharpened the `total_amount_mismatch` check to account for a known `tax_amount`, so a shop that maps price + tax + total together sees the warning mostly disappear, since tax was the actual explanation for it. Verified with a controlled scenario showing the real gap this closes: 50.0% tax-inclusive margin vs. 38.5% true net margin on the same data. **Explicitly not built:** Workshop Performance's `price_charged` (repairs) has the identical tax exposure but wasn't part of the report that prompted this; same fix pattern applies later. Verified via the backend suite (267 passing) and a clean frontend build. |
| 1.14 | 05/08/2026 | Found and fixed a real revenue-accuracy bug in B7's detection engine (`app/imports/detection.py`), surfaced by a pilot's actual test file (a POS export with both a pre-tax `Subtotal` column and a genuine post-tax `Total Amount` column): layer 1's alias matching was strictly first-column-wins, and `"subtotal"` is a listed synonym for our `total_amount` field — since `Subtotal` sat to the left of `Total Amount` in the file, it silently claimed the `total_amount` mapping, leaving the real total unmapped and revenue understated by the tax amount on every row. Fixed with a new sub-pass: an exact canonical-name match (a column literally called `total_amount`/`Total Amount`) is now claimed *before* any alias/synonym match is considered, regardless of column order — a structural fix that protects all four entity types (sales/inventory/purchases/repairs) against this whole class of alias-shadowing bug, not just this one instance. Also, prompted by the same investigation: reworded the sales/repairs field-mapping labels to explicitly say "charged to the customer" vs. "what YOU paid" (the two were being read backwards), added a framing note above the mapping form, and updated the `total_amount_mismatch` warning to also name per-line discounts as an expected, harmless cause (confirmed: `total_amount` already correctly absorbs any discount since it's trusted as-is, so a discount-driven mismatch needs no fix, just correct labelling — no discount field was added). Verified via a new regression test reproducing the exact bug shape, the full backend suite (268 passing), and a clean frontend build. |
| 1.15 | 06/08/2026 | Closed a data-accuracy gap raised directly (a client accidentally re-uploading an overlapping export would silently double-count revenue/stock/workshop income — nothing checked a new file against what was already imported). Sales' existing `order_reference` is now checked against every previously-imported sale for the business before a group is written; two new optional fields, `purchase_reference` and `repair_reference`, do the same for purchases and repairs (which had no reference field at all before this). A row/group reusing an already-used reference is rejected outright (`duplicate_reference`, in the existing rejection summary — never silently imported), including within a single file (a second occurrence of the same PO/job number later in the same upload). Undoing an import correctly frees its references for reuse with no extra code, since undo deletes the underlying rows entirely. Inventory (stock-count) uploads were deliberately left out of this — reconciliation there is already idempotent by design against *current* stock, not a per-file transaction log, so there's no equivalent "re-imported the same file twice" hazard to close. **Known, stated limitation, not solved here:** a row with no reference at all (most real-world exports today) has no reliable natural key — a genuine repeat sale and an accidental duplicate look identical, and no heuristic (date+product+price matching) was added, since it would misfire on real repeat transactions. Verified via the backend suite (281 passing), a clean frontend build, and a live walkthrough against the running Docker stack: uploaded a sales file, re-uploaded an overlapping one (rejected, revenue unchanged), undid the first, then confirmed the same reference re-imports successfully. |
| 1.16 | 06/08/2026 | Marked C13 complete: deterministic forecasting baseline (seasonal/moving average, PR-10) — the first stage of the platform that predicts anything forward-looking, and still not AI (Business Logic First holds). Covers both business-wide revenue and per-product unit demand, the latter feeding a clearly-caveated `suggested_reorder_quantity`. See C13's own roadmap entry above for the full design. New dashboard "Forecast" section: a revenue line chart with a confidence band, a 7/14/30-day horizon toggle, and a per-product reorder table sorted by urgency (soonest-to-need-restocking first). Found and fixed a real design-time bug before shipping: an early version zero-filled the lookback window all the way back to the 90-day cap regardless of how much real history existed, which silently defeated the "don't forecast on too little data" gate for any new business. Verified via the backend suite (296 passing) and a clean frontend build. |
| 1.17 | 06/08/2026 | Made the forecast section's confidence range and reorder caveat visible by default rather than hover-only, per direct feedback — the "typical range" is a sample standard deviation, not a formal statistical guarantee, and that distinction shouldn't depend on a user hovering to find it. Also fixed a real correctness bug found live: v1.15's cross-upload duplicate-reference dedup for purchases keyed on `purchase_reference` alone, so a genuine multi-line PO (one PO number covering several different products — completely normal) had every line after the first wrongly rejected as a duplicate. Real data from a pilot's 1656-row purchase-order export triggered this exactly: 835 rows incorrectly rejected. Fixed by keying purchases' dedup on `(reference, product_id)` instead of the reference alone — resolving the product now happens before the duplicate check, not after. Applied the same fix defensively to repairs (`(reference, description, price_charged, labour_cost)` — repairs have no product_id equivalent, so the rest of the row's detail disambiguates instead), since one invoice covering multiple repairs is the same plausible real-world shape, even though no live bug was observed there yet. Also clarified the pre-existing, unrelated `duplicate_product_in_file` warning: split into a separate `duplicate_product_cost_overwritten` code for purchases specifically, since "used the last value" read as if stock quantity were being dropped when it's actually only about which row's cost price is kept — every row's quantity was always added regardless of this warning. Verified via the backend suite (300 passing, including new regression tests for both the multi-line-PO-imports-correctly case and the genuine-duplicate-still-rejected case) and a live re-verification against the running Docker stack confirming a multi-line PO now imports both lines. |
| 1.18 | 06/08/2026 | Closed a data-accuracy gap raised directly: a purchase dated before a shop's most recent stock count is very likely already reflected in that count, so applying its quantity again would silently inflate derived stock. `_write_purchases` (`app/imports/importer.py`) now rejects any purchase row whose date is strictly before the business's last completed, unreversed inventory import's effective date — reusing `ImportRecordRepository.latest_completed_by_entity_type` as-is, no new query needed. Rejected outright by default (new code `purchase_predates_last_stock_count`, same rejection-summary treatment as `duplicate_reference` — no movement written, not silently applied), with an explicit client-controlled override: a new `include_purchases_before_last_stock_count` query param on `POST .../uploads/{id}/import`, surfaced as an unchecked-by-default checkbox next to "Run import" for purchases uploads specifically, for the case where the physical count genuinely predates the delivery and the system's date-only guess is wrong. Recovery for "ran without checking it, want those rows counted after all" is undo + re-upload with the box checked — the same pattern every other import decision in this pipeline already uses (no new recovery mechanism invented). **Deliberately scoped to purchases only, stated not solved:** repairs never write `InventoryMovement` at all, so this class of risk is structurally N/A there; sales carry the identical risk in the opposite direction (a sale dated before the last count may already be reflected in that lower count too) but building that mirror-image guard is deferred, not silently ignored — sales' write path is more complex (multi-item grouping) and this wasn't part of what was reported. Verified via the backend suite (304 passing, 4 new integration tests covering the reject/override/no-stock-count/undone-stock-count cases) and a live walkthrough against the running Docker stack confirming a stale purchase is rejected by default and imports correctly with the override checked. Also deleted an empty `TEST_SHOP` test business from the dev database at the user's request (zero uploads/products — confirmed before deleting). |
| 1.19 | 06/08/2026 | **Supersedes v1.18.** Reframed per direct feedback: most shops export sales and their stock count together, daily, from the same POS — stock naturally reflects "final stock of the day" — and the platform will eventually ingest data automatically rather than by manual upload, where "upload order" isn't a coherent concept at all. Replaced v1.18's order-based reject/override guard with a general, order-independent fix: `InventoryMovement` gains `event_date` (the calendar date a movement's underlying event actually happened — `sale_date`/`purchase_date`, or a stock count's new optional `as_of_date`, defaulting to the upload's processing date) and `resulting_quantity_on_hand` (an adjustment row's authoritative reconciled total, stored directly rather than re-derived). `InventoryMovementRepository.sum_by_product_ids` — the one function nearly every dashboard/forecast number reads current stock through — is rewritten around these: the most recent stock count (by its own date, not upload order) becomes the baseline, and only sales/purchases dated *after* it are added on top; anything dated on or before is presumed already reflected in that count and excluded automatically, correctly, regardless of what order the underlying files arrived in. Same function name and signature, so every caller (`retail_operations.py`, `alerts.py`, `forecast.py`) gets the corrected math for free. v1.18's reject/override mechanism is retired entirely (its `include_purchases_before_last_stock_count` param and uploads-page checkbox both removed) — every purchase row now writes normally, always (movement, cost update, reference dedup all unaffected), with a new purely-informational warning (`purchase_excluded_from_current_stock`) replacing the old rejection. A new migration backfills `event_date` for existing rows: exactly, via a join to `sales.sold_at`, for `sale` movements; as a stated one-time approximation from `created_at` for `purchase`/`adjustment` movements, which never captured a real date before this. **Deliberately deferred, not bundled into this already-large change:** the undo-ordering guard (`ImportRecordRepository.has_later_completed_inventory_import` / `ImportSupersededByLaterInventoryImport`) may itself now be redundant under this same date-aware model — worth revisiting separately. Verified via the backend suite (310 passing, including a new dedicated `test_date_aware_stock.py` covering multiple reconciliations over time, an out-of-order backdated reconciliation, and the sales-side mirror case), a clean frontend build, a real migration run against the local dev Postgres (backfill confirmed on ~8,000 existing rows), and a live walkthrough against the running Docker stack proving the exact scenario reported: a stock count of 40, a stale purchase of 25 correctly excluded (with the new warning) and a fresh purchase of 10 correctly added — current stock 50, not 40, not 75. |
| 1.46 | 08/08/2026 | Added an address-and-timezone validator for the business profile page, per direct proposal (Geoapify address geocoding -> timezonefinder lat/lon-to-IANA-zone lookup -> server-side validation -> Neon), reviewed and endorsed with two refinements before building: keep a manual timezone override (geocoding fails or guesses wrong sometimes), and it needed its own new API key (can't be provisioned by this assistant). New `app/geocoding/` module: `client.py` (the only Geoapify caller, mirrors `app/ai/client.py`'s "one file owns the provider boundary" shape), `timezone_lookup.py` (a module-level `TimezoneFinder()` singleton — constructing one per call would be wasteful, its own docs warn against it), `service.py` orchestrates the two and degrades gracefully (a clear "not configured" / "no match found" / "provider request failed" result, never a raised error) when `GEOAPIFY_API_KEY` is unset or the request fails — same PR-5.4-style posture as the AI provider gateway. New `POST /businesses/{id}/validate-address` — deliberately an explicit action, not autocomplete-as-you-type (Geoapify's free tier caps at 3,000 requests/day; editing a profile is infrequent, there's no case for per-keystroke volume here); returns a suggestion only, applied into the profile form via a new "Use this address" button, still requires the normal Save click like any other edit — never writes anything itself. Live-verified `timezonefinder` directly: it covers the *entire* globe including ocean (via `Etc/GMT+N` nautical zones), not just land, correcting an initial doc-comment assumption that "no match" was a real, if rare, outcome — in practice it never happens for a valid coordinate. 562 backend tests passing (up from 550, including a mocked-provider-boundary suite for every degradation path plus two live geographic lookups), clean frontend build. **Not yet live-verified end-to-end**: no real `GEOAPIFY_API_KEY` exists yet (the user's own account, not committed) — the whole pipeline up to and including the Geoapify HTTP call itself is untested against the real provider; every other path (not-configured, no-match, provider-error, and the full match-through-timezone flow with a mocked provider response) is verified. |
| 1.45 | 08/08/2026 | Added an "All branches" combined dashboard view — direct request, scoped via two clarifying questions first: covers every section (Financial, Retail, Workshop, Forecast, Findings — the full answer, not just Financial), and only offered when every business in the group shares one timezone (never silently blends two different "this week"s into one number; a mismatch surfaces as a plain 409 naming the timezones instead). New `app/application/business_group.py`: `resolve_authorized_group` resolves a business's whole standalone-shop-plus-branches family (`app/repositories/business.py::get_business_group`, symmetric — works starting from the parent or any branch) and checks membership *per business*, not just the group relationship, before combining anything — a manager added to only one branch, not the primary shop, correctly can't pull in data from a business they don't have access to. Every aggregation function sums or concatenates **raw** per-business inputs (repository rows, before any ratio is computed) and then calls the exact same, unmodified formula from `app/analytics/*.py` exactly once over the combined data — deliberately never merges already-computed per-business percentages, which would silently average unrelated rates instead of computing one true combined rate. A product/category is already a UUID scoped to one business's own table, so combining per-product tables (top sellers, stock cover, forecast reorder rows) is a plain, collision-free union, never an identity merge — two branches' own "Chain Lube" stay two separate rows. The revenue forecast's combined confidence band isn't a separate statistical formula either: raw daily sale amounts are concatenated across the group *before* bucketing into a daily series, then run through the same `compute_baseline_forecast` untouched — mathematically identical to the group being one shared sales table. All five existing analytics routes (`app/api/analytics.py`) gained an opt-in `all_branches` query param (default `false`, every existing call site unaffected); dashboard gained a "Combine all branches into one view" checkbox, shown whenever the account has more than one business (the one-shop-per-account limit guarantees that's always exactly the group). 550 backend tests passing (up from 540) — new coverage proves the sum-not-average property directly (two businesses of different sizes, summed revenue checked against each one's own individually-computed figure) plus the membership/timezone/wiring paths end-to-end through the real routes — clean frontend build, and the whole pipeline live-verified against the real dev database (Test Bike Shop + its Galway branch) before any test was written. |
| 1.44 | 08/08/2026 | Fixed a real, significant bug reported as "'Could not save this mapping' even though I checked all fields" for a new branch — root-caused live against the branch's actual stuck uploads (verified directly, then rolled back, no data changed) rather than guessed at: **it wasn't branch-specific at all.** `frontend/app/uploads/page.tsx`'s `FIELD_ORDER` for sales/inventory/purchases never listed `"category"`, even though `backend/app/imports/aliases.py::CANONICAL_FIELDS` has required it (as an optional field within the set) since v1.26's product-categories feature — and `confirm_mapping` rejects anything whose field-mapping keys aren't an *exact* match to the canonical set. Net effect: confirming a sales, inventory, or purchases mapping has been silently guaranteed to fail for every business, not just this branch, since v1.26 shipped (Aug 7) — repairs was the only entity type unaffected, since it has no category field. Live-reproduced both ways against the branch's real stuck upload to confirm the exact mechanism before fixing: the same suggested mapping succeeds with `category` included, fails with `InvalidFieldMapping` without it. Fixed by adding `category` to `FIELD_ORDER`/`FIELD_LABELS`/`FIELD_HINTS` for the three affected entity types, matching the backend's set exactly. Separately, and independently worth fixing: `handleConfirmMapping`'s error handling (plus three sibling catch blocks in the same file) always showed one fixed guessed message regardless of the real failure — the backend already returns an accurate, specific `detail` per exception type (missing required fields, a stale/renamed column, upload not ready, ...) but the frontend discarded it every time. Now surfaces `ApiError.message` like the rest of this frontend already does, so a genuinely different failure won't masquerade as the same "check your mapping" message again. **Known residual fragility, not solved here:** `FIELD_ORDER` and `CANONICAL_FIELDS` are two independently-maintained lists in different languages with no shared source of truth and no automated check that they stay in sync — this exact class of drift could recur the next time a canonical field is added on one side only. No backend changes; 540 backend tests still passing, clean frontend build, and the fix verified against real data (not just the type-checker) before shipping. |
| 1.43 | 08/08/2026 | Reworked the onboarding page into a proper "Company Profile" area, per direct feedback covering four points. **(1)** The "Add a branch" form had no Cancel button of its own (only the toggle button re-labelling itself) — added an explicit one next to "Add branch," clearing the form. **(2)** Top nav's "Onboarding" link renamed to "Company Profile" (same `/onboarding` route). **(3)** The list's inline 9-field "Edit profile" form (one per business, all expanding in place) is replaced by a "View profile" link to a new dedicated page, `/onboarding/[id]` — the full profile, editable, with **Save** (PATCH, then back to the list) and **Cancel** (back to the list, nothing sent) buttons, closing the list itself back down to one line of quick actions per business. **(4)** Each business now shows one of exactly four statuses — **Subscribed / Pending Payment / Cancelled / Deleted** — collapsed from the raw Stripe subscription status plus this app's own soft-delete flag. Showing "Deleted" required a real, scoped backend change: `GET /businesses` gained an opt-in `include_deleted` query param (default `false` — every existing caller, every other page's business selector, is unaffected) and `BusinessOut` gained `deleted_at`; only the Company Profile list passes `include_deleted=true`. A deleted business is listed read-only (no actions — the backend already 404s any PATCH/DELETE against one), closing a real gap where "was this shop actually removed?" had no answer short of querying the database directly. "Upload data" next to each business continues to gray out unless status is exactly "Subscribed" (v1.39/v1.41's fix, unchanged, now reusing the same four-state label). 540 backend tests passing (up from 538, new coverage for `include_deleted` and its tenant isolation), clean frontend build, live-verified against the real dev database including one business the account owner had genuinely deleted mid-session — its "Deleted" status rendered correctly. |
| 1.42 | 08/08/2026 | Reported directly: "we are missing the button to add more branches" on the onboarding page. Verified live before changing anything — the button was genuinely present in the served code (checked directly inside the running container) and the real dev-DB data was correct ("Test Bike Shop" confirmed standalone and not soft-deleted, which is exactly what makes the button render) — this was not a missing feature or a broken condition. Root cause: each business had grown into one long, unstyled, run-on line of inline buttons (upload link, subscription status, delete, edit profile, add-a-branch, all separated by "—"), and the branch button specifically sat at the very end of it, easy to lose. Restructured each business's row into clearly separated lines (name/role, upload link, subscription status, then a dedicated "+ Add a branch (€30/mo)" line for standalone shops, then delete/edit-profile below) so it can't be scanned past. Also restarted the frontend dev container as a precaution — its own logs showed several "Fast Refresh had to perform a full reload due to a runtime error" events from earlier in the session, raising the possibility of a stale client bundle compounding the visibility problem. No backend changes — clean frontend build. |
| 1.41 | 08/08/2026 | Two bugs reported directly. **(1) "Delete this shop does nothing"**: replaced the `window.confirm()` dialog with a plain inline "Delete X? Yes / No" prompt (the same expand-in-place pattern already used for the branch and profile forms) — native confirm dialogs are known to silently no-op in some embedded/webview browser contexts rather than actually showing anything, which is consistent with "no action" on a real click; the inline version has no such dependency and also matches the explicit "Yes/No" wording asked for, rather than the browser's own "OK/Cancel." **(2) "Upload page's business dropdown still shows a shop pending payment"**: the dropdown (and the page's default business selection) previously listed every business on the account regardless of subscription status, even though uploads there always 402 for one that isn't active. `frontend/app/uploads/page.tsx` now fetches every business's subscription status itself (`Promise.all`, so the dropdown only ever filters once real data is in, never flashing empty first), restricts the dropdown to actively-subscribed businesses only, auto-corrects the selection off a pending/unsubscribed business onto a subscribed one once available, and shows a clear "none of your shops has an active subscription yet" message with a link to Onboarding if none qualify — deliberately scoped to this page alone (`useBusinessSelector` itself stays unfiltered, since dashboard/reports correctly keep showing every business, subscribed or not, per v1.36's read-routes-stay-open design). No backend changes — 538 backend tests still passing, clean frontend build. |
| 1.40 | 08/08/2026 | Added a per-business profile — direct request: "a full profile for owners in each branch: name of the owner/manager, Location, Time Zone, address, contact email, and some other necessary fields." Scoped via a direct question first: descriptive contact/location record-keeping only, not a second login (a real per-branch-manager account would need an invite/permissions system that doesn't exist anywhere in this codebase yet — confirmed out of scope). New nullable `Business` columns (migration `f3c7b2e9a1d4`, applied to the real dev Postgres): `manager_name`, `contact_email`, `contact_phone`, `location_label` (a short label like "Dublin - Rathmines", distinct from the formal shop `name`, which is often identical across a primary shop and its branches), `address_line1`, `city`, `postal_code`, `country` — `timezone` already existed and is now editable through the same form rather than only at creation. New `PATCH /businesses/{id}` (any member, not owner-only — unlike delete/add-branch, editing contact info has no financial or destructive weight), `exclude_unset` semantics so an omitted field is left alone while an explicit null clears it, and a defensive field whitelist in the repository layer independent of the request schema. Refactored all four `BusinessOut`-constructing routes onto one shared `_to_business_out` helper — this codebase already hit a real bug once from that exact inline-construction pattern (a field silently defaulting to `None` in two routes that forgot to pass it through), so a field added to the model now only needs updating in one place. Onboarding page gained an "Edit profile" expandable form per business, same pattern as "Add a branch." 538 backend tests passing (up from 533), clean frontend build, migration verified live against the real dev database. |
| 1.39 | 08/08/2026 | v1.38 only fixed the onboarding page's own per-business "Upload data" link — reported directly, and confirmed live, that the *shared* `AppNav` component's own "Upload data" link (present on every page's top nav — dashboard, uploads, reports, chat) was still unconditional, so it kept implying real upload access for an unsubscribed business (verified concretely against a real newly-added, payment-not-completed branch, `Test_Branch_Added`, which correctly has no `Subscription` row at all — `require_active_subscription` was, and remains, correctly enforced server-side the entire time; this was purely a second instance of the same frontend false-impression gap). `AppNav` now fetches the given `businessId`'s subscription status itself and only renders a working "Upload data" link when `status === "active"`, otherwise "Upload data (subscribe first)" text — mirroring the onboarding page's own fix exactly, the one other place this link exists. Clean frontend build; no backend changes. |
| 1.38 | 08/08/2026 | Two follow-ups reported directly right after v1.37 shipped, against the same two real dev-DB businesses. **(1)** Archived `Test_Shop` (soft-deleted directly, per explicit instruction — it had no subscription and no data, so nothing else was touched) rather than keeping it around as a `parent_business_id`-linked branch, which is what v1.37 had turned it into. **(2)** Closed a real, confirmed UX gap: the onboarding page's "Upload data" link rendered unconditionally for every business regardless of subscription status, which looked like real access even for a business the backend would 402 on the moment an actual upload was attempted (`require_active_subscription` was, and remains, correctly enforced server-side the whole time — verified directly against the real dev DB: "Test Bike Shop" has no active Stripe subscription today and its uploads route does 402). The link now only renders when `status === "active"`; otherwise it's plain "Upload data (subscribe first)" text, matching the real gate instead of contradicting it. **(3)** Reworked the "Add a branch" flow per direct feedback: submitting the inline form now redirects straight into Stripe Checkout for the branch price (`redirectToCheckout`), rather than leaving a newly-created, unpaid branch sitting in the list as a separate manual "Subscribe" step. A branch that exists but hasn't completed payment (abandoned Checkout, or paid Stripe hasn't confirmed the webhook yet) now reads "payment incomplete" with a "Complete payment (€30/mo)" button — recoverable, not a dead end — rather than either silently granting access or vanishing; the underlying create-before-pay shape is deliberately unchanged (matches every standalone shop's existing, already-tested pattern, and avoids a materially riskier redesign of the Stripe webhook handlers, which currently all key off a `business_id` already existing in event metadata and would need out-of-order-delivery-safe branch creation logic duplicated across three separate event handlers to defer business creation to the webhook itself — flagged as a real, deliberately-not-taken alternative, not silently avoided). No backend changes this round — 533 backend tests still passing, clean frontend build. |
| 1.37 | 08/08/2026 | Tier 2 branch billing + nav consistency, direct request. **(1) Paid branch add-on (€30/month)**: a branch is a second full `Business` row (`parent_business_id` set, schema groundwork already laid in v1.36) with its own separate `Subscription` row, reusing the exact same checkout/webhook/cancel infrastructure as any standalone business — a deliberate simplification over the originally-stated "one shared subscription, quantity line item" idea (confirmed via direct question: today's checkout is hardcoded to one fixed-price, quantity-1 line item, zero multi-item precedent anywhere in the billing code). New `settings.stripe_branch_price_id`, a new Stripe test-mode Price created directly via the app's own already-configured `rk_test_` key (a safe, reversible config action, not a financial transaction); `billing/service.py::start_checkout` selects it automatically whenever `business.parent_business_id is not None`. New `POST /businesses/{id}/branches` (owner-only, `create_branch_business` in the repository layer — exempt from the one-standalone-shop limit, gated on parent ownership via `Membership.role == "owner"`, not by any pre-paid quantity). Frontend: `frontend/app/onboarding/page.tsx` shows each branch labelled "↳ Branch of {parent}", offers "Add a branch (€30/mo)" on every standalone shop via an inline form, and the standalone-shop-limit check/hint text now correctly excludes branches (previously would have wrongly counted them). **(2) Nav consistency**, direct request: "Log Out" and "Onboarding" (renamed from "Manage shops") now live in the shared `AppNav` component itself rather than only on the onboarding page — every screen that renders `AppNav` (dashboard/uploads/reports/chat) gained both; the onboarding page itself gained `AppNav` for the first time, closing the reported gap where Dashboard/Reports/Ask ORLA were unreachable from it. **(3) Real-data correction, reported honestly rather than assumed**: live-checked the two named dev-DB businesses ("Test Bike Shop", "Test_Shop") against real Stripe test-mode data before touching anything — found neither has a matching Stripe subscription today, despite the stated expectation that the first was already paid; see this turn's reply for the full finding. The one safe, non-financial correction made directly: `Test_Shop.parent_business_id` now points at Test Bike Shop's id, so it's correctly recognized as its branch going forward — no subscription/billing state was fabricated for either business. 533 backend tests passing (up from 523), clean frontend build. **Not yet live-verified end-to-end in the browser**: doing so needs the account owner's own login (Supabase session) or a real Stripe test-mode Checkout completion, neither of which this assistant can perform on the user's behalf. |
| 1.36 | 08/08/2026 | Subscription enforcement, prioritized from a larger backlog: uploads worked regardless of subscription status until now (`app/billing/access.py::require_active_subscription` existed since an earlier stage but was never wired to a route), and there was no limit on how many businesses one account could create, nor any way to delete one. **(1) Uploads blocked without an active subscription**: `require_active_subscription` now gates the two routes that actually create new business value — `POST /businesses/{id}/uploads` (`request_upload`) and `POST .../uploads/{id}/import` (`run_import`) — leaving every read/cleanup route (list, freshness, detect/confirm-mapping, undo) on plain `get_current_membership`, so a lapsed account can still see and fix what it already imported. Frontend gained a typed `ApiError` (previously `apiGet`/`apiPost` discarded the HTTP status into a generic string — zero status-aware handling existed anywhere in this frontend before this) surfacing the backend's actual `detail` message, and `frontend/app/uploads/page.tsx` shows a specific "subscribe to continue" prompt on a 402 instead of a generic failure. **(2) One shop per account**: `create_business_with_owner` now rejects a second standalone business for the same owner (409, `BusinessLimitReached`) — confirmed via a live-reported design tension during planning that "branches billed as a line item on the same subscription" doesn't match today's billing model (`Subscription` is one row per business, and Stripe checkout is hardcoded to one fixed-price, quantity-1 line item with zero multi-item precedent anywhere in the codebase); scoped this pass to the limit itself plus schema groundwork only (`businesses.parent_business_id`, unused by any route yet), with the actual paid-branch checkout flow explicitly deferred as a separate follow-up rather than guessed at. **(3) Delete shop**: soft-delete only (`businesses.deleted_at`) — confirmed live that none of the ~20+ `business_id` foreign keys in this schema have `ON DELETE CASCADE`, so a hard delete would fail outright the moment any child row exists; archiving cancels the business's Stripe subscription (new `app/billing/client.py`/`service.py::cancel_subscription`) but leaves every sale/product/upload/audit row untouched, consistent with this codebase's audit-log-conscious posture. New `DELETE /businesses/{id}`, owner-role-gated — the first real enforcement of `Membership.ROLES` anywhere in this codebase (defined since the model was introduced, never checked until now). Found and fixed a related gap while building this: `GET /businesses/{id}` didn't check `deleted_at` either, so a soft-deleted business was still directly fetchable by id. Frontend: `frontend/app/onboarding/page.tsx` now hides the create-business form once a standalone business exists (showing the 409's own message instead) and adds a confirm-gated Delete button per business; `frontend/components/AppNav.tsx` gained a persistent "Manage shops" link — confirmed there was previously no way back to onboarding/billing once a user had ≥1 business short of typing the URL. 523 backend tests passing (up from 508), clean frontend build, live-verified against the real Postgres dev database (not just SQLite) end-to-end: limit enforcement, soft-delete, data-not-touched, and slot-freed-on-delete all confirmed directly against production-shaped data. |
| 1.35 | 08/08/2026 | A third live-reported "period follow-up" failure ("what is the last period?") made clear v1.34's prompt-only fix (telling the classify model to infer the same intent as the previous turn) doesn't generalise reliably across phrasings on this free-tier model — it held for the one wording it was tuned against and failed on the next two. Replaced with a deterministic recovery, the same proven shape as the existing reorder-question recovery: `ChatRequest`/`answer_question` gain `previous_intent` (the prior turn's own `ChatResponse.intent`, echoed back by the frontend — untrusted client input, only ever checked against a new fixed `_CONTINUABLE_INTENTS` allow-list before use, never trusted directly). A new `_looks_like_a_period_follow_up_question` keyword check (mirroring `_looks_like_a_reorder_question`) gates the recovery: an unresolved `out_of_scope` classification for a period-referencing question with a valid, allow-listed `previous_intent` recovers to that exact intent instead of refusing — can never grant a more sensitive intent or override a genuine refusal for an unrelated question, matching the existing recovery's own safety invariant exactly (verified with a dedicated test asserting a genuine topic change right after a valid `financial_performance` turn still refuses correctly). Also closed the deeper data gap this kept surfacing: `FinancialPerformanceSummary`/`FinancialPerformanceOut` gain `previous_period` (the exact date range `revenue.previous` was computed over) — previously only the previous period's revenue *figure* was ever in the payload, never its actual dates, so even a correctly-routed answer could only honestly say "I don't have the dates." Frontend (`frontend/app/chat/page.tsx`) now tracks and resends the last answer's intent alongside its question/answer. 508 backend tests passing (up from 501), clean frontend build, live-verified against the exact reported scenario. |
| 1.34 | 08/08/2026 | Two fixes reported directly, both live-confirmed against real traffic from the user's own session — backend logs showed the exact 429s during their test, timestamped before this fix landed. **(1) "ORLA is temporarily unavailable"**: `app/ai/client.py`'s `chat_completion` gained a bounded retry — on a 429 from every model in the fallback chain, wait 2s and retry the whole chain once more (OpenRouter's routing isn't perfectly sticky, so a second attempt can land on a freed-up backend) before finally degrading to the existing graceful fallback (PR-5.4 kept as the genuine last resort, never removed). **(2) "when was the previous period?" still hitting the generic out-of-scope fallback**: confirmed the frontend was already serving v1.33's memory changes (real traffic in the access logs), reproduced the exact phrasing live, and found classify wasn't reliably inheriting the previous exchange's intent for this wording specifically. Strengthened `_CLASSIFY_SYSTEM_PROMPT_TEMPLATE`'s memory instruction from a passive "may resolve an ambiguous reference" note into an explicit directive — a short follow-up that only makes sense in light of the previous exchange should default to the SAME intent as the previous answer, with a worked example. Re-verified live: now correctly classifies `financial_performance` and answers honestly ("I don't have the dates for the previous period in the data supplied") instead of refusing outright — surfacing a separate, smaller, genuine gap (the previous period's date range isn't in the payload at all, only its revenue) flagged, not fixed, here. Attempting fix (1) also **surfaced two real, pre-existing problems while diversifying the fallback chain away from OpenAI+Google to reduce simultaneous rate-limiting**: first shipped a 3rd fallback on NVIDIA, which live-testing caught immediately breaking classify *entirely* — every question, not just follow-ups, silently defaulted to `out_of_scope` with zero trace in the logs. Root cause, live-verified: `nvidia/nemotron-3-nano-30b-a3b:free` 404s outright under this app's own `data_collection: deny` privacy restriction ("No endpoints found matching your data policy (Free model training)" — that provider's free tier requires allowing training on the data), and the model that had been in the fallback chain since before this fix, `inclusionai/ling-3.0-tiny:free`, turned out to 400 on every classify call for an unrelated reason — it doesn't support structured-outputs mode at all, a silent gap this investigation surfaced rather than introduced. Replaced both with `cohere/north-mini-code:free`, confirmed live to return clean, valid JSON under both restrictions — every fallback candidate is now live-tested against this app's actual request shape before being added, not picked from the public catalog on trust. Separately hit and fixed OpenRouter's own hard cap on the `models` array (400: "'models' array must have 3 items or fewer") after briefly shipping 4; capped back to 3 and added a defensive `models[:_MAX_MODELS_PER_REQUEST]` slice in `client.py` itself so a future config edit can't silently reintroduce this. Added logging to `_parse_intent_json`'s previously-silent failure paths (no content, non-JSON, non-dict, unrecognised intent) specifically because their silence is what let the NVIDIA regression hide as ordinary "not about this business" refusals — now distinguishable in the logs. Building test coverage for that logging then surfaced one more real, pre-existing test-infrastructure issue: Alembic's own CLI calls `logging.config.fileConfig()`, which defaults to `disable_existing_loggers=True`, silently disabling `app.ai.service`'s logger for the rest of any pytest session that runs a migration first — fixed with a small local fixture that re-enables it for the duration of the affected tests. 501 backend tests passing (up from 489). |
| 1.33 | 08/08/2026 | Added last-exchange-only conversation memory to ORLA chat, closing a repeatedly-flagged gap: "what was the previous period?" as a follow-up used to hit the generic out-of-scope fallback since ORLA had nothing to resolve "previous" against. Per direct scope decision: only the immediately preceding question/answer is resent, never the whole thread (bounded token cost by design), and it still counts against the existing per-business daily cap with no new cost mechanism. `ChatRequest` gains optional `previous_question`/`previous_answer`; `answer_question`/`_classify_intent`/`_generate_answer` thread them through as real prior `user`/`assistant` turns in the `messages` array (not spliced into a system prompt as text — the standard, injection-safe shape for conversational context with a chat-completions API). The classify call gets the previous answer truncated to 500 chars (classify only needs to know the topic, and this module already hit a real regression once from prompt growth exhausting its reasoning-token budget — see v1.24); the explain call gets the full text. `validate_grounded` gained a `previous_answer` parameter (additive-only, same shape as the `question` parameter from v1.30) so a correct follow-up naturally restating a figure ORLA already gave isn't wrongly flagged as an invented number. `orla_constitution.md` updated to state the memory boundary explicitly: prior exchange may resolve what a question refers to, never supply a new fact beyond what's already grounded. Frontend (`frontend/app/chat/page.tsx`) tracks the last real exchange in its own state (separate from the displayed thread, which can also hold an error message) and resets it whenever the selected business changes — a stale exchange from a different business would be actively wrong to carry over, not just stale, now that it's threaded into the AI call. 489 backend tests passing (up from 483), a clean frontend build, and live re-verification against Gate B confirming the exact previously-broken scenario now resolves correctly. |
| 1.32 | 08/08/2026 | Scoped a subset of a proposed "ORLA AI Skills, SOPs, Grounding & Personality" architecture plan (user-supplied PDF) — assessed the full plan first and found roughly two-thirds of it already built under different names (its "Findings Engine" = the existing `app/analytics/findings.py`; its "AI Skill Router" = the existing `_classify_intent`/`ALLOWED_INTENTS` dispatch, ADR-020; its "Grounding Validator" = the existing `app/ai/guardrail.py`; its OpenRouter privacy restriction = already set via `data_collection: deny`). Per direct instruction not to replace existing analytics/recommendation logic, deliberately built only the two genuinely new, low-risk pieces the user selected: **(1)** a formal, versioned Constitution — the grounding/scope rules that previously lived as an inline f-string in `_EXPLAIN_SYSTEM_PROMPT_TEMPLATE` are now a standalone `backend/app/ai/orla_constitution.md`, reviewable and diffable independent of code changes, loaded once at import time. **(2)** a Personality layer, `backend/app/ai/orla_personality.md` — warm, professional, lightly Irish, explicitly subordinate to the constitution (tone only, never changes a fact or number). Deliberately declined the plan's larger Skills/SOPs YAML framework and a rebuilt "Findings Engine"/"Skill Router" — no real scaling pain exists yet against the current ~11-intent inline dispatch, and building it now would mean duplicating already-working, already-fire-tested infrastructure. Switched the explain prompt's assembly from `str.format()` to plain concatenation (the constitution/personality files are free-form prose that may contain a literal `{`/`}`, which `.format()` would misparse as a placeholder). Deliberately NOT applied to the classify step — that prompt already hit a real regression once from growing too large for its reasoning-token budget (see v1.24); adding a second block of prose there for no functional benefit (classify has no free-text "voice" to carry) wasn't worth the risk. 483 backend tests passing, plus a new regression test asserting both governance files actually reach the explain call's system prompt. |
| 1.31 | 08/08/2026 | Fixed a real finding-detection gap reported directly: "Which products are hurting my profitability even though they sell well?" got "I don't have any profitability figures in the data" — false; per-product margin data exists, there was simply no rule that could answer this specific question shape. `evaluate_products_at_loss` only ever catches an outright net loss (`gross_profit < 0`), and `rank_products_by_margin`'s top-5/bottom-5-by-gross-profit slices can leave a strong-revenue, thin-but-positive-margin product outside both windows entirely — neither the most nor least profitable in absolute terms. New `evaluate_thin_margin_high_revenue` rule (`app/analytics/findings.py`): re-ranks the full, unsliced product list by revenue (a new 4th return value on `rank_products_by_margin`, threaded through `FinancialPerformanceSummary` internally — never exposed on the API schema, an unbounded list has no place on the wire) and flags a top-5-by-revenue product whose margin sits 10+ points below the business's own overall average, skipping anything already caught by the at-a-loss rule. Flows automatically into the dashboard's existing Findings & Recommendations section and ORLA's `findings_recommendations` chat intent — no frontend changes needed (the existing generic finding/recommendation rendering already handles any new `finding_type`). A second issue in the same report — a follow-up question ("what was the previous period?") falling back to the generic out-of-scope message — is not a new bug: ORLA is deliberately single-turn (no conversation history resent as model context, a stated cost-control decision from Stage E, documented in `frontend/app/chat/page.tsx`'s own comment) and has no memory of the prior answer to refer back to. 483 backend tests passing (up from 477), including a dedicated integration test reproducing the exact "one dominant thin-margin bestseller among otherwise-healthy products" scenario by hand-computed numbers. |
| 1.30 | 08/08/2026 | Ran a live 20-question ORLA fire test against Gate B (representative subset of a formal 56-question stress-test document) — cross-tenant isolation, hallucination resistance, and prompt-injection resistance all held with zero findings. Found and fixed five real bugs: `out_of_scope`→`forecast` recovery for reorder-shaped questions; explain-step provider failures now correctly tagged `provider_unavailable` instead of the stale prior intent; the guardrail's `validate_grounded` gained an additive-only `question` parameter so a number the user themselves supplied (e.g. "€2,000") isn't wrongly flagged when the model echoes it back; numbered-list markers ("1.", "2.") in a multi-item answer are stripped before the numeric-claim scan, fixing a 100%-reproducible false rejection on every "give me N actions" style question; and `ClassifyResult` gained an optional `horizon_days` so "the next two weeks" correctly fetches a 14-day forecast instead of always the 7-day default. Also raised explain `max_tokens` 500→800 and added currency-formatting guidance to the explain prompt. See F29's changelog entry above for full detail. 477 backend tests passing (up from 470). |
| 1.29 | 07/08/2026 | Fixed three real ORLA chat issues found via a pasted user transcript, each diagnosed live before fixing: (1) a SKU-annotated product lookup ("...WorkshopPro Lubricant Plus (SKU-00175)...") returned zero matches — the combined "name (sku)" format is exactly what ORLA's own disambiguation labels use, so `ProductRepository.search_by_name_or_sku` now also tries a trailing "(...)" annotation split from the rest of the query; (2) "what's my biggest cost/expense" had no intent to land in — new `category_breakdown` chat intent reusing the existing `app/application/category_breakdown.py`, no new calculation logic; (3) a bare many-match follow-up reply (assumed by the user to be a "lost memory" bug) was actually the guardrail correctly rejecting an ungrounded explain-step answer — ORLA never had conversation memory to lose; the many-match message now models a real example follow-up question instead of "could you be more specific?" See F29's changelog entry above for full detail. 463 backend tests passing (up from 459). |
| 1.20 | 06/08/2026 | Removed the undo-ordering guard flagged as a likely-redundant follow-up in v1.19 — confirmed, then acted on. Under the old flat-sum stock model, a reconciliation's movement was a *delta* computed against whatever the running total happened to be at write time; undoing an earlier import afterward would silently invalidate that frozen delta, which is exactly what `ImportRecordRepository.has_later_completed_inventory_import` / `ImportSupersededByLaterInventoryImport` existed to block. Since v1.19 made a reconciliation store its own absolute `resulting_quantity_on_hand` instead — current stock is recomputed fresh from whatever's in the ledger every time it's read, filtered by `event_date`, never by write order — that hazard no longer exists: deleting a movement dated on/before the latest reconciliation changes nothing (it was already excluded from the total); deleting one dated after it correctly reduces the total by its own contribution. Verified this directly by replaying the exact scenario the guard was originally built for (sale, then a later reconciliation, then undo the sale) and confirming the resulting stock is correct, not silently wrong — the bug the guard prevented simply can't happen anymore. Removed the guard method, the exception class, its API handler, and the now-untestable tenant-isolation test for the guard's business-scoping (its whole premise no longer applies); rewrote the two "undo is blocked" tests into "undo succeeds and stock is still correct" tests instead of deleting them, so the exact original bug scenario stays under regression coverage going forward. Also fixed a bug introduced and caught during this same change: an edit to `app/imports/exceptions.py` briefly left an orphaned `__init__` override sitting inside `ImportNotReversible` (a leftover from the removed exception class) that silently broke its real constructor — caught immediately by the full test suite, fixed before commit. Verified via the backend suite (308 passing) and a live walkthrough against the running Docker stack: purchase dated before a later stock count, undo request now returns 200 (previously 409), stock correctly unchanged afterward. |
| 1.21-1.23 | — | Not recorded as separate rows here — these versions' changes (the Gate B synthetic-data pass, the schema-detection tie-breaking fix, the returns-support fix, and the `provider_unavailable` fix) were documented inline in F29's own changelog entry above instead of as table rows; noted here only so the version numbering isn't a silent gap. |
| 1.24 | 07/08/2026 | Extended F29's Gate B follow-up work: ORLA can now answer questions naming a specific date/range (`explicit_date` period, resolved against the business's actual local "today") and questions about one specific product, purchase/order, or repair by name/SKU/reference (`product_lookup`/`purchase_lookup`/`repair_lookup`, a shared zero/one/many-match deterministic dispatch — never a silent guess). `_classify_intent` now returns a `ClassifyResult` dataclass instead of a bare tuple. Found and fixed a real regression during live verification: the larger classify prompt this required pushed some questions into exhausting the classify call's reasoning-token budget with no visible output left — raised `max_tokens` 300 -> 700. See F29's changelog entry above for full detail. 424 backend tests passing (up from 384). |
| 1.28 | 07/08/2026 | Reports: Download PDF button (browser print, no new dependency), a Sections table of contents, and a real bug fix — "Top recommendations"/"Action Plan" showed the generic rule text with no product name at all, missing the finding-message pairing the dashboard already had. Dashboard: category filter + labels added to Forecast's "Products to watch" and to Findings & Recommendations (`get_findings`/`get_forecast` both gained `category_id`, but whole-business rules — revenue decline, low margin, incomplete cost data, high return rate — always read unfiltered figures, per direct instruction); Findings & Recommendations (and the report's Action Plan) now render as two visually separate groups, "Business performance" and "Stock & products," rather than one mixed list. Shared recommendation-list logic extracted into `frontend/lib/findings.ts` so both pages use one implementation. See F29's changelog entry above for full detail. 459 backend tests passing (up from 455). |
| 1.27 | 07/08/2026 | Fixed the category filter's real discoverability gap, found by direct feedback right after v1.26 shipped: with zero categories anywhere yet, the filter dropdown and every category label hid themselves entirely, making "no data yet" indistinguishable from "not built." Dropdown now always renders (disabled + hinted when empty). Assigned real categories to all 180 Gate B products via a genuine no-op inventory reconciliation import, and one-time-backfilled `sell_price` for 174 of them from their own sale history (the v1.26 fix only applies to sales imported after it shipped) — Category Breakdown now shows real, non-zero figures. See F29's changelog entry above for full detail. |
| 1.26 | 07/08/2026 | Product categories: data capture (new optional "category" upload field, match-or-create against `ProductCategory`, "latest wins" assignment — matches the existing `Product.category_id` schema that had zero writer before this), per-section dashboard filters (Financial Performance + Retail Operations, each independent), and a reports Category Breakdown table (revenue, expenses = purchase cost not COGS, stock value at sell price not cost). New `InventoryMovement.unit_cost` column closes a real gap found while confirming the "expenses" formula with the user: no per-purchase cost history existed anywhere before this. Two real bugs found live and fixed: every one of 180 real Gate B products had `sell_price = NULL` (new `ProductRepository.update_sell_price`, the missing counterpart to `update_cost_price`'s own v1.11 fix); loading an already-stored report crashed the page entirely on a field added earlier this session, fixed with defensive guards rather than assuming every stored report matches today's schema. See F29's changelog entry above for full detail. 455 backend tests passing (up from 424). |
| 1.25 | 07/08/2026 | Raised `ai_daily_request_limit_per_business` (PR-5.5) from 30 to 200, per direct instruction, after diagnosing a real point of confusion: adding real OpenRouter account credit does *not* unlock a paid model or spend that credit — the configured model (`openai/gpt-oss-20b:free`) is always `$0`/call by construction, and paying only raises OpenRouter's own free-tier daily quota (50 -> up to 1000/day). The thing actually blocking testing was this app's own, much lower, unrelated 30/day cap, confirmed live (`count_today_for_business` returned exactly 30/30 for the test business). 200 stays comfortably under OpenRouter's own new ceiling with zero cost impact at the current free-tier model. Updated `test_daily_usage_cap_blocks_further_ai_calls` to read the real configured limit instead of a hardcoded 30. Recorded the real production cost-control design for later, in "Future Improvements" below — this count-based cap is explicitly *not* that; a real launch on a paid model needs a spend-based per-business cap (using the already-logged `AIRequest.cost_eur`) tied to a fixed share of the €80/month plan price, tiering a client down to a free/cheaper model past ~50% of their allotted budget rather than hard-blocking. 424 backend tests still passing. |
