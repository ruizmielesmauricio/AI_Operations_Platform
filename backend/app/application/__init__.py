"""Application services — orchestrate domain logic and repositories on behalf
of thin API routes. This is where a route handler's use case (e.g. "import a
file", "generate a dashboard payload") is coordinated, without the route
itself containing business logic.
"""
