"""Core business logic and formulas — deterministic, no AI, no I/O.

Per CLAUDE.md's Core Rule ("Business Logic First"): every calculation,
aggregation, or validation the platform performs lives here or in
app/analytics/, in plain Python, fully unit-testable without a database or
network connection.
"""
