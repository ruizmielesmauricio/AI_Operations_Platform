# Integration tests

Tests that hit a real (local/test) Postgres database — e.g. repository
methods, Alembic migrations applying cleanly, import pipeline round-trips.
Empty until the first repository lands (roadmap Phase 2).
